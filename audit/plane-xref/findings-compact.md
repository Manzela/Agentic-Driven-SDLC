# Verified findings (compact) — 52 confirmed/revised


## P0 AI-03 — UPD-E7-STATUS patched only 1 of 7 targets — applier ignores additional_match_keys; REQ6/REQ27/REQ11/REQ12/REQ23/E6-S5 all unreconciled
- items: story:ASCP-E7-REQ16 (seq 54, landed), story:ASCP-E7-REQ6 (seq 49), story:ASCP-E7-REQ27 (seq 52), story:ASCP-E7-REQ11 (seq 50), story:ASCP-E7-REQ23 (seq 53), story:ASCP-E7-REQ12 (seq 51), story:E6-S5 (seq 46)
- note: CONFIRMED — a distinct sibling of the GT-D3 collision class, not a restatement (GT-D3 = two ops colliding on one id; this = one op meant to fan out to seven targets but hitting one). Verified: plan op UPD-E7-STATUS match_key='story:ASCP-E7-REQ16' + additional_match_keys=[REQ6,REQ27,REQ11,REQ23,REQ12,E6-S5] (6 keys). resolve() (138-148) reads only op['match_key'] — never additional_match_keys. apply_log shows exactly ONE update on 73e5a0ce (REQ16). Live board: REQ16=Backlog with C16 text (landed). The other six UNTOUCHED with raw EARS desc: REQ6=In-Verification; REQ27=Human-Review; REQ11=Agent-Executing + labels[human:review]; REQ12=Agent-Executing + labels[human:review]; REQ23=Blocked + relations all-zero (no blocker edge, S8); E6-S5=Done (S6). REQ11/REQ12 still pair human:review with Agent-Executing (S7). Report line 367 claims C16/S6/S7/S8 all READY after apply. Claim!=reality decisively.
- action[field-edit]: Apply the C16 true-up individually to REQ6/REQ27/REQ11/REQ23/REQ12/E6-S5: state->Backlog (evidence-outrunning states off); strip label human:review where it co-occurs with Agent-Executing on REQ11/REQ12 (S7); for REQ23 either POST blocked_by to its real prereq (NEW-11/NEW-12) or move off Blocked (S8); move E6-S5 off Done (S6). Also fix resolve() to honor additional_match_keys (fan-out) or split UPD-E7-STATUS into 7 single-target ops.

## P0 AO-01 — All 13 created NEW items are oracle-free on the live board: the applier silently drops every op's acceptance_criteria[] array
- items: ascp-audit-reorg:NEW-01, ascp-audit-reorg:NEW-02, ascp-audit-reorg:NEW-05, ascp-audit-reorg:NEW-06, ascp-audit-reorg:NEW-08, ascp-audit-reorg:NEW-10, ascp-audit-reorg:NEW-13, ascp-audit-reorg:NEW-14
- note: Independently confirmed on live board + in source. apply_upsert_plan.py:186-193 builds `body` from ONLY description_html / state / labels; the string 'acceptance_criteria' appears NOWHERE in the file (verified by reading the full script). Live GET work-items for every NEW item: the description_html carries only the rationale paragraph and ZERO AC text. Substring probes against the plan's own AC strings all returned ABSENT: NEW-01 ('Spine wired: 5 governance', 'fails OPEN with a diagnostic'), NEW-05 ('CI assert proves CLAUDE_AGENT_NAME', 'test_subagent_stop_actor', 'tools[] allowlists'), NEW-08 ('validate_spec()', 'UNMAPPED completeness', '60s timeout sentinel'), NEW-02 ('block_streak','BLOCKED-ON','FORBIDDEN'), NEW-10 ('ruff/.py','Fails OPEN'), NEW-06 ('flagd kill-switch OFF','a test proves a still-enabled','same predicate'), NEW-14 ('blocked-by edge to UPD-E8-13','Roadmap-by-phase'). The single 'byte-for-byte' hit in NEW-05 is in the rationale prose, not an AC. report plane-reconciliation-report.md:276 (§5.1) titles these 'the load-bearing few, verbatim-grade'. CLAIM!=REALITY genuinely holds RIGHT NOW. This breaks CH-07 (located-oracle-before-done) on the very items created to enforce it, including the priority:blocking Phase-0 items NEW-01/02/05/08. Not a duplicate of GT-D1/D2/D3 (those are mis-resolution + missing relations; this is the AC-array drop on creates).
- action[update]: Fix apply_upsert_plan.py to render each op's acceptance_criteria[] into the POST/PATCH description_html (append '<h4>Acceptance criteria</h4><ul><li>...</li></ul>') for BOTH create and update paths, then re-run --apply. Backfill all 13 created items (NEW-01..11,13,14). Order: NEW-05 (COH-1 gate, see AO-02), then the other priority:blocking NEW-01/02/08, then the high/normal tail. This is the single highest-leverage fix: it restores the checkable 'done' to every owning item the prior pass created.

## P0 AO-02 — NEW-05's COH-1 apply-step-0 hard-gate oracle (AC3 byte-for-byte CI assert) never landed — the blocking pre-gate is unverifiable on the board
- items: ascp-audit-reorg:NEW-05, story:ASCP-E8-13
- note: Confirmed. plane-upsert-plan.json _meta.apply_step_0_gate: 'COH-1 ... encoded as AC3 on NEW-05'; AC3 = a CI assert that CLAUDE_AGENT_NAME resolves byte-for-byte to 'verifier.md' across pre_tool_use_hook.py, subagent_stop_hook.py, test_subagent_stop_actor.py, test_pre_tool_use_authority.py. Live NEW-05 (seq 254) description_html is 639 chars of option-(a) decision prose only: substring probes for 'CI assert proves CLAUDE_AGENT_NAME', 'test_subagent_stop_actor', 'tools[] allowlists' all ABSENT. So the runnable oracle for GT-7 / COH-1 (the fail-closed-but-stuck risk: gates compare against the literal 'verifier.md' but no agent frontmatter carries it, GT-2) cannot be mechanically checked from the board before an executor applies the actor gates (UPD-E2-S2, UPD-E4-S1/S3). This is a specific, more-severe instance of AO-01 on the designated step-0 gate; kept separate because the apply-order semantics make it independently P0.
- action[update]: Append NEW-05 AC1-AC4 verbatim to its description, foremost AC3 (the byte-for-byte CI assert over pre_tool_use_hook.py / subagent_stop_hook.py / test_subagent_stop_actor.py / test_pre_tool_use_authority.py). Phrase the step-0 gate as a pass/fail predicate ('CI assert COH-1 passes') not an imperative ('reconcile the names'). Add a relates_to/blocked-by edge once relation support exists (AO-04) so the actor-gate stories (UPD-E2-S2, UPD-E4-S1/S3) are visibly downstream of NEW-05.

## P0 BP-1 — E8-13.1 (seq 211) settings-registration AC still says 6 hooks + PreCompact + matcher 'Write|Edit|MultiEdit' — contradicts the staged 01-settings.json (5 hooks, no PreCompact, matcher includes Bash) the SAME pass authored
- items: 211, story:ASCP-E8-13, 262, ascp-audit-reorg:NEW-11
- note: INDEPENDENTLY CONFIRMED against the live board RIGHT NOW. Live work-item ca0e7cb3 (seq 211, task:ASCP-E8-13#0): NAME='ASCP-E8-13.1 Author .claude/settings.json six-hook command registration'; live description_html='...Register Stop, PreToolUse, PostToolUse (matcher Write|Edit|MultiEdit), SubagentStop, SessionStart, PreCompact as type:command...'. On disk audit/steering-audit/proposed/01-settings.json registers exactly 5 events (SessionStart, PreToolUse, PostToolUse, SubagentStop, Stop), has NO PreCompact, sets env SPINE_REQUIRED_EVENTS='PreToolUse,PostToolUse,Stop,SubagentStop,SessionStart', and _comment_governance says 'registers 5 of the 6 designed governance events ... PreCompact is deferred to D7 ... wired as-scoped at 5/6'. 01-settings.json PreToolUse matcher is 'Edit|Write|MultiEdit|Bash' (L27); the live AC matcher 'Write|Edit|MultiEdit' OMITS Bash (the anti-tee-bypass fix #63787). GT-1/GT-3: no .claude/settings.json and no pre_compact hook on disk, so building seq 211 verbatim registers a non-existent PreCompact command (partial/broken spine) and drops Bash (re-opening the shell-redirection bypass). Genuine sibling of GT-D3: a prior UPDATE whose intended content never landed on the live AC. P0 upheld — precondition story for every other gate. Evidence: live seq 211 name+desc; 01-settings.json L3/L9/L27/L39; GT-1,GT-3.
- action[update]: Rewrite seq 211 (external_id task:ASCP-E8-13#0) to match the as-scoped decision the prior pass already committed to in 01-settings.json. NAME -> 'ASCP-E8-13.1 Author .claude/settings.json five-hook (as-scoped) command registration'. DESC -> 'Register Stop, PreToolUse (matcher Edit|Write|MultiEdit|Bash), PostToolUse (matcher Edit|Write|MultiEdit), SubagentStop, SessionStart (matcher startup|resume|compact) as type:command (task 9.1). Set enabledPlugins ralph-loop@claude-plugins-official:false. Set env SPINE_REQUIRED_EVENTS to those five registered events. The Bash leg on PreToolUse is the anti-tee-bypass fix (#63787, best-practices G8). PreCompact is the 6th designed event, DEFERRED to NEW-11 (seq 262, D7) — do NOT register it here until its hook is authored on disk. Confirm the .claude/agents/*.md subagents auto-discover by frontmatter name. Write tests/smoke/test_hook_config.py.' Add a relates_to edge seq 211 -> NEW-11 (seq 262) as the item that re-adds PreCompact + re-widens SPINE_REQUIRED_EVENTS.

## P0 COV-01 — NEW-12 never created on the live board; REQ-INFRA-005 still points at a non-existent NEW-12 (plan root cause: only create carrying a match_key)
- items: 20, story:REQ-INFRA-005, ascp-audit-reorg:NEW-12
- note: Independently confirmed against LIVE API: paging all 256 work-items, external_id 'ascp-audit-reorg:NEW-12' = MISSING. REQ-INFRA-005 (df8b5217) is live (state Spec-Verified) and its description_html STILL reads '...Reframe AC#4 to depend on NEW-12 authoring the migrations...' — a dangling reference to an item that does not exist. .apply_log.json: 13 creates / 26 updates (report+plan claim new:14, update:25 at report lines 56/60); df8b5217 is the ONLY id hit by two update ops (the migration-authoring op and the REQ-INFRA-005 true-up both target df8b5217; the second clobbered the first). On-disk: zero *.sql files (GT-5 reconfirmed) so the eight .kiro migrations have no owning item. Report line 365 (Active Sprint) lists 'NEW-01/02/05/08/12 (blocking)' citing the non-existent item. claim!=reality holds RIGHT NOW. Roots/extends GT-D1 (adds applier root cause + dangling ref + false sprint claim), not a bare restatement.
- action[create]: Create the missing owning item as external_id ascp-audit-reorg:NEW-12 (by external_id, NOT match_key): name 'Author eight .kiro migrations 001-008 + run_state completeness + gate_audit_log append-only grant', module E1, cycle Phase 0, labels phase:0/type:wiring/priority:blocking, state Backlog; port AC1-AC4 from report §5.1. Applier root-cause fix: forbid op:create from carrying a match_key, or hard-error when a create's match_key resolves to an existing id (it currently silently demotes to update). Then field-edit REQ-INFRA-005 so its 'NEW-12' reference resolves to the real item.

## P0 COV-02 — Total relation vacuum: NEW-14's blocked-by topology never landed; every probed item returns all relation buckets = 0
- items: 251, ascp-audit-reorg:NEW-14, story:ASCP-E8-13, story:E2-S1, story:ASCP-E5-S10, story:ASCP-E7-REQ23, ascp-audit-reorg:NEW-11, story:ASCP-E7-REQ11
- note: Independently confirmed via LIVE GET /work-items/{id}/relations/: E8-13 (110f71c8), NEW-14 (82f43d5b), E2-S1 (bf3e17e3), E5-S10 (76e1a1a6), E7-REQ23 (09176986), NEW-11, E7-REQ11 — ALL buckets empty (ALL_ZERO). NEW-14 is live (Spec-Verified) as a story DESCRIBING blocked-by edges that do not exist. apply_upsert_plan.py PATCHes only description_html/state/labels (no relation POST). Report §6 (lines 365/367) claims 'NEW-14 ensures the sprint's dependency-ordering cannot schedule a hook-consuming story ahead of E8-13' and Workflow-Kanban 'READY after apply' — unsupported. Generalizes GT-D2 (which asserts only E8-13) to the whole board + the false readiness claim. Note: GT-D2 cited E8-13 id a120af4f but live E8-13 by external_id is 110f71c8 (the A1 target in the apply log); relations are empty on 110f71c8 regardless.
- action[relation]: Extend the applier with relation-creation (POST /work-items/{id}/relations/), then add blocked_by edges from each hook-consuming story (E2-S1/S2/S3, E3-1/-2/-4/-22, E4-S1/S2/S3/S4, E5-S10, E6-S2/S3/S7, E7-REQ6/11/12/16/23/27, E8-14/18/21/26) to E8-13 (110f71c8) plus the ralph-disable leg. Until edges exist, field-edit NEW-14 off Spec-Verified back to Backlog/NOT-DONE since its AC1/AC2 (edges exist) are unmet.

## P0 HGD-1 — No board item owns the phantom-field→content-diff fix; live PreToolUse status/in_scope authority is inert on every real Edit/Write (and human_signed is read from the payload too)
- items: 34, 35, 55, 211, 253, 254, 256
- note: INDEPENDENTLY CONFIRMED against live disk + live board. (1) Live .claude/hooks/pre_tool_use_hook.py:37 `field = (tool_input or {}).get("field")`; lines 41/44 gate R1/R2 on `field == "status"` / `field == "in_scope"`. A real Edit payload is {file_path,old_string,new_string} and Write is {file_path,content} — neither carries `field`, so the entire status/in_scope authority branch is dead pre-write. (2) Whole-board scan of all 256 snapshot desc_text for phantom|content-diff|_changed_coverage|old_string|new_string|JSON delta returned NONE — no item carries an AC to replace tool_input['field'] with a diff. (3) Live E4-S2 (seq34) and E4-S3 (seq35) descs say only 'EXTENDS the unified hook (NEW-10)' = the PostToolUse five-layer leg; NEW-10 (seq253) desc confirms it 'builds the one [PostToolUse] hook both stories extend' — not the PreToolUse authority predicate. Artifact 03-pre_tool_use-steering.md §2 (`_changed_coverage_fields`) + §3 (the four AP-14 regression tests) is the load-bearing fix and is unowned. SIBLING DEFECT of the same class, confirmed in the same file: live line 74 `human_signed=bool(ti.get("human_signed", False))` reads the in_scope authority signal FROM tool_input — contradicting the hook's own docstring (line 13 'never from the payload') and artifact 03 fix #1; an agent can set human_signed=true in its own Write payload and self-exempt an in_scope flip even after the field predicate is fixed. Both are the same generalization gap.
- action[create]: Create a Phase-0 blocking story 'Generalize pre_tool_use_hook.py authority gate (artifact 03): phantom-field→content-diff + out-of-band human_signed'. ACs: (1) replace the tool_input['field'] branch with _changed_coverage_fields diffing old_string/new_string (Edit) and on-disk-content-vs-content (Write); (2) COVERAGE_MODEL_PATHS + PROTECTED_PREFIXES read from tools/execution_bounds with current literals as defaults; (3) human_signed resolved out-of-band via _resolve_human_signed (HUMAN_SIGNED env / .claude/approvals/{session_id}.human-signed), NEVER from tool_input — fixes the live line-74 self-exempt hole; (4) pin the R1-R5 verbatim catalog + the four AP-14 + two self-exemption regression tests from artifact 03 §3. resolves:['G6','AP-14','A-INTENT-01','artifact:03']. Block it by E8-13/E8-13#0 (registration) and NEW-05 (verifier.md literal). Closest existing home if update-over-create preferred: task:ASCP-E4-S3#3 or a new E4-S1 subtask.

## P0 HGD-2 — Live PreToolUse matcher spec is Write|Edit|MultiEdit (no Bash) — the protected-path anti-workaround bypass is open and unowned
- items: 211, 55
- note: INDEPENDENTLY CONFIRMED via live API. GET work-items/{E8-13#0=ca0e7cb3}/ live description verbatim: 'Register Stop, PreToolUse, PostToolUse (matcher Write|Edit|MultiEdit), SubagentStop, SessionStart, PreCompact as type:command (task 9.1)'. No Bash in the matcher. Whole-board scan of 256 items for matcher.*Bash | Edit\|Write.*Bash | Bash.*redirect returned NONE — no item owns adding Bash. Live pre_tool_use_hook.py:52 R3 string is a bare verdict 'protected artifact may not be edited by an agent: {path}' with no anti-workaround clause. Artifact 03 §5 explicitly flags this as the bypass A1 must close ('ensure the matcher covers Bash, not only Edit|Write') and §1 R3 carries the 'Do NOT write it via Bash redirection, tee...' clause. Wiring the spine as seq 211 specifies ships the #63787 tee-bypass on day one.
- action[update]: Update story:ASCP-E8-13 (seq55) and subtask task:ASCP-E8-13#0 (seq211): change the PreToolUse matcher from Write|Edit|MultiEdit to Edit|Write|MultiEdit|Bash, and add an AC requiring the live pre_tool_use_hook.py R3 deny string to carry the CH-05 anti-workaround clause ('Do NOT write it via Bash redirection, tee, or another tool — the same authority check applies') plus a named HANDOFF-to-main out, and to distinguish a Bash-write-to-protected-path from benign Bash. Cross-reference artifact 03 §1/§5.

## P0 HGD-3 — COH-1 fail-closed-but-stuck contradiction is UNFIXED on the live board: subtask seq 131 still specifies suffix-less `name: verifier`
- items: 131, 254, 25, 60
- note: INDEPENDENTLY CONFIRMED via live API. GET work-items/{E4-S3#0=55e5291b}/ live description verbatim: 'Write frontmatter (name: verifier, model: claude-opus-4-8, tools: [Read, Grep, Glob, Bash])'. The hooks pin the literal 'verifier.md' (pre_tool_use_hook.py:25 VERIFIER_AGENT='verifier.md'; subagent_stop_hook.py:30 same) and actor_identity resolves actor from CLAUDE_AGENT_NAME (GT-7). A suffix-less verifier resolves to actor 'verifier' != 'verifier.md', blocking every proven-flip (fail-closed-but-stuck). NEW-05 (seq254) live desc records 'DECISION (apply-step-0): take option (a) name: verifier.md' and E8-20 (seq60) live desc adds 'VERIFIER_ROLE default MUST be byte-consistent with the COH-1 reconciliation (verifier.md)' — so two/three live items now give OPPOSITE frontmatter instructions, and the brittle one (seq131) is the buildable subtask an implementer follows verbatim. Prior pass created the right-convention owning item but never edited the contradicting subtask.
- action[update]: Update task:ASCP-E4-S3#0 (seq131): change 'name: verifier' to 'name: verifier.md' and append 'COH-1: the frontmatter name MUST be byte-identical to the hooks VERIFIER_AGENT literal verifier.md; a suffix-less name fail-closes every proven-flip (GT-7).' Apply the same edit to any sibling specifying a bare role name: check task:ASCP-E4-S1#0 (seq119, 'name: implementer') for the implementer.md analogue and E6-S4 (seq45) dropdown values. Add a blocked_by edge from E4-S3 (and E4-S1) to NEW-05 (see HGD-5).

## P0 PSD-01 — NEW-14 dependency edges never landed: all sampled spine/consumer items have empty relation buckets on the live board
- items: 55, 251, 37, 61, 53, 44, 50
- note: Independently confirmed against live API on 2026-06-18. GET /work-items/{id}/relations/ returned all 8 buckets empty (blocking/blocked_by/relates_to/duplicate/start_after/start_before/finish_after/finish_before = 0) for every item queried: E8-13 (story:ASCP-E8-13, live id 110f71c8-d196-482d-a807-fc6463874523, seq55), NEW-14 (seq251), E5-S10 (seq37), E8-21 (seq61), E7-REQ23 (seq53), E6-S3 (seq44), E7-REQ11 (seq50). ID CORRECTION worth recording: PSD-01 cites E8-13 as 110f71c8 which is CORRECT (it is the story); GT-D2 cited a120af4f which is actually task ASCP-E8-13#3 (seq214), a wrong id — the relation target must be 110f71c8. All 25 named consumer external_ids verified present in the live board (0 missing). plane-upsert-plan.json contains 0 relation operations; apply_upsert_plan.py has no relation-creation code path (only PATCHes description_html/state/labels via body + POSTs module/cycle membership); .apply_log.json has 0 'relation' entries. NEW-14 is a description-only placeholder. This is a genuine claim!=reality P0 that scales and quantifies GT-D2 (27 items vs GT-D2's single E8-13 sample) and falsifies report §5.1 NEW-14 AC1/AC2, §6 Active-Sprint, §7 Milestone-1.3 which assert the blocked-by topology as applied board state. Not a duplicate of GT-D2: it verifies the full consumer set and the report's topology guarantee.
- action[relation]: Create 25 blocked_by relations via POST /work-items/{consumer_id}/relations/ with E8-13 (live id 110f71c8-d196-482d-a807-fc6463874523, story:ASCP-E8-13 — NOT a120af4f) as the blocker. Consumers (all verified present, all buckets empty): story:E2-S1, story:E2-S2, story:E2-S3, story:ASCP-E3-1, story:ASCP-E3-2, story:ASCP-E3-4, story:ASCP-E3-22, story:ASCP-E4-S1, story:ASCP-E4-S2, story:ASCP-E4-S3, story:ASCP-E4-S4, story:ASCP-E5-S10, story:E6-S2, story:E6-S3, story:E6-S7, story:ASCP-E7-REQ6, story:ASCP-E7-REQ11, story:ASCP-E7-REQ12, story:ASCP-E7-REQ16, story:ASCP-E7-REQ23, story:ASCP-E7-REQ27, story:ASCP-E8-14, story:ASCP-E8-18, story:ASCP-E8-21, story:ASCP-E8-26. The same 25 edges satisfy AC1 and AC2 (ralph-disable folded into E8-13). Re-read each relation back before flipping NEW-14 (seq251) Spec-Verified->Done. Extend apply_upsert_plan.py with a relations writer so this is not a manual one-off.

## P0 RED-01 — acceptance_criteria is a phantom field: applier drops it for ALL 39 ops, so the R1/R5/R6/COH-1 contracts (NEW-10 AC2, E8-21 AC2/AC3, NEW-03 AC6, E8-20 AC3) never landed; R5 scope contract is live-prose-only with zero relations
- items: 56, 61, 60, 34, 35, 253, 259, 256, 262, 191, 20
- note: VERDICT revised: core CONFIRMED, sub-claim mis-scoped. INDEPENDENTLY CONFIRMED against live board + plan on-disk:
(1) PHANTOM FIELD: plane-upsert-plan.json carries acceptance_criteria on 39/39 ops (union-of-keys verified). apply_upsert_plan.py lines 187-194 build body from ONLY description_html/state/labels; acceptance_criteria is never read, never rendered into description_html. .apply_log.json fields == [description_html,state,labels] for all ops. So every AC was silently dropped. This is a NEW mechanism, not a restatement of GT-D2/D3 (those are relations + apply-collision); this is a distinct data-loss path.
(2) NAMED LOST CONTRACTS confirmed in the plan's acceptance_criteria but ABSENT from live description_html: NEW-10 AC2 (R1 'E4-S2/E4-S3 EXTEND this hook; neither re-registers'); E8-21 AC2 (R5 boundary 'owns ONLY escalation/HANDOFF rungs ... the boundary is tested') + AC3 (R6 cite NEW-03/NEW-13 by id); NEW-03 AC6 (THE single canonical HANDOFF contract for the 5 anti-idle layers); E8-20 AC3 (COH-1 VERIFIER_ROLE default == 'verifier.md' byte-for-byte + ImportError fallback test). Live E8-14/E8-21 desc mention 'R5'/'scope contract' in PROSE only; no AC#, no boundary test spec.
(3) ZERO RELATIONS: live /relations/ for E8-14(56), E8-21(61), NEW-10(253), E4-S2(34), E4-S3(35), NEW-03(259), NEW-09(256), NEW-11(262), E8-20(60), E7-REQ11(50/516) ALL return blocking/blocked_by/relates_to/duplicate = 0. The R5 scope contract is asserted in prose with no relates_to edge between E8-14<->E8-21; NEW-14's promised blocked_by edges absent (GT-D2 sibling, do not re-report as new — cross-ref only).
(4) NEW-11 vs E7-REQ11.3 (seq 191 'pre_compact_hook.py checkpoint writer + baseline-hash producer') is a REAL overlap with NEW-11 ('PreCompact resume_state_hash checkpoint hook'); CONFIRMED but NEW-11 adds COH-2 re-add-to-spine semantics REQ11.3 lacks, so it is overlap-not-pure-dup -> relates_to, not cancel.
(5) E6-S5 (story:E6-S5) is live in DONE state for a bidirectional state-mirror story the spine (never fired, GT-1) cannot have completed -> off-Done PATCH valid.
CORRECTION (why revised, not confirmed): the finding calls 'NEW-09/256 + E8-20/60 both own execution_bounds' a created dup. REFUTED as accidental-dup: NEW-09 is a DESIGNED conditional fallback -- its live desc reads 'Conditional create. Only instantiate if UPD-E8-20 is NOT taken ... skip this create unless that fold is rejected', and it sits in BACKLOG (not Spec-Verified) while E8-20 took the ownership fold. It is a dormant either/or, not an accidental clobber. It SHOULD still be cancelled now the fold is taken, so the action survives, but the 'dup' framing is wrong. Severity stays P0: the dropped R1/R5/R6/COH-1 contracts are the load-bearing coordination semantics that prevent two stories from conflicting-editing evaluate_stop and prevent VERIFIER_ROLE drift (GT-7 fail-closed-but-stuck); losing them silently breaks the spine's coherence guarantees.
- action[update]: FIX-THE-PIPELINE (root cause, do once): patch apply_upsert_plan.py to (a) render op['acceptance_criteria'] into description_html as an <h4>Acceptance criteria</h4><ol> block appended to the existing desc, and (b) emit relation POSTs to /work-items/{id}/relations/. Then RE-APPLY so the dropped ACs persist. CONTENT EDITS (per item): UPD E8-14 -> append AC1/AC2 (R5: owns 005_run_state.sql migration + reentrancy-guard half; boundary tested); UPD E8-21 -> append AC1/AC2/AC3 (R5 escalation-rungs half + R6 cite-by-id); UPD NEW-10 -> append AC2 (R1 E4-S2/E4-S3 EXTEND not re-register); UPD NEW-03 -> append AC6 (canonical HANDOFF contract for the 5 layers); UPD E8-20 -> append AC2/AC3 (VERIFIER_ROLE byte == 'verifier.md' + ImportError fallback test). RELATIONS: add relates_to E8-14<->E8-21 (R5); add blocked_by E4-S2->NEW-10 and E4-S3->NEW-10 (R1); add relates_to NEW-03->{E8-14,E8-21,E5-S10,E6-S2,E6-S3} (R6 5-layer cite); add relates_to NEW-11->E7-REQ11 (seq 191) NOT cancel (it adds COH-2). FIELD-EDIT: cancel/close NEW-09 (E8-20 took the fold); PATCH E6-S5 off Done -> Backlog (spine never fired, GT-1).

## P0 TWL-01 — CH-10 escalate-don't-re-inject rung never implemented; the 49x loop fix is unactioned finding-text inside E5-S10's live description
- items: 37, 142, 143, 144, 145, 61, 234, 235, 236, 237, 238
- note: Independently confirmed against live board + on-disk. E5-S10 (seq 37, id 76e1a1a6, updated 2026-06-18T17:39) live description_html ends VERBATIM: 'ALSO omits the CH-10 escalate-don't-re-inject rung that directly closes C-LOOP-04 — add it.' That is unactioned finding-text, not an AC. Live descriptions of all four E5-S10 subtasks (142/143/144/145) and all five E8-21 subtasks (234-238): 234=HANDOFF-before-block ordering, 235=lex-specialis tiebreak, 236=reentrancy composition, 237=Z3 fixtures, 238=Plane mirror — NONE implements block_streak / _escalate / a rung ladder. On-disk .claude/hooks/stop_hook.py (full, 219 lines) has NO _escalate, NO _block_streak, NO block_streak read; the unproven gate (lines 174-179) emits a bare 'Stop blocked: N in-scope item(s) not proven: [ids]' with no escalation. Report line 102/122 marks C-LOOP-04 RESOLVED; lines 179/336 say 'add the CH-10 rung'. CLAIM(resolved)!=REALITY(unauthored) holds NOW. Correction to prior evidence: 02-stop_hook §1 carries a _block_streak FALLBACK (lines 79-89) that varies the string even with no D3 populator, so the proposed mechanism is NOT 'the only thing' preventing re-injection and does not depend on NEW-02 — this strengthens the finding: fully specced yet zero board ACs implement it. P0 stands.
- action[update]: UPDATE story:ASCP-E5-S10 (seq 37): replace the trailing finding-text with a concrete AC, AND add/repurpose a subtask under the single owner (E8-21, per TWL-04) implementing the CH-10 ladder per 02-stop_hook §1/§2.3: rung-0 locate+act, rung-1 acknowledge-repeat+redirect (single _escalate template), rung>=BLOCK_ESCALATION_CAP=3 -> HANDOFF (exit 0). Require a unit test asserting a repeated identical block escalates (string differs at rung-1) then converts to HANDOFF, never re-injects byte-for-byte. Since 02-stop_hook ships a hook-owned fallback streak, the test must NOT depend on NEW-02 being wired.

## P0 TWL-02 — Reentrancy guard specced on the wrong signal (run_state.stop_hook_active self-mutex) — will not suppress Claude Code's forced-continuation re-injection
- items: 142, 236, 56, 61
- note: Confirmed against live ACs and on-disk. E5-S10.1 (seq 142, live desc): 'Implement with_reentrancy_guard(fn): set stop_hook_active=True; skip if already set; release only on allow.' E8-21.3 (seq 236, live desc): 'with_reentrancy_guard(evaluate_stop) via run_state.stop_hook_active; release the flag only when the blocking condition clears. Unit test asserts no cascade during a blocking cycle.' Both describe a hook-WRITTEN self-mutex keyed on run_state. The canonical mechanism (02-stop_hook §1 is_reentrant lines 47-56 + order-of-ops 292-296; charter CH-07 line 160) READS event.get('stop_hook_active') from the Stop event stdin and returns 0 FIRST. On-disk stop_hook.py main() (lines 201-215) never reads stop_hook_active (net-new). Claude Code writes the event payload, not run_state, so a run_state-keyed mutex 'released only on allow' cannot detect re-entry (and would in fact stay set across a blocking cycle — opposite of allow-on-reentry). E8-21.3's only test ('no cascade during a blocking cycle') tests the self-mutex, not event-level zero-token re-entry. Report success criterion #4 (line 442) 'A Stop re-entry emits zero tokens' is correct but no board AC asserts it. CLAIM!=REALITY holds.
- action[update]: UPDATE task:ASCP-E8-21#2 (seq 236) and task:ASCP-E5-S10#0 (seq 142): the guard MUST read event['stop_hook_active'] from the Stop event payload as the FIRST action in main() and return exit 0 with zero stdout/stderr (02-stop_hook §1 is_reentrant + order-of-operations). Remove the run_state.stop_hook_active set/'release-only-on-allow' self-mutex framing. Add AC + unit test: a Stop event carrying stop_hook_active:true produces exit 0 and emits zero steering tokens (report success criterion #4).

## P0 TWL-03 — NEW-02 (D3 populator) has zero relations and zero ACs on the live board — the HANDOFF/idle fix depends on it but the dependency and its load-bearing test exist only in prose
- items: 258, 61, 56, 37
- note: Confirmed live. GET /work-items/{NEW-02 id 80d42539}/relations/ returns EMPTY-ALL (every bucket empty); E8-21, E8-14, E5-S10, E8-13, E5-S10.1, E8-21.3 ALL return EMPTY-ALL too — consistent with GT-D2 (apply_upsert_plan.py has no relation-create capability). NEW-02 live description (411 chars) is rationale prose only ('This story owns the writer. Lands LAST in Phase-0...') — it does NOT carry report §5 AC1 (counter-list write), AC2 (BLOCKED-ON sentinel parser), or AC3 ('a test proves a 3-streak triggers HANDOFF, not re-injection', report line 295). NEW-03/10/13 live descriptions are also prose-only (hasAC=False). Report line 123 marks C-IDLE-03 'RESOLVED (behavior live after D3)' and line 419 calls NEW-02 the milestone 'that turns anti-loopmaxxing from inert to firing', yet it is an unactioned stub with no consumer edges, so apply-order can schedule consumers before the populator. CLAIM!=REALITY holds. SIBLING of GT-D1 (NEW item created as stub) and GT-D2 (relations never created), not a restatement — the specific NEW-02 instance with AC3's test missing.
- action[update]: UPDATE ascp-audit-reorg:NEW-02 (seq 258) to carry report §5 AC1-AC3 verbatim, especially AC3 (a 3-streak -> HANDOFF, not re-injection) and AC2 (BLOCKED-ON: <dependency> -> run_state.external_blocker). THEN add blocked_by edges via the LIVE relations endpoint (NOT apply_upsert_plan.py — GT-D2 class) from E8-14, E8-21, story:ASCP-E5-S10, ASCP-E3-4, ASCP-E4-S1, E6-S2/S3/S5 to NEW-02 per report line 153. Caveat in the AC that 02-stop_hook's hook-owned fallback streak makes the ladder non-degenerate-until-wired, so NEW-02 unblocks the EXACT signal, not the only signal.

## P0 WF-01 — C16/S6/S7/S8 state true-up landed on only 1 of 7 targets — six additional_match_keys silently dropped (sibling of GT-D1/D3)
- items: story:ASCP-E7-REQ16, story:ASCP-E7-REQ6, story:ASCP-E7-REQ27, story:ASCP-E7-REQ11, story:ASCP-E7-REQ23, story:ASCP-E7-REQ12, story:E6-S5
- note: INDEPENDENTLY CONFIRMED on the live board 2026-06-18. Tried to refute and could not. Live GET /work-items/: REQ16 (73e5a0ce)=Backlog, updated_at=2026-06-18T17:40:56, and its description_html carries the full C16 true-up text ('status claims outrun on-disk reality'). The other six are ALL untouched: REQ6=In-Verification, REQ27=Human-Review, REQ11=Agent-Executing, REQ23=Blocked, REQ12=Agent-Executing, E6-S5=Done — every one updated_at=2026-06-17T22:32 (original creation stamp) and every one still carries its original '<div><p><b>EARS requirements</b>' description with C16-text=False (verified by string search on live description_html). apply_log records UPD-E7-STATUS as ONE action against the single id 73e5a0ce; upsert-plan operations[38] lists match_key=story:ASCP-E7-REQ16 + additional_match_keys=[REQ6,REQ27,REQ11,REQ23,REQ12,E6-S5]. The applier resolved only the primary match_key. Exactly the GT-D1/D3 collision class (additional_match_keys are not iterated). 1/7 = ~14% applied. Severity P0 upheld: this is the spine's state-honesty milestone and 86% of it never landed.
- action[field-edit]: Issue six independent state PATCHes to Backlog (state id d83bd05d-f8a2-4440-ad90-00596609af6c): REQ6, REQ27, REQ11, REQ23, REQ12, E6-S5 (resolve each external_id to its live id via /work-items/ — note GT-D2's a120af4f for E8-13 is stale; resolve fresh), and re-apply the C16 true-up description_html to each of the six. ROOT-CAUSE fix in apply_upsert_plan.py: fan out additional_match_keys into N independent resolve+PATCH calls, emitting one apply_log action per resolved id, so a multi-target op can never collapse to a single id again. Then read-back-verify each (GT-D3 closure).

## P0 XC-01 — C16 true-up landed on 1 of 7 — applier ignores additional_match_keys; 6 E7/E6 stories still carry states that outrun on-disk reality
- items: 49, 50, 51, 52, 53, 46, 54, story:ASCP-E7-REQ16, story:ASCP-E7-REQ6, story:ASCP-E7-REQ27, story:ASCP-E7-REQ11, story:ASCP-E7-REQ23, story:ASCP-E7-REQ12, story:E6-S5
- note: CONFIRMED on every leg. CODE: apply_upsert_plan.py resolve() (lines 138-148) reads only op['match_key']/op['target_name']; 'additional_match_keys' appears nowhere in the file (grep-confirmed). PLAN: op UPD-E7-STATUS declares match_key=story:ASCP-E7-REQ16 + additional_match_keys=[REQ6,REQ27,REQ11,REQ23,REQ12,E6-S5] (7 targets). LOG: .apply_log.json has exactly ONE update for this op, id=73e5a0ce (=REQ16). LIVE API (read today): REQ16=Backlog (fixed); REQ6=In-Verification; REQ27=Human-Review; REQ11=Agent-Executing; REQ23=Blocked; REQ12=Agent-Executing; E6-S5=Done — all six unchanged, zero supporting on-disk artifact (GT-5/GT-6). REQ16's own live desc enumerates the contradiction it leaves unfixed in its 6 siblings. The Kanban 'no item sits in a state its on-disk evidence cannot support' guarantee is false for 6 of 7 targeted items. Not a duplicate of GT-D1/2/3 (those are NEW-12 / relations / collision-class). P0 stands: an executor scheduling off these states is actively misdirected (e.g. REQ11/REQ12 read as in-flight work).
- action[field-edit]: Re-run the true-up against the 6 missed targets individually: story:ASCP-E7-REQ6, REQ27, REQ12 -> Spec-Verified-or-Backlog; story:ASCP-E7-REQ11 -> Backlog and resolve its agent_role:human + Agent-Executing pairing (S7); story:ASCP-E7-REQ23 -> Backlog (S8: Blocked with no blocker edge — relations bucket verified empty); story:E6-S5 -> Spec-Verified/Backlog (S6: Done with no in-slice evidence). Separately, fix resolve() to iterate match_key + additional_match_keys so fan-out ops hit every declared target (root cause of this and any future fan-out op).

## P0 XC-02 — All 39 ops authored acceptance_criteria the applier never writes — live items carry only a prose contradiction-note, not their testable ACs
- items: 55, 257, 258, 262, story:ASCP-E8-13, ascp-audit-reorg:NEW-01, ascp-audit-reorg:NEW-02, ascp-audit-reorg:NEW-05, ascp-audit-reorg:NEW-08
- note: CONFIRMED. CODE: apply_upsert_plan.py builds the PATCH/CREATE body (lines 187-217) from only description_html/state/labels; op['acceptance_criteria'] is never serialized. LOG: every action's fields=[description_html,state,labels]. PLAN: 39/39 ops carry a non-empty acceptance_criteria[] (python-verified). LIVE API (read today): E8-13, NEW-05, NEW-02, NEW-08, NEW-01 descriptions contain ZERO 'ACn' markers — only the 3-5-sentence prose note (e.g. E8-13 live desc len=468, ends after 'agent_role=human retained'; AC1 'exactly five governance events', AC5 ralph Stop-ownership telemetry, AC6 SPINE_REQUIRED_EVENTS are absent). ONE correction to the prior framing: NEW-14's live desc DOES contain 'ACn' substrings, but those are incidental cross-references ('E4-S1 AC4/AC5 describe pre_tool_use...'), not NEW-14's own structured ACs — so the '10 of 13 NEW items lack AC markers' count is imprecise, but the load-bearing claim (the applier writes no acceptance_criteria; the testable contracts the report's coverage matrices treat as landed exist only in the JSON) is fully confirmed. P0 stands.
- action[update]: Extend the applier to append an '## Acceptance Criteria' block (rendered from op['acceptance_criteria']) into description_html on create/update, then re-PATCH each item. Highest priority: ascp-audit-reorg:NEW-05 (COH-1 byte-consistency AC3), NEW-02 (D3 run_state counter list AC1, BLOCKED-ON sentinel AC2/AC4), NEW-08 (validate_spec four-field verdict AC1), and story:ASCP-E8-13 (AC1 five-events, AC5 ralph telemetry, AC6 SPINE_REQUIRED_EVENTS) — the spine-critical testable contracts currently invisible on the board.

## P1 AI-04 — priority:* label set on zero created items — applier drops op['priority']; Priority-Triage view empty for spine items
- items: ascp-audit-reorg:NEW-01, ascp-audit-reorg:NEW-02, ascp-audit-reorg:NEW-05, ascp-audit-reorg:NEW-08, story:ASCP-E8-13
- note: CONFIRMED. Plan ops carry priority as a TOP-LEVEL field (NEW-01/02/05/08='priority:blocking', UPD-E8-20='priority:high') separate from labels[]. apply_upsert_plan.py body-builder (186-193) reads description_html/state/labels only — never op['priority'] and never sets native priority. Live board: NEW-01 native priority='none' labels=[type:wiring,gate:stop,phase:0]; NEW-02 same; NEW-05 priority='none' (agent:* + type:wiring + phase:0); NEW-08 priority='none' [type:functional,phase:0] — NONE carry priority:blocking label. E8-13 native priority='urgent' but labels=[human:review,type:wiring,gate:stop,phase:0] carry NO priority:blocking label. Report line 370 claims '5 priority:blocking items (NEW-01/02/05/08/12 + E8-13)'; NEW-12 doesn't exist and the rest lack the label. Refinement: E8-13 DOES have native priority=urgent, but the report's view groups by the priority:* LABEL which is what's missing, so the label-grouped triage is empty for the spine — claim!=reality holds.
- action[field-edit]: Add priority:* label per plan: priority:blocking on NEW-01/02/05/08 + E8-13 (+ NEW-12 when re-created); priority:high on NEW-03/04/09/10/14; priority:normal on NEW-06/07/11/13. Patch apply_upsert_plan.py to fold op['priority'] into the label_ids set (and/or set native priority) so re-runs are idempotent.

## P1 AO-03 — E7 + E6-S5 status true-up landed on only 1 of 6 stories: 5 advanced 'outrun-reality' states the report claims fixed still stand (incl. REQ23 Blocked-without-edge)
- items: story:ASCP-E7-REQ16, story:ASCP-E7-REQ6, story:ASCP-E7-REQ27, story:ASCP-E7-REQ11, story:ASCP-E7-REQ12, story:ASCP-E7-REQ23, story:E6-S5
- note: Confirmed-but-rescoped. .apply_log.json final action 'E7 + E6-S5 status true-up (REQ16/REQ6/REQ27/REQ11/REQ23/REQ12 + E6-S5)' is a SINGLE update op on a SINGLE id 73e5a0ce — which is REQ16. Live states verified: REQ16=Backlog (the only one moved, and its desc now carries the C16/S6/S7/S8 true-up note); REQ6=In-Verification, REQ27=Human-Review, REQ11=Agent-Executing, REQ12=Agent-Executing, REQ23=Blocked, E6-S5=Done — all UNCHANGED, all still carrying their original EARS-requirements text, not the true-up. report:367 claims 'E7 stories off Done/In-Verification (C16); E6-S5 off Done-without-evidence (S6); E7-REQ23 Blocked-without-link reconciled (S8) ... No item sits in a state its on-disk evidence cannot support' — refuted: 5 of 6 stories still sit in states their absent on-disk tools/migrations/telemetry cannot support, and REQ23 is still Blocked with ALL relation buckets empty (live relations endpoint), a dead-end Kanban node. REVISION vs the original finding: the report does NOT silently omit the REQ23 fix — report:238 (§4.4 row S8) explicitly states the intended fix; the defect is purely that the apply collapsed six logical true-ups into one op→one id (a GT-D3-class collision), so 5 of the 6 intended edits never executed. Severity P1 holds.
- action[field-edit]: Issue five separate state edits (each a distinct op resolving to a distinct id): ASCP-E7-REQ6, REQ11, REQ12 -> Backlog or Spec-Verified; ASCP-E7-REQ27 -> Backlog; E6-S5 -> off Done (Spec-Verified) until in-slice evidence exists. Also reconcile the agent_role:human + Agent-Executing mismatch on REQ11/REQ12 (S7). For ASCP-E7-REQ23 (S8): either move off Blocked or add the blocked-by relation to its prereq (NEW-11 / migrations) once relation support exists, so the Blocked terminal is backed by a checkable edge. Carry each story's true-up note into its own description (none but REQ16 got it).

## P1 AO-04 — NEW-14 (board-integrity blocked-by edges) is undeliverable as built and verifies to nothing — zero relations exist anywhere; same class as GT-D2 but generalized board-wide
- items: ascp-audit-reorg:NEW-14, story:ASCP-E8-13, story:ASCP-E4-S1, story:ASCP-E7-REQ23
- note: Confirmed on substance; rescoped because it materially overlaps GT-D2. plane-upsert-plan.json has 0 relation ops (only create+update); apply_upsert_plan.py has no relation-creation path (only PATCH work-items + POST module-issues/cycle-issues). Live relations endpoint returns ALL-EMPTY buckets for every probed item: NEW-14 itself, E8-13, E4-S1, E7-REQ23, E6-S2, REQ-INFRA-005 — board-wide zero blocked_by/blocking/relates_to. NEW-14's AC1/AC2 (the ~24 blocked-by edges to E8-13 + the ralph-disable leg) describe edges that have no executable op and did not land. report:92 ('turns that assumption into enforced board topology (explicit blocked-by edges)') and report:365 ('NEW-14 ensures the sprint's dependency-ordering cannot schedule a hook-consuming story ahead of E8-13') are refuted: the 'enforced topology' resolves to nothing; Active Sprint / Roadmap ordering is NOT protected. REVISION: GT-D2 already names exactly this (NEW-14 edges absent, applier has no relation capability, 'generalizes to the whole board'), so this is largely a restatement of the confirmed GT-D2 with the added live evidence that EVERY probed item — not just E8-13 — has empty buckets, and the added observation that NEW-14 is therefore mis-stated as Spec-Verified. Kept as 'revised' (not 'duplicate') only because it adds the Spec-Verified-state defect and the board-wide generalization; the executor should treat it as the actionable child of GT-D2.
- action[relation]: Add a relation-creation step to the applier (POST work-items/{id}/relations/ with type=blocked_by) and create the ~24 edges named in NEW-14 AC1: E2-S1/S2/S3, ASCP-E3-1/-2/-4/-22, ASCP-E4-S1/S2/S3/S4, ASCP-E5-S10, E6-S2/S3/S7, ASCP-E7-REQ6/REQ11/REQ12/REQ16/REQ23/REQ27, ASCP-E8-14/18/21/26 -> blocked_by ASCP-E8-13 (plus the ralph-disable leg). Until the edges exist, demote NEW-14 from Spec-Verified — its 'done' (the edges) is unmet. This action also discharges GT-D2.

## P1 BP-2 — Reentrancy guard (seq 236) reads stop_hook_active from the run_state DB row, but the runtime delivers it on Stop-event STDIN; stop_hook.py never reads it and with_reentrancy_guard is unimplemented
- items: 236, 189, 217, 258
- note: CONFIRMED on live board + on disk. Live work-item 7b259aa5 (seq 236, task:ASCP-E8-21#2) NAME='ASCP-E8-21.3 Implement the reentrancy guard composition', live desc='with_reentrancy_guard(evaluate_stop) via run_state.stop_hook_active; release the flag only when the blocking condition clears...'. On disk: grep for 'with_reentrancy_guard' and 'stop_hook_active' across .claude/hooks/ and tools/ returns ZERO matches (exit 1) — the guard is unimplemented. stop_hook.py main() reads ONLY event.get('run_state') (L211) and event.get('feature_list') (L212) from stdin; never reads the top-level stop_hook_active boolean the runtime delivers per code.claude.com/docs/en/hooks. Seq 189 (E7-REQ11.1) and seq 217 (E8-14.3) add stop_hook_active as a run_state DB column populated by NEW-02's loop driver (seq 258), not by the runtime. Live relations on seq 236 AND seq 189 are ALL EMPTY (blocked_by/blocking/relates_to=0), so the recommended relates_to edge does not yet exist (consistent with GT-D2). Keying the anti-loop guard off a DB column the runtime never sets means a persistently-unproven item still blocks to the runtime block-cap and is force-overridden (#63674). Claim (DB-column gate) != reality (stdin flag authoritative). P1 upheld. Evidence: live seq 236 desc; stop_hook.py:211-212; grep zero hits; digest seq 189/520; empty relations on 236 & 189.
- action[update]: Amend seq 236 (task:ASCP-E8-21#2) AC: 'The reentrancy guard MUST read the top-level stop_hook_active boolean from the Stop/SubagentStop event STDIN (the runtime-delivered flag, per code.claude.com/docs/en/hooks) and exit 0 / allow when true. The run_state.stop_hook_active column (E7-REQ11.1 seq 189, populated by NEW-02 seq 258) is a DURABLE MIRROR for the loop driver, NOT the gate input — the stdin flag is authoritative. Add a unit test feeding {stop_hook_active:true,...} on stdin and asserting allow()/exit 0 regardless of unproven in-scope items.' Then add a relates_to edge seq 236 <-> seq 189 documenting the stdin-vs-DB split (POST .../work-items/{id}/relations/, since the applier never created edges per GT-D2).

## P1 BP-3 — All three hooks print the steering reason as JSON to STDOUT then exit 2 (runtime reads only STDERR on exit 2, so every block silently drops its reason); NO board item owns the channel fix. Prior pass cited wrong seqs (615/259); real items are seq 214 + seq 124
- items: 214, 124, story:ASCP-E8-13, story:ASCP-E4-S1
- note: CONFIRMED in substance but the prior finding's seq citations are WRONG and must be corrected before any edit lands on the wrong item. On disk: pre_tool_use_hook.py main() does print(json.dumps(decision)) (L75) then 'return 0 if allow else 2' (L76); stop_hook.py main() prints decision to stdout (L214) then exit 2 (L215); subagent_stop_hook.py prints json (L109) then exit 2 (L110). All three write the reason to STDOUT; runtime discards stdout on exit 2 (only stderr fed back). The two board ACs asserting stderr behavior the code never produces: seq 214 (task:ASCP-E8-13#3, 'Property 21 PBT: exit 0 proceeds, exit 2 blocks with stderr fed back') and seq 124 (task:ASCP-E4-S1#5, 'Assert stderr names the blocking item ID'). The finding cited 'seq 615' and 'seq 259' — those are DIGEST LINE numbers, not seq ids; seq 259 is actually NEW-03 and seq 615 does not exist. Digest grep for stderr returns only these two; no 'permissionDecision'/'hookSpecificOutput'/stdout-fix item exists, so 'no item owns the channel fix' HOLDS. Revised only to repair board_items; severity P1 upheld (steering channel dead on every block). Evidence: pre_tool_use_hook.py:75-76; stop_hook.py:214-215; subagent_stop_hook.py:109-110; live seq 214/124 desc.
- action[create]: Create a NEW owning item (e.g. NEW-15, Module E8, Phase 0, type:wiring, gate:stop): 'Fix hook output channel: emit steering reason on the runtime-honored channel'. AC: on a block, write the located reason to STDERR and exit 2 (PreToolUse/Stop/SubagentStop), OR switch to exit-0 conforming JSON (PreToolUse: hookSpecificOutput.permissionDecision=deny + permissionDecisionReason + hookEventName; Stop/SubagentStop: top-level decision:block + reason). Stop printing the decision dict to stdout on the exit-2 path (pre_tool_use_hook.py L75, stop_hook.py L214, subagent_stop_hook.py L109). Add blocked_by edges: seq 214 (Property 21) blocked_by NEW-15 and seq 124 (E4-S1.6) blocked_by NEW-15. Cite code.claude.com/docs/en/hooks exit-code semantics + best-practices G4. CORRECT the prior seq citations: Property-21 PBT = seq 214 (NOT 615); E4-S1.6 = seq 124 (NOT 259).

## P1 COV-03 — 6 of 7 claimed E7/E6-S5 status true-ups never landed; only E7-REQ16 moved to Backlog
- items: story:ASCP-E7-REQ6, story:ASCP-E7-REQ11, story:ASCP-E7-REQ12, story:ASCP-E7-REQ23, story:ASCP-E7-REQ27, story:E6-S5, 73e5a0ce
- note: Confirmed against LIVE states: E7-REQ6=In-Verification, E7-REQ11=Agent-Executing (label human:review, confirmed via /labels/), E7-REQ12=Agent-Executing (human:review+gate:stop), E7-REQ23=Blocked (human:review+priority:blocking, relations ALL_ZERO), E7-REQ27=Human-Review, E6-S5=Done (completed_at 2026-06-17T22:26). Only E7-REQ16=Backlog. apply-log: the true-up op 'E7 + E6-S5 status true-up (REQ16/...)' has a SINGLE id 73e5a0ce = E7-REQ16 (one PATCH per op). So 1 of 7 landed; S6 (Done-no-evidence), S7 (human:review vs Agent-Executing on REQ11/REQ12), S8 (Blocked-no-link on REQ23) all UNFIXED. Report §6 line 367 'No item sits in a state its on-disk evidence cannot support' is false.
- action[field-edit]: Split the single true-up op into per-item field-edits: E7-REQ6/REQ11/REQ12/REQ27 -> Backlog (or Spec-Verified); E6-S5 -> Backlog (off Done; no in-slice evidence). For E7-REQ23 either encode its blocker relation (prereq NEW-11/migrations) via COV-02's relation work or move off Blocked. Reconcile REQ11/REQ12 human:review<->Agent-Executing (a human-owned item cannot be agent-executing).

## P1 COV-04 — Outrun-state defect broader than enumerated: E8-15/E8-19/E5-S17/E5-S30/E5-S32/E6-S6 never targeted for true-up
- items: story:ASCP-E8-15, story:ASCP-E8-19, story:ASCP-E5-S17, story:ASCP-E5-S30, story:ASCP-E5-S32, story:E6-S6
- note: Confirmed against LIVE states: E8-15=Agent-Executing (Inner-Loop Orchestration), E8-19=Agent-Executing (Predictive Routing), E5-S17=In-Verification (Supply-Chain & Security Gates), E5-S30=Human-Review (DeepEval), E5-S32=Human-Review (Kill-Switch), E6-S6=In-Verification (Integration observability). None appears in any apply-log update op. On-disk reality reconfirmed this session: .claude/settings.json ABSENT, .claude/agents/ only .gitkeep, zero *.sql, no tools/execution_bounds — so every Agent-Executing/In-Verification claim outruns reality. E8-15 (inner-loop orchestration) cannot be 'executing' with no settings.json + no agents. The true-up scope (COV-03) was limited to 6 E7/E6-S5 names AND those didn't land; these 6 further advanced-state stories were never enumerated.
- action[field-edit]: Extend status true-up to E8-15, E8-19, E5-S17, E5-S30, E5-S32, E6-S6 -> Backlog/Spec-Verified per on-disk evidence. Add a board-integrity invariant: no story may sit in a started/completed/cancelled state-group while its named on-disk artifact (hook/tool/migration/agent) is absent. Make this invariant a checkable board-preflight before the next apply.

## P1 HGD-4 — SubagentStop role-separation subtask seq 134 still trusts payload actor_agent and emits bare-verdict exit-2 strings, contradicting the live hook and artifact 04; the SubagentStop channel/string rewrite is unowned
- items: 134, 61
- note: CONFIRMED with one scope nuance. GET work-items/{E4-S3#3=065102c3}/ live desc verbatim: 'Parse the Subagent-Result Envelope from stdin (actor_agent, requirement_id, omission_declaration, optional evidence[]) ... role-separation block (actor_agent == implementer.md -> exit 2) ... exit 2 naming missing fields.' This reads+trusts actor_agent FROM the envelope and emits bare exit-2 reasons. Live subagent_stop_hook.py:98 already resolves actor via resolve_identity(CLAUDE_AGENT_NAME) and:60 rejects a record actor_agent that mismatches the runtime actor (forgery) — so the buildable subtask directly contradicts the shipped hook and artifact 04 fix #1. Live hook also prints JSON to STDOUT on exit 2 (line 109) = the G4 channel bug (exit-2 reads stderr only), has TEN bare-verdict reason strings (lines 53/59/62/69/72/76/78/82), and no stop_hook_active guard (G2). SCOPE NUANCE (the original finding slightly over-stated 'no item owns COH-3'): E8-21 (seq61) live desc DOES own the COH-3 approve->allow lockstep-test leg and the escalation rungs ('the COH-3 accept-value note (A4 approve->allow; update lockstep test)'). But board scan confirms NO item owns the SubagentStop bare-verdict→located+action string-catalog rewrite, the G4 stderr channel fix, or the G2 stop_hook_active guard — the stderr/stop_hook_active board hits (seq214, seq236) are all for the STOP hook, not SubagentStop. The load-bearing claim (seq134 contradiction + unowned channel/string-catalog/G2) is fully intact.
- action[update]: Update task:ASCP-E4-S3#3 (seq134): replace 'Parse the Subagent-Result Envelope from stdin (actor_agent...) then role-separation block (actor_agent == implementer.md -> exit 2)' with 'Resolve actor via tools/actor_identity (CLAUDE_AGENT_NAME); block when resolved_actor != verifier.md OR record.actor_agent != resolved_actor (artifact 04 fix #1). Emit located+action reasons to STDERR on exit 2 (the live hook wrongly prints JSON to stdout); honor stop_hook_active reentrancy.' Separately widen UPD-E8-21 (seq61) ACs to OWN the full artifact-04 SubagentStop string-catalog rewrite + the G4 stderr channel fix + the G2 stop_hook_active guard (it already owns COH-3 approve->allow + escalation), giving the SubagentStop leg a single owner.

## P1 HGD-5 — Every claimed dependency/ordering edge for the dynamism chain is absent as a live relation (GT-D2 generalized across the graph)
- items: 55, 60, 61, 56, 25, 34, 35, 33, 252, 253, 254, 256, 258, 259, 251
- note: INDEPENDENTLY CONFIRMED via live GET /work-items/{id}/relations/. All buckets EMPTY (blocking/blocked_by/relates_to/duplicate = []) on every item queried before/through rate-limiting: E8-13, E8-20, E8-21, E8-14, E2-S2, E4-S2, E4-S3, E4-S1, NEW-01, E8-13#0 (10 distinct items, ALL EMPTY). Yet NEW-14 (seq251) live desc CLAIMS 'This story adds the explicit Plane blocked-by relations so the Active Sprint/Roadmap ordering cannot schedule a hook-consuming story ahead of the registration', and reorg notes describe edges (E2-S2 'hard dependency on NEW-05', E8-21 'Add the NEW-02 dependency', E8-14 'Cross-link NEW-02', NEW-02 'lands LAST'). GT-D2 confirmed the applier (apply_upsert_plan.py) has NO relation-creation capability and was scoped to NEW-14→E8-13 only; this verifies the omission is SYSTEMIC across the whole dynamism graph, not the single NEW-14 case. The ordering that makes the hooks fire correctly (tools/execution_bounds + verifier.md frontmatter MUST exist before the gates compare the verifier.md literal; NEW-02 must populate counters before E8-21 escalation is non-inert) lives only in prose, invisible to any executor reading live blocked-by edges.
- action[relation]: Create the missing blocked_by edges via POST /work-items/{id}/relations/ (the applier cannot): E2-S2←NEW-05; E4-S3←NEW-05 and E4-S1←NEW-05 (COH-1, see HGD-3); E8-21←NEW-02; E8-14↔NEW-02 (relates_to: column-vs-populate split); E5-S10←NEW-05, ←E8-13, ←NEW-02; HGD-1's new pre_tool_use story ←E8-13 and ←NEW-05; all Phase-0 hook-consuming stories ←E8-13 (the NEW-14 edges that were claimed but never landed). Re-verify each with a follow-up GET (do not trust any apply log).

## P1 PSD-02 — UPD-E7-STATUS batch op true-upped only 1 of its 7 named targets (apply-collision class, GT-D3 sibling)
- items: 54, 49, 52, 50, 53, 51, 46
- note: Confirmed live. The batch op (.apply_log.json id 73e5a0ce-a0df-4063-a539-b08bce19396e) resolves to a SINGLE item: story:ASCP-E7-REQ16 (seq54), whose live state is now Backlog and whose live description carries the C16 true-up text ('Contradiction fix (C16) + §4.4 status incoherences: E7 stories carry advanced states (Done, In-Verification, Human-Review, Agent-Executing, Blocked) while their underlying tools/migrations/telemetry...'). In plane-upsert-plan.json this op uses match_key story:ASCP-E7-REQ16 plus an additional_match_keys field — but the applier's resolve() returns exactly one id, so the 7-target batch collapsed to one PATCH. Live states of the other 6 (re-fetched individually 2026-06-18): ASCP-E7-REQ6=In-Verification, ASCP-E7-REQ27=Human-Review, ASCP-E7-REQ11=Agent-Executing, ASCP-E7-REQ23=Blocked, ASCP-E7-REQ12=Agent-Executing, E6-S5=Done. REQ11 and E6-S5 live descriptions still show their original EARS text (no C16 note) — never PATCHed. Falsifies report §6 Workflow-Kanban 'E7 stories off Done/In-Verification (C16)'. Same root cause as PSD-03 S6/S7/S8.
- action[field-edit]: Re-issue six separate state PATCHes to the C16 true-up state (Backlog per the plan): ASCP-E7-REQ6 (seq49), ASCP-E7-REQ27 (seq52), ASCP-E7-REQ11 (seq50), ASCP-E7-REQ23 (seq53), ASCP-E7-REQ12 (seq51), E6-S5 (seq46), each as its own op keyed on a unique external_id. Structurally: forbid multi-target batch ops in the upsert plan (drop additional_match_keys); make resolve() error if a match_key set maps to anything other than 1:1 so collisions surface instead of silently dropping siblings (this is the GT-D3 class).

## P1 PSD-03 — S3/S4/S6/S7/S8 phase/state incoherences are still live and unreconciled — but the cause is missing/collapsed ops, not an applier that 'cannot set cycles'
- items: 47, 59, 46, 50, 51, 53
- note: End-state mismatches CONFIRMED live: S3 E6-S6 (story:E6-S6, seq47) = phase:3 label / Phase-2 cycle / In-Verification — not re-phased. S4 E8-19 (seq59) = phase:3 label / Phase-5 cycle / Agent-Executing, AC says Phase 6 — triple mismatch. S6 E6-S5 (seq46) = Done / phase:3 label / Phase-2 cycle, no evidence. S7 E7-REQ11 (seq50) = THREE phase labels (phase:0,phase:1,phase:2) / Phase-3 cycle / Agent-Executing; E7-REQ12 (seq51) = phase:3 / Agent-Executing. S8 E7-REQ23 (seq53) = Blocked with all relation buckets empty (verified live). Contrast cases confirmed coherent: S1 E5-S31 phase:4/Phase-4/Spec-Verified; S5 E3-22 phase:0/Phase-0; S2 E6-S3 (seq44) phase:0/Phase-0/Spec-Verified. REVISED because the finding's evidence-mechanism is WRONG: apply_upsert_plan.py CAN set cycles — assign() (lines 170-180) POSTs to /cycles/{cid}/cycle-issues/ and /modules/{mid}/module-issues/, and the .apply_log 'fields' list only logs the PATCH-body keys (description_html/state/labels) by construction, so 'cycle never in the field set' proves nothing. The real reasons it never landed: (a) S3 E6-S6 and S4 E8-19 have NO op in plane-upsert-plan.json at all — report §4.4 only marked them 'UPD-...(recommended)' / 'executor applies', never planned; (b) S6/S7/S8 were folded into the collapsed UPD-E7-STATUS batch (PSD-02 root cause) and so never PATCHed. Severity P1 retained.
- action[field-edit]: These need actual ops (they were recommended/collapsed, not applied): E6-S6 (seq47) -> single phase label matching its phase:0 consumer + Phase-0 cycle + correct agent_role; E8-19 (seq59) -> phase:6 label + Phase-6 cycle per its AC; E6-S5 (seq46) -> move off Done until evidence exists + reconcile phase:3-label/Phase-2-cycle; E7-REQ11 (seq50) -> collapse three phase labels to one + align label/cycle + true-up state; E7-REQ12 (seq51) -> align + true-up; E7-REQ23 (seq53) -> add blocked_by edge to its prereqs or move off Blocked (see PSD-04). The cycle moves ARE supported by assign() — author one op per item with epic_module+phase_cycle set; do not assume the tool needs extending for cycles (it does need extending for relations).

## P1 TWL-04 — Reentrancy + escalation-rung ownership is triply-claimed and self-contradictory across E5-S10, E8-14, E8-21 on the live board
- items: 37, 56, 61, 142, 236
- note: Confirmed live, with one sharpening. Report R5 (line 197) DOES state a clean contract: E8-14 owns the reentrancy guard, E8-21 owns the escalation/HANDOFF rungs. E8-21 story (seq 61) live desc echoes it: 'E8-21 owns the escalation/HANDOFF rungs inside evaluate_stop; E8-14 owns the reentrancy guard half.' BUT the board CONTRADICTS that contract three ways: (1) E8-21.3 (seq 236) is titled 'Implement the reentrancy guard composition' and its AC implements with_reentrancy_guard — placing reentrancy under E8-21, not E8-14; (2) E5-S10.1 (seq 142) ALSO implements with_reentrancy_guard — a third claimant; (3) E8-14 story (seq 56) live desc says 'E8-14 owns ONLY the 005_run_state.sql column migration — it does NOT populate the counters' and never states it owns the reentrancy guard — directly contradicting R5's assignment. So three subtasks/stories touch the guard and NONE owns the escalation rung as a subtask (TWL-01). R6 'cite-don't-restate' is sound for the PRINCIPLE but the block_streak ladder is hook CODE (02-stop_hook §1), not citable A8/charter prose, so citing would leave the mechanism unimplemented. CLAIM(clean contract)!=REALITY(board encodes the opposite) holds. P1 retained (misleads an executor; TWL-01/02 carry the P0 break).
- action[update]: UPDATE the three items to a single non-overlapping contract matching R5: assign the EVENT-LEVEL reentrancy guard to exactly ONE item (E8-14 seq 56, adding an explicit reentrancy AC since its desc currently disclaims everything but the migration), retitle/repoint E8-21.3 (seq 236) to the block_streak escalation LADDER (not reentrancy), and rewrite E5-S10.1 (seq 142) to CONSUME both rather than re-implement with_reentrancy_guard. Clarify R6 on E8-21 (seq 61): charter/A8 own the PRINCIPLE (CH-10) + BLOCKED-ON sentinel, but the rung MECHANISM (block_streak ladder + _escalate template) is hook code that must be implemented, not merely cited.

## P1 TWL-06 — E8-13 story claims it disables ralph-loop, but no E8-13 subtask AC encodes the disable or the ralph-absent-from-telemetry test
- items: 55, 211, 212, 213, 214
- note: Confirmed live + on-disk. E8-13 story (seq 55, live desc): 'sets enabledPlugins."ralph-loop@…": false for governed sessions.' But E8-13.1 (seq 211, live desc): 'Register Stop, PreToolUse, PostToolUse..., SubagentStop, SessionStart, PreCompact as type:command... Write tests/smoke/test_hook_config.py asserting all hooks are command-type and the four subagents are discoverable.' — NO enabledPlugins ralph disable, NO test asserting ralph stop-hook.sh is absent from hook telemetry. (Digest: 212/213/214 cover pre_tool_use guards / GitHub ruleset / exit-code property tests — none carries the disable.) GT-10/GT-1: ralph-loop's stop-hook.sh currently owns the Stop event and is the literal 49x re-injection source; report success criterion #5 (line 442) 'ralph stop-hook.sh absent from hook telemetry' exists but is unmapped to any subtask AC. Report lines 102/105/120/122 credit E8-13 with 'the ralph disable' for resolving C-HOOK-01/C-LOOP-04. CLAIM(disable owned)!=REALITY(prose-only, no verifiable AC/test) holds. P1 retained.
- action[update]: UPDATE task:ASCP-E8-13#0 (seq 211): add an AC that settings.json sets enabledPlugins['ralph-loop@...']=false for governed sessions, and extend test_hook_config.py (or add a telemetry smoke test) to assert the ralph stop-hook.sh path never appears in Stop hook_events. Mirror report success criterion #5 as the verification oracle.

## P1 TWL-08 — NEW-03 (A8 goal directive) and NEW-02 (BLOCKED-ON parser) are not bound by an AC or a relation — the anti-idle sentinel channel is unverifiable end-to-end
- items: 259, 258
- note: Confirmed live. NEW-03 (seq 259, id 566e8261) live description (533 chars) is prose ('...supplies the BLOCKED-ON sentinel that pairs with NEW-02 ... Forbids the agent from writing run_state directly (emits the sentinel instead)') — hasAC=False. GET /work-items/{NEW-03}/relations/ = EMPTY-ALL; NEW-02 relations = EMPTY-ALL — so the two halves of the two-ended contract (02-stop_hook §4: agent emits 'BLOCKED-ON: <dependency>', the NEW-02 driver parses it into run_state.external_blocker, the Stop hook HANDOFFs) carry neither a matching AC nor a relation edge between them. Report line 123 marks C-IDLE-03 RESOLVED via NEW-03+NEW-02. CLAIM!=REALITY holds: a regression in either half passes uncaught. Re-confirmed against the canonical spec (02-stop_hook §2.2 row 4 + §4 driver-owned external_blocker). P1 retained. Sibling of TWL-03; distinct in that it is the cross-item BINDING gap, not the NEW-02 stub itself.
- action[relation]: Add a relates_to edge (live relations endpoint, GT-D2 class) between ascp-audit-reorg:NEW-03 (seq 259) and ascp-audit-reorg:NEW-02 (seq 258), and UPDATE both to carry the paired AC: NEW-03 = agent ends its stop summary with 'BLOCKED-ON: <dependency>' and never writes run_state; NEW-02 = parser maps that exact sentinel line to run_state.external_blocker. Add a paired integration test that the sentinel produces a HANDOFF (exit 0), not a re-block, per 02-stop_hook §2.2 trigger (4).

## P1 WF-02 — S8 NOT landed: REQ23 still in Blocked with zero blocker relations (dead-end Blocked, sibling of GT-D2)
- items: story:ASCP-E7-REQ23
- note: CONFIRMED live. GET /work-items/09176986-91b3-4618-a233-85e90c40c6d0/relations/ returns ALL EIGHT buckets empty ({blocking:[],blocked_by:[],duplicate:[],relates_to:[],start_after:[],start_before:[],finish_after:[],finish_before:[]}). Live state=Blocked (cancelled-group 8ea88a3c), updated_at=2026-06-17T22:32 (untouched). upsert-plan AC3(S8) required REQ23 to either carry a blocker edge OR move off Blocked; neither occurred. Corroborated the mechanism: grep of apply_upsert_plan.py for relation/blocked_by/relations returns NOTHING — the applier has no relation-POST path, so the edge was never creatable by this tool (GT-D2 class). REVISION to board_items: dropped the original 'ascp-audit-reorg:NEW-12' (GT-D1: NEW-12 does not exist) and 'NEW-11' references — keep the finding scoped to REQ23 as the dead-end Blocked item.
- action[relation]: PREFERRED: move REQ23 off Blocked to Backlog as part of the WF-01 PATCH batch (its true blocker is 'prereqs absent on disk', not an in-board blocker). If Blocked is retained, add a blocked_by edge via POST /work-items/09176986-91b3-4618-a233-85e90c40c6d0/relations/ with relation_type=blocked_by to a REAL existing prereq: NEW-11 (PreCompact resume_state_hash, ascp-audit-reorg:NEW-11 — created in the prior apply per apply_log) and/or REQ-INFRA-005 (df8b5217) as the migration owner. Do NOT cite NEW-12 (does not exist). A Blocked state with no blocked_by edge must not persist on the Kanban.

## P1 WF-03 — S7 NOT landed: REQ11 & REQ12 still Agent-Executing while carrying human:review (human-owned item cannot be agent-executing)
- items: story:ASCP-E7-REQ11, story:ASCP-E7-REQ12
- note: CONFIRMED live. Live state REQ11=Agent-Executing, REQ12=Agent-Executing (both group=started, state id b7b5ff8a), both updated_at=2026-06-17T22:32 (untouched). board-digest labels confirm REQ11=[...,human:review,...] and REQ12=[...,gate:stop,human:review,...] (digest seq 50/51). The human:review + Agent-Executing contradiction is fully live. Reconciliation depended on the same swallowed UPD-E7-STATUS PATCH (WF-01). Note correctly retained from the finding: v1 API does not expose issue-type custom-property values (issue-properties endpoints 404), so agent_role is verified via the human:review label proxy co-occurring with an agent-driven state — a sound board-visible proxy.
- action[field-edit]: Resolved by the WF-01 PATCH batch (REQ11/REQ12 -> Backlog d83bd05d). Add an executor guard: any item bearing human:review (or agent_role=human) may not occupy an agent-driven started state (Agent-Executing/Spec-Compiling/In-Verification driven by an agent). Optionally extend E6-S7's non-vacuous gate-isolation property test to assert no human-owned item sits in an agent-execution state.

## P1 WF-04 — S6 NOT landed: E6-S5 still Done with no in-slice evidence for a blocking reconciliation item
- items: story:E6-S5
- note: CONFIRMED live. E6-S5 (f07788c7) live state=Done (completed-group d30f24ad), updated_at=2026-06-17T22:32 (untouched), description_html still the original EARS block (C16-text=False). It remains the board's ONLY Done item (GT-12: Done=1). E6-S5 is 'Bidirectional state-mirror and traceability-triple sync' (priority:blocking, gate:completion) and depends on a webhook board + Postgres traceability store that do not exist (GT-5 zero *.sql). A blocking item in the completed group with zero on-disk evidence is the single strongest readiness misrepresentation on the Kanban. AC4(S6) required moving it off Done; it never moved.
- action[field-edit]: PATCH E6-S5 (f07788c7) state Done -> Backlog (d83bd05d) as part of the WF-01 batch. Done must be reachable only via the Stop+OPA+required-check path (E6-S3 C11 reframe); until those gates exist and fire, no item may sit in the completed group. Read-back-verify after PATCH.

## P1 WF-05 — C16 op SCOPE was too narrow: 5 further evidence-impossible advanced-state stories were never enumerated for true-up
- items: story:E6-S6, story:ASCP-E8-29, story:ASCP-E8-26, story:ASCP-E8-19, story:ASCP-E8-15
- note: CONFIRMED live by direct GET of each story. E6-S6=In-Verification (integration OTel spans; telemetry absent), ASCP-E8-29=In-Verification (omission-declaration gate; subagent_stop hook/schema absent GT-3/GT-2), ASCP-E8-26=In-Verification (reasoning-loop detection; telemetry absent), ASCP-E8-19=Agent-Executing (predictive routing), ASCP-E8-15=Agent-Executing (inner-loop orchestration — spine unwired per GT-1, so nothing can be agent-executing). All five updated_at=2026-06-17T22:32 (untouched). None appear in UPD-E7-STATUS match_keys (operations[38]). Confirms the report-line-367 universal claim ('No item sits in a state its on-disk evidence cannot support') was never achievable from the op's scope even had all 7 targets applied. Minor scope check on the action: E8-13 is live Spec-Verified (not built) — the action's premise holds.
- action[field-edit]: Extend the true-up to all five: PATCH E6-S6, ASCP-E8-29, ASCP-E8-26 (In-Verification) and ASCP-E8-19, ASCP-E8-15 (Agent-Executing) to Backlog (d83bd05d) per the C16 rule (Agent-Executing/In-Verification require a wired spine + the consumed tool on disk; neither holds — GT-1/GT-5). E8-15 specifically must not be Agent-Executing while E8-13 (the spine wiring itself) is only Spec-Verified-not-built. Fold these into the same fanned-out batch as WF-01.

## P1 XC-03 — GT-D1 root cause pinned and blast radius widened: NEW-12 op carries a match_key so it could never create; dangling-referenced by 4 live items
- items: 20, 27, 28, 54, story:REQ-INFRA-005, story:ASCP-E3-1, story:ASCP-E3-2, story:ASCP-E7-REQ16
- note: CONFIRMED, and materially additive to GT-D1 (not a mere restatement). CODE+PLAN: op id=NEW-12 has op='create' BUT match_key='story:REQ-INFRA-005'; apply_upsert_plan.py:195 calls resolve(op) BEFORE the create branch, and resolve() matches match_key first (lines 139-142) -> returns the existing REQ-INFRA-005 id, so the 'create' executed as an UPDATE of df8b5217. LOG confirms TWO update actions on df8b5217 (NEW-12 op then UPD-REQ-INFRA-005 op) — the second clobbered the first. LIVE API (read today): external_id ascp-audit-reorg:NEW-12 returns NOT FOUND; no item by that name exists; only 13 audit items exist though plan _meta.counts.new=14. The NEW-12 reference is dangling in REQ-INFRA-005 live desc ('depend on NEW-12 authoring the migrations') and in E3-1/E3-2 precondition blocks ('after NEW-08 + NEW-12 + spine'); E3-1/E3-2 are thus gated on an owning item that can never be reached. This pins the GT-D1 root cause to a specific bug (match_key on a create op + resolve-before-create ordering) and widens the dangling scope from 1 item (GT-D1) to 4. P1 appropriate.
- action[create]: Create standalone NEW-12 (E1, Phase 0, priority:blocking, external_id ascp-audit-reorg:NEW-12, state Spec-Verified) owning 'author eight .kiro migrations 001-008 + run_state completeness columns + gate_audit_log append-only grant' with plan AC1-AC4. Root-cause code fix: strip match_key from any op whose op=='create', OR make the applier treat op=='create' as create-only regardless of match_key (skip the resolve() shortcut for creates). After creation the NEW-12 references in REQ-INFRA-005/E3-1/E3-2/E7-REQ16 resolve.

## P1 XC-05 — E8-13 parent/child PreCompact contradiction: parent wires 5 events (no PreCompact) while child task #0 still says register 6 incl. PreCompact + smoke-test all hooks command-type
- items: 55, 211, story:ASCP-E8-13, task:ASCP-E8-13#0
- note: CONFIRMED on live + snapshot. Parent story (seq 55, story:ASCP-E8-13) live desc: 'Wires SessionStart / PreToolUse / PostToolUse / SubagentStop / Stop' = 5 events, PreCompact omitted (the C1 rewrite). Child task #0 (seq 211, task:ASCP-E8-13#0, untouched by the prior pass — no apply-log entry) reads: 'Register Stop, PreToolUse, PostToolUse (matcher Write|Edit|MultiEdit), SubagentStop, SessionStart, PreCompact as type:command (task 9.1)... Write tests/smoke/test_hook_config.py asserting all hooks are command-type and the four subagents are discoverable.' Remediation 05 lines 120/356/434 mark PreCompact (D7) deliberately NOT registered / NOT AUTHORED; GT-3 confirms no pre_compact_hook.py on disk; GT-2 confirms .claude/agents/ is .gitkeep-only so 'four subagents discoverable' also fails. Parent and child now disagree on event count (5 vs 6) and the child's smoke test would assert a 6th hook that the parent's design omits. Genuinely new (not GT-D1/2/3). P1 stands.
- action[update]: Edit task:ASCP-E8-13#0 (seq 211) to register exactly five events (drop PreCompact), align the smoke test to assert only the five registered hooks (and gate the 'four subagents discoverable' assertion on NEW-05 landing the agent frontmatter, since .claude/agents/ is currently .gitkeep-only). Add a relates_to edge from task:ASCP-E8-13#0 to NEW-11 (PreCompact/D7) so the deferred 6th event is tracked, not silently dropped.

## P2 AI-05 — NEW-09 created as a live duplicate execution_bounds owner alongside UPD-E8-20 (preferred fold) — both landed
- items: ascp-audit-reorg:NEW-09 (seq 256, Backlog), story:ASCP-E8-20 (seq 60, Spec-Verified)
- note: CONFIRMED-BUT-REVISED to P2. Real: plan NEW-09 conditional='only if UPD-E8-20 not applied'; UPD-E8-20 supersedes='NEW-09'; apply_upsert_plan.py has zero conditional/supersedes handling (executes every op, loop line 184). Live board: E8-20 (seq 60, Spec-Verified) desc='Expand E8-20 to OWN authoring tools/execution_bounds (PREFERRED over a standalone item)'; NEW-09 (seq 256, Backlog) ALSO exists, desc='Conditional create. Only instantiate if UPD-E8-20 is NOT taken... executor should prefer the E8-20 fold and skip this create'. Both name tools/execution_bounds. WHY P2 not P1: NEW-09 is in Backlog (not Spec-Verified) and self-deprecates / points to E8-20 as preferred, so an executor reading it is steered to skip it — a redundancy/ambiguity that pollutes a 'who owns execution_bounds' query, not an active competing owner that misdirects execution. Severity lowered to match blast-radius.
- action[update]: Close/cancel ascp-audit-reorg:NEW-09 (low blast-radius, already Backlog) with a note pointing to ASCP-E8-20 as the live owner, OR set NEW-09 relates_to E8-20 and re-label as superseded placeholder. Long-term: teach the applier to honor conditional/supersedes so guard ops are skipped.

## P2 AI-06 — .apply_log.json is a plan-echo with no per-op outcome — no result/http-status/error, which is why every silent drop went uncaught
- items: plane-integration/.apply_log.json
- note: CONFIRMED. apply_upsert_plan.py log entries (lines 202, 217) are {op,name,id|external_id,fields} only; fields=list(body)=keys SENT, not accepted. Live inspection of .apply_log.json: 39 actions, key-sets exactly {('fields','id','name','op'),('external_id','fields','name','op')} — NO status/result/error/http_status/returned_id/collided_with key anywhere. df8b5217 appears as TWO update actions sitting silently adjacent with no collision signal. Log records 13 creates + 26 updates while plan _meta.counts says new:14/update:25 — that count mismatch is the only (passive) trace of the NEW-12 demotion, and the log cannot surface the dropped relations, dropped priority, or 6 untouched E7 stories. 'It's in the apply log' is not evidence the effect landed; the live board is the only valid oracle. Claim (faithful applied record) != reality (write-intent echo).
- action[none]: Tooling fix: extend apply_upsert_plan.py to capture per-op {http_status, returned_id, error, collided_with} and assert each op['op']=='create' yielded a NEW external_id (not a resolve-to-existing). Add a post-apply reconcile step that re-GETs each touched item and diffs sent-vs-landed desc/state/labels. Until then treat the live board, not the log, as the apply-integrity oracle.

## P2 AI-07 — S3 (E6-S6) and S4 (E8-19) never encoded as ops — phase-label vs AC incoherence persists; cycle sub-claim unverifiable
- items: story:E6-S6 (seq 47), story:ASCP-E8-19 (seq 59)
- note: CONFIRMED-BUT-REVISED. Solid: report §4.4 lines 233-234 mark S3 (E6-S6 re-phase span-contract + correct agent_role) and S4 (E8-19 reconcile phase:3 label to its Phase-6 AC) as '(recommended)... executor applies the field edit'; neither appears as an op in plane-upsert-plan.json (grep of all 39 op ids: no UPD-E6-S6, no UPD-E8-19) and neither is in apply_log. Live board: E6-S6 label=phase:3, state=In-Verification, raw EARS desc (unreconciled); E8-19 label=phase:3, state=Agent-Executing, raw EARS desc (unreconciled). By contrast the explicitly-opped E5-S31 DID land its label=phase:4 (control). Report line 368 claims 'E6-S6 (S3), E8-19 (S4) label<->cycle/AC reconciled' READY — false on the label/state/AC axis. REVISION: the finding's 'cycle' sub-claim (E6-S6 in Phase-2 cycle, E8-19 in Phase-5 cycle) is UNVERIFIABLE here — the work-item payload returns cycle=None for E6-S6, E8-19 AND the control E5-S31, so cycle membership is not exposed via this endpoint; scope the confirmed defect to label/state/AC (decisive) and drop the unverifiable cycle assertion. P2 (label/AC incoherence, polish-grade) is correct.
- action[field-edit]: Encode the two missing edits as real ops: E6-S6 -> set label phase:0 to match its consumer (S3) and correct agent_role from 'research' to the implementer role per the COH-1 <role>.md map; E8-19 -> set label phase:6 to match its own AC (S4). Verify cycle membership separately via the cycle-issues endpoint (the work-item payload does not expose it) before claiming cycle reconciliation.

## P2 AO-06 — Reconciliation notes on E5-S10 and E6-S7 hand the executor an imperative, not a checkable AC; E8-13 carries no post-apply verification oracle
- items: story:ASCP-E5-S10, story:E6-S7, story:ASCP-E8-13
- note: Confirmed on the live board. E5-S10 desc ends '...omits the CH-10 escalate-don't-re-inject rung that directly closes C-LOOP-04 — add it.' — an instruction with no oracle (no test name, no assert; and its actual AC1-AC3 from the plan, incl. the CH-10 rung AC, did not land per AO-01: 'block_streak'/AC text absent). Live E6-S7 desc ends 'Make the check assert gates EXIST AND don't read Plane state.' — intent stated, no checkable predicate (no named gate symbols that must be importable); the C13 vacuous-pass form is described in prose but not encoded as a runnable assertion. E8-13 desc states the registration + ralph-disable intent but carries no post-apply verification oracle on the item, and specifically the AC5 telemetry assertion ('stop-hook.sh ABSENT from hook telemetry') is absent from E8-13's description_html (verified: substring 'ABSENT from hook telemetry' and 'AC5' both not present) — the report:73/425 'post-apply verification step 5' lives only in the report, not the item. CLAIM (report §4.1 C8/C13)!=REALITY genuinely holds. This is the vague-imperative / no-oracle pattern the audit's CH-07 / A-LOOP-01 condemn, landing on the items meant to fix it.
- action[update]: Rewrite as testable ACs on the items: E5-S10 'a property test proves the Nth identical Stop block escalates to a different message/HANDOFF, never a byte-identical re-injection (closes C-LOOP-04 via the CH-10 rung)'; E6-S7 'a test asserts the gate modules (stop_hook / pre_tool_use / coverage_query) are importable AND a grep over them finds zero Plane-state reads — the test FAILS if the gates are absent (non-vacuous)'; E8-13 'append AC5 inline: hook-decision telemetry shows zero stop-hook.sh invocations and the governance Stop hook owns the Stop event (post-apply pass/fail gate).' Fold these into the AO-01 applier re-run.

## P2 AO-07 — NEW-06's teardown oracle is unverifiable on the board: its ACs did not land (AO-01) and its strongest AC is defined by reference to E8-13 AC5, which also did not land
- items: ascp-audit-reorg:NEW-06, story:ASCP-E8-13, story:E6-S2
- note: Confirmed. Live NEW-06 (605-char desc) carries only the teardown-rationale prose; substring probes for its plan ACs ('flagd kill-switch OFF', 'a test proves a still-enabled', 'same predicate') all ABSENT — so the 'concrete checkable oracle' the report §3.1/§5 credits to NEW-06 for resolving A-LOOP-01 is not on the item (AO-01 instance). Compounding: the plan defines NEW-06 AC1 as 'the same predicate UPD-E8-13 AC5 verifies', but E8-13 AC5 itself never landed as text (verified: 'ABSENT from hook telemetry' / 'AC5' not present in E8-13 description_html, see AO-06) — so even if NEW-06's AC1 were restored it would point at another item's missing oracle. The doctrine->runnable-check promotion the report claims (A-LOOP-01 RESOLVED via NEW-06) did not materialize on the board. CLAIM!=REALITY confirmed.
- action[update]: Land NEW-06 AC1-AC4 on the item (via the AO-01 applier fix) AND make AC1 self-contained: state the telemetry assertion inline ('hook-decision telemetry shows zero stop-hook.sh invocations and governance stop_hook.py owns the Stop event; a test proves a still-enabled plugin FAILS the disabled-oracle'), not by reference to E8-13 AC5. After relation support lands (AO-04), cross-link NEW-06 <-> E8-13 and NEW-06 <-> E6-S2 via relates_to.

## P2 BP-4 — subagent_stop_hook.py returns decision:'approve' (not a valid SubagentStop value); the rename is only a buried note on the E8-21 STORY (seq 61), no task owns the code edit. Prior pass mis-cited the note location (seq 234) and the omission_guard owner (seq 247)
- items: 61, 134, 248, story:ASCP-E4-S3, story:ASCP-E8-29
- note: CONFIRMED in substance, mis-scoped seqs corrected. On disk subagent_stop_hook.py evaluate() returns {'decision':'approve',...} on the happy path (L84), main() maps approve->exit 0 (L110), docstring codifies 'approve'|'block' (L48). Current docs: SubagentStop top-level decision only valid value is 'block'; allow = omit decision / exit 0 (best-practices doc 03 G3/G5, L141). The COH-3 'A4 approve->allow' note lives on the E8-21 STORY (seq 61, live desc '...Add ... the COH-3 accept-value note (A4 approve->allow; update lockstep test)'), NOT on seq 234 (which is E8-21.1 HANDOFF ordering) as the finding claimed. The omission_guard exit-2 is seq 248 (task:ASCP-E8-29#1, 'Implement the omission_guard in subagent_stop_hook.py'), NOT seq 247 (E8-29.1 schema authoring). E4-S3.4 is seq 134 (correct in finding). The rename is a concrete code+test change buried as a note on a story whose scope is HANDOFF ordering; seq 134 and seq 248 both build on this hook. Harmless today only because exit 0 happens to allow, but the JSON misleads any future reader (e.g. additionalContext continuation). P2 upheld. Evidence: subagent_stop_hook.py:48,84,110; best-practices 03 L141; live story seq 61 desc; digest seq 248.
- action[update]: Promote the rename from a buried note to a concrete AC. Add to seq 134 (task:ASCP-E4-S3#3): 'subagent_stop_hook.evaluate returns NO decision field on accept (omit it) and exits 0; reserve decision:"block"+reason for objections; prefer hookSpecificOutput.additionalContext to continue the loop without the pre-2.1.163 error label (best-practices G5). Update the approve->allow lockstep test. Fix the L48 docstring and the L84/L110 return.' Note this on the omission_guard seq 248 (task:ASCP-E8-29#1) since it edits the same hook, and add a relation seq 248 blocked_by seq 134. CORRECT the prior seq citations: COH-3 note is on the E8-21 story seq 61 (NOT 234); omission_guard is seq 248 (NOT E8-29.2 / seq 247).

## P2 COV-05 — NEW-09 and E8-20 both own tools/execution_bounds: the conditional-create rule fired anyway
- items: 256, ascp-audit-reorg:NEW-09, story:ASCP-E8-20
- note: Confirmed against LIVE: NEW-09 (35e30590) exists, state Backlog, desc reads 'Conditional create. Only instantiate if UPD-E8-20 is NOT taken ... Authors tools/execution_bounds with the same AC set as UPD-E8-20.' E8-20 (3d0b588f, Spec-Verified) desc reads 'Expand E8-20 to OWN authoring tools/execution_bounds (PREFERRED over a standalone item) ... SPEC_COMPLETION_HARD_CAP=7 ... VERIFIER_ROLE.' Both update-E8-20 AND create-NEW-09 are in the apply log. So E8-20 WAS taken yet NEW-09 was still created unconditionally (applier has no conditional-create logic) -> two live items author the same module. Report §3.3/§5 'NEW-09 (prefer tightening E8-20)' violated.
- action[field-edit]: Cancel/close NEW-09 (or relabel superseded-by-E8-20) since E8-20 now owns execution_bounds; alternatively strip NEW-09's content and convert to a relates_to pointer to E8-20. Record in E8-20 that the standalone fold was rejected. Applier fix: implement conditional-create (skip a create whose 'only-if' sibling update was applied).

## P2 COV-06 — PreCompact D7 owned by both NEW-11 and existing task E7-REQ11.3 with no scope contract
- items: 262, ascp-audit-reorg:NEW-11, story:ASCP-E7-REQ11, 191
- note: Confirmed against LIVE: NEW-11 (11244f6b, Backlog) desc = 'The producer of resume_state_hash for A5's drift compare (D7) ... E7 durable-state/observability scope.' Existing task E7-REQ11.3 (seq191, digest line 525) = 'pre_compact_hook.py checkpoint writer + baseline-hash producer ... Write durable run_state.resume_state_hash.' Both author the PreCompact checkpoint + resume_state_hash producer. E7-REQ11 relations ALL_ZERO (verified live) so no scope-contract edge exists between NEW-11 and E7-REQ11.3 — unlike the R5 E8-14/E8-21 reconciliation the report applied elsewhere. Split-ownership pattern recurs here unaddressed.
- action[relation]: Add a relates_to edge (and scope-contract note) between NEW-11 (owns the unified D7 hook) and E7-REQ11's task .3 (extends it), mirroring the R5 E8-14/E8-21 treatment, so the two don't independently re-author pre_compact_hook.py. Reference NEW-11 from E7-REQ11.3's description. Depends on COV-02 relation-creation support landing first.

## P2 PSD-04 — D7 PreCompact producer (NEW-11, phase:2) and resume-integrity consumer (E7-REQ23, phase:2 in a Phase-3 cycle) have no edge enforcing producer-before-consumer
- items: 262, 53, 50
- note: Confirmed live. NEW-11 (ascp-audit-reorg:NEW-11, seq262) = phase:2 label / Phase-2 cycle / Backlog, all relation buckets empty (verified live). E7-REQ23 (seq53) = phase:2 label / Phase-3 cycle / Blocked, all relation buckets empty (verified live) — a Blocked state with no encoded blocker, the §4.4-S8 dead-end. No blocked_by edge connects REQ23 to NEW-11. E7-REQ11 (seq50) carries phase:0/1/2 labels and (per its task breakdown) overlapping PreCompact-checkpoint ownership with NEW-11, with no relates_to link. This is a sequencing AMBIGUITY (no edge + label/cycle drift), correctly scoped P2 — not a hard phase inversion. Depends on GT-D1 (NEW-12 does not exist on the board, verified) for the migrations leg of the recommended edge.
- action[relation]: Add blocked_by edges once relation-writing exists (see PSD-01): ASCP-E7-REQ23 --blocked_by--> NEW-11 (ascp-audit-reorg:NEW-11, seq262); and ASCP-E7-REQ23 --blocked_by--> NEW-12 only after NEW-12 is actually created (it is absent per GT-D1, so this edge cannot be made yet). Add a relates_to edge ASCP-E7-REQ11 <-> NEW-11 to make the duplicate PreCompact-checkpoint ownership explicit (or fold REQ11's checkpoint task into NEW-11). Separately reconcile REQ23/REQ11 phase-label vs cycle per PSD-03.

## P2 TWL-05 — Report NEW-13 AC2 mislabels escalate-don't-re-inject as CH-08 (clause numbering broadly skewed); canonical clause is CH-10 — but the mislabel lives in the report doc, not yet on the live board
- items: 260, 37
- note: Real but mis-scoped and over-severe. CONFIRMED at the report layer: report line 336 AC2 reads 'CH-08 (escalate-don't-re-inject)' while charter 00-charter.md line 217 defines CH-10='Vary or escalate on repeat; bounded 2-3 rungs -> HANDOFF' and line 178 defines CH-08='HANDOFF before block, as a pure data predicate.' Same AC2 also mis-maps CH-07 (it labels CH-07='located oracle before any done-claim', whereas charter line 160 defines CH-07='Reentrancy/read stop_hook_active') — the AC2 numbering is broadly skewed, not just one clause. SKEPTIC correction: the LIVE NEW-13 board item (seq 260, id 2f6d9abb) does NOT carry any CH-08/CH-10 numeric label — its live description uses prose 'escalate-don't-re-inject' with no clause number (hasAC=False). So claim!=reality does NOT yet hold ON THE LIVE BOARD; the defect is a latent report-doc error that would mis-encode the board only if AC2 is copied verbatim. Downgraded P1->P2 (doc-latent, not a live contradiction today) and scoped to the report + the act of authoring NEW-13's AC.
- action[update]: FIX report NEW-13 AC2 (line 336) clause numbering before it lands: escalate-don't-re-inject=CH-10 (charter line 217); HANDOFF-before-block=CH-08 (line 178); reentrancy=CH-07 (line 160). When authoring NEW-13 (seq 260) acceptance text, encode escalate-don't-re-inject as CH-10. Ensure E5-S10/E8-21 cite CH-10 for the rung and CH-07 for reentrancy.

## P2 TWL-07 — NEW-10 PostToolUse forcing-function is a prose stub with no AC and no relation to its extending stories — but the cited extending-story seq numbers are wrong
- items: 253, 127, 133
- note: Core finding CONFIRMED, evidence corrected. NEW-10 (seq 253, id fdae6051) live description (327 chars) is rationale-only ('This story builds the one hook both stories extend. The .json leg is LIVE: tools/feature_list_check.py exists on disk (225 lines)') — hasAC=False, and GET relations returns EMPTY-ALL. It does not carry report §5 AC1 (one targeted check on the one changed file), AC2 (E4-S2/E4-S3 extend it), or AC3 (fails open). C-REDUND-06 convergence fix is exactly this hook (report line 102/313). SKEPTIC correction: TWL-07 cites 'E4-S2 (seq 270)' but seq 270 does not exist in the board digest (the snapshot's sequence_id field is null so '270' was a bad cite). The actual PostToolUse extending subtasks are ASCP-E4-S2.3 (seq 127, 'PostToolUse changed-files handoff to wiring_checker.py') and ASCP-E4-S3.3 (seq 133, 'PostToolUse five-layer feedback hook'). The substance (no AC, no edge to the extenders) is real; only the seq pointer was wrong, so verdict=revised. P2 retained (redundancy/convergence gap, not spine-breaking).
- action[update]: UPDATE ascp-audit-reorg:NEW-10 (seq 253) to carry report §5 AC1-AC3, and add relates_to/blocking edges (via the live relations endpoint) to ASCP-E4-S2.3 (seq 127) and ASCP-E4-S3.3 (seq 133) so the 'both extend, neither re-registers' contract is enforceable. Add an AC asserting the hook runs ONE check on the ONE changed file and returns a located oracle + one corrective step (not a whole-suite re-run), and fails open (never exit 2).

## P2 WF-06 — Prior report's 'Workflow Kanban READY after apply' verdict is unverified-and-false; downstream readers will trust an inert state surface
- items: story:ASCP-E7-REQ16, story:ASCP-E7-REQ6, story:E6-S5, story:ASCP-E7-REQ23
- note: CONFIRMED as a flag-for-awareness. WF-01..WF-05 (all independently live-confirmed this pass) show the 'READY after apply' Kanban verdict is contradicted on every C16/S6/S7/S8 leg except REQ16: 6/7 C16 targets unmoved, REQ23 Blocked-without-edge live (relations empty), E6-S5 still the sole Done, 5 further items out of scope. The verdict was derived from the PLAN, not a live read-back — the apply_log only echoes intended fields ([description_html,state,labels]), not realized server state, which is precisely the GT-D3 hazard. An executor consuming the Workflow-Kanban to pick work sees false readiness.
- action[none]: Flag for awareness: do not treat plane-reconciliation-report.md line 367 as ground truth. After the WF-01..WF-05 PATCHes land, perform a live read-back (GET each /work-items/{id}/ + /relations/) and only then mark the Kanban honest. Standing applier rule: every UPDATE/field-edit op must be confirmed by a post-PATCH GET of realized state before being logged applied (closes the GT-D3 class report-wide), and multi-target ops must log one verified action per resolved id.

## P2 XC-06 — Phase-0 spec-completion items sit Spec-Verified while their load-bearing prereqs are NOT AUTHORED — softer outrun-state inconsistency (one cited item mis-stated)
- items: 37, 61, 258, story:ASCP-E5-S10, story:ASCP-E8-21, ascp-audit-reorg:NEW-02
- note: REVISED: real but the evidence was wrong on one item. LIVE API (read today): E5-S10=Spec-Verified, E8-21=Spec-Verified, NEW-02=Spec-Verified — all three CONSUME or ARE the D3 run_state populator which remediation 05 §7.1 line 432 classes NOT AUTHORED (and GT-6 confirms no such tool on disk), so the inconsistency with the C16/Kanban 'state reflects on-disk reality' rule is real. BUT the finding also cited ASCP-E3-4 as Spec-Verified; live state is Spec-Compiling (already off the verified tier), so E3-4 is dropped from board_items and the claim is narrowed. This remains a defensible posture (Spec-Verified = the SPEC is verified, not the implementation), hence op:none / flag-for-awareness rather than a forced true-up; the inconsistency is that the prior pass applied the C16 'state must reflect on-disk reality' rule to E7 but not to these Phase-0 consumers or to NEW-02 itself. P2 (polish/consistency), not P1.
- action[none]: Record one decision and apply it uniformly: EITHER keep E5-S10/E8-21/NEW-02 at Spec-Verified and add a one-line note that Spec-Verified means spec-only (implementation tracked separately), OR true-up NEW-02 (and the consumers' state claims) until the D3 populator/NEW-08 validator are authored — matching how C16 was enforced on E7. Do not leave the rule applied to E7 but not here.

## P3 BP-5 — best-practices doc 03 §1.3/§1.7 event lists are a point-in-time snapshot vs current official docs — an external-doc currency note, NOT a live-board claim!=reality
- items: 260, story:ASCP-E8-13
- note: The on-disk doc text matches the finding's quotes EXACTLY: 03-best-practices.md §1.3 (L46) lists additionalContext on 'PreToolUse, UserPromptSubmit, SessionStart, and Stop/SubagentStop (>=2.1.163)'; §1.7 (L88) lists no-matcher events as 'UserPromptSubmit, Stop, PostToolBatch, CwdChanged, MessageDisplay'. So the quoted staleness is real ON DISK. HOWEVER, per the adversarial bar (confirmed only if claim!=reality holds on the LIVE BOARD), this is NOT a board defect: the finding itself states 'No board item is wrong because of this' and uses op:none. It is an additive doc-currency observation about EXTERNAL official-docs coverage (PostToolUse additionalContext, PostToolUseFailure, PostToolBatch, SubagentStart, Setup, TeammateIdle/TaskCreated/TaskCompleted no-matcher events) that I cannot independently confirm against live board state. NEW-13 (seq 260, Spec-Verified) does formalize doc 03 as a tier-1 standard, so the flag is legitimate awareness but mis-severity as P2. Downgraded to P3; op:none retained. Evidence: 03-best-practices.md L46,L88; live seq 260 NEW-13 Spec-Verified.
- action[none]: Flag for awareness on NEW-13 (seq 260) before canonizing doc 03 as the tier-1 standard: re-pin the canonical-docs-fetched date and refresh §1.3 additionalContext event list + §1.7 no-matcher set against the current code.claude.com/docs/en/hooks reference (PostToolUse now supports additionalContext; add PostToolBatch/SubagentStart/Setup and the TeammateIdle/TaskCreated/TaskCompleted/WorktreeCreate no-matcher events). No live board item requires editing.

## P3 HGD-6 — Duplicate owner of tools/execution_bounds: both E8-20 (fold taken) and NEW-09 (Backlog, skip-unless-rejected) are live — dormant duplication of the registry single-source-of-truth
- items: 60, 256
- note: CONFIRMED both items are live but REVISED P2→P3. GET live descs verbatim: E8-20 (seq60, Spec-Verified) 'Expand E8-20 to OWN authoring tools/execution_bounds (PREFERRED over a standalone item)'; NEW-09 (seq256, Backlog) 'Conditional create. Only instantiate if UPD-E8-20 is NOT taken... Default-disabled: executor should prefer the E8-20 fold and skip this create unless that fold is rejected.' The fold WAS taken (E8-20 desc explicit) yet NEW-09 survives. Severity lowered because the real-world harm is low: NEW-09 is in Backlog and SELF-DISARMING ('skip this create unless the fold is rejected'), so an executor reading it is told not to act — dormant duplication, not an active second drifting source. Also PARTIALLY REFUTES the original recommended action: E8-20's live desc ALREADY carries the byte-consistency note ('The VERIFIER_ROLE default MUST be byte-consistent with the COH-1 reconciliation (verifier.md)'), so re-adding that AC is redundant. Net: tidy-up, not a P2 dynamism risk.
- action[field-edit]: Close/cancel NEW-09 (seq256) as superseded-by-E8-20 (the fold was taken), or convert it to a relates_to pointer at E8-20, so tools/execution_bounds has exactly one owner. Do NOT re-add the VERIFIER_ROLE byte-consistency note to E8-20 — it is already present. Optionally verify no consumer subtask (e.g. E4-S3#0 'coverage >= 85%', E2-S3 '7-check list') hardcodes a threshold the registry should own.