# Consolidated Remediation Plan — Spec-to-Evidence Control System

**Source:** 30-component completeness review (workflow wsiq7z0a4) → adversarial fix-validation (wp4k9f8rp + wdmrgoq33).
**Confirmed edits:** 329 → **318 distinct** after merging 11 shared-artifact keys. **Refuted:** 171.

**By severity:** DEFECT 39, INCONSISTENCY 86, MISSING-WIRING 77, UNDERSPECIFIED 116


## requirements.md — 41 edits

### 🔴 DEFECT (1)

**1. Define dangling REQ-SPEC-020..024 / REQ-SPEC-021 EARS statements or replace with canonical Req 4.x numbers**  ·  _[spec_validator.py]_
- **anchor:** requirements.md:101 'PRD references: REQ-SPEC-020..024' (Requirement 4); REQ-SPEC-021 referenced at design.md:209,718,726,842 but never EARS-defined
- **change:** REQ-SPEC-020..024 appear ONLY as PRD references (requirements.md:101, 385) and REQ-SPEC-021 is cited 4x in design.md as the Stop-hook spec-gate requirement but is never defined as an EARS statement anywhere. Either (a) add explicit REQ-SPEC-020..024 EARS definitions under Requirement 4 mapping REQ-SPEC-021 = criterion 4.2 (the violation_count>0 Stop-block), or (b) replace the dangling REQ-SPEC-0xx citations in design.md:209/718/726/842 with the canonical Requirement 4.2/4.6 criterion numbers, removing the dangling references.

### 🟠 INCONSISTENCY (20)

**2. Fix glossary wording implying evidence is always present on every item**  ·  _[feature_list.json]_
- **anchor:** Glossary feature_list.json entry, requirements.md:28
- **change:** Line 28 reads '...with type, status, dependencies, acceptance criteria, and evidence' as if evidence is always present, but evidence is optional and present only on proven items. Reword to '...with type, status, dependencies, acceptance criteria, in_scope, and (when proven) an Evidence_Record.'

**3. Cross-reference the 'Complex' EARS-pattern resolution from the glossary, Req 1.4, and Property 16**  ·  _[feature_list.json]_
- **anchor:** Glossary EARS line listing six patterns incl. Complex (requirements.md:31); Req 1.4 'five EARS patterns' (requirements.md:65)
- **change:** The glossary lists six EARS patterns (incl. Complex) while the schema enum (design.md:347), Req 1.4, and Property 16 use five; design.md:1188 resolves this ('Complex = a documented composition of the five base patterns; no schema change') but that resolution is not referenced anywhere the reader hits the five-vs-six mismatch. Add a parenthetical to the glossary line 31 (and a note in Req 1.4 and Property 16) pointing to the design.md:1188 resolution so 'six' (glossary) and 'five' (schema/Req 1.4) are explicitly reconciled.

**4. Update glossary: SessionStart_Hook responsibilities + add a Progress_File term**  ·  _[session_start_hook.py]_
- **anchor:** Glossary entry 'SessionStart_Hook' (requirements.md:27) and the glossary block (requirements.md:17-48) which defines Coverage_Model (L19) and feature_list.json (L28) but no progress-file term
- **change:** Two glossary gaps: (1) the SessionStart_Hook entry still reads only 'restores run state at the start of a session' — append '...and computes the resumed-state integrity hash, writing run_state.resume_integrity_ok (non-blocking; enforced by the PreToolUse integrity guard, Requirement 23)'. (2) criterion 11.3 (requirements.md:221) names 'the progress file' as a first-class load target, but no Progress_File glossary term exists (Coverage_Model and feature_list.json do). Add a glossary entry: 'Progress_File: the version-controlled claude-progress.txt checkpoint that SessionStart reads and PreCompact writes.'

**5. REQ-VERIFY-004 under-specifies the PostToolUse check set (omits type-check AND AST)**  ·  _[post_tool_use_hook.py]_
- **anchor:** Requirement 9, acceptance criterion 4 (requirements.md:189): "WHEN a file edit completes, THE PostToolUse_Hook SHALL run lint, SAST, and wiring checks..."
- **change:** Amend criterion 9.4 to enumerate the full check set so requirement, design hook-wiring row (design.md:216), and task 18.1 (tasks.md:311) agree: change 'run lint, SAST, and wiring checks' to 'run lint, type-check (mypy), SAST (Semgrep), and wiring checks'. This reconciles the requirement with design.md:216 ('lint, type check, SAST (Semgrep), wiring check') and task 18.1 which runs ruff+mypy+semgrep+wiring_checker. Note that the structural layer's AST analysis (REQ-VERIFY-001) is performed inside wiring_checker.py via the ast module (task 19.1, tasks.md:327), so the wiring check subsumes AST — state this so 'lint+type+SAST+wiring' is not misread as omitting the structural-layer AST element.

**6. Amend glossary 'Complex' EARS pattern to a documented composition of the five base patterns**  ·  _[spec_validator.py]_
- **anchor:** requirements.md:31 glossary 'EARS ... Patterns: Ubiquitous, Event-driven, State-driven, Unwanted behaviour, Optional, Complex' vs requirements.md:65 (Req 1.4 'exactly one of the five EARS patterns')
- **change:** requirements.md:31 lists SIX patterns including 'Complex' while Req 1.4 (line 65) says 'exactly one of the five EARS patterns' — internally contradictory within requirements.md. Annotate the glossary 'Complex' entry at line 31 as a documented composition of the five base patterns (not a 6th assignable enum value), matching design.md:1188 and the schema ears_pattern enum (design.md:347, which lists only the five). This makes requirements.md internally consistent and aligns with what spec_validator.py and Property 16 actually assign.
- _adds EARS/use-case wiring_

**7. Add EARS criterion + glossary text backing the feature_list_sha bind**  ·  _[plan-approved.json]_
- **anchor:** Requirement 18 acceptance criteria (lines 324-328); glossary entry `plan-approved.json` (line 43); Property 6 'Validates: 7.4, 18.1' (design.md:926)
- **change:** Add acceptance criterion 18.4: 'IF plan-approved.json exists BUT its feature_list_sha does not equal the canonical SHA-256 of the current feature_list.json, THEN the PreToolUse_Hook SHALL block all implementation Write/Edit/MultiEdit tools and require re-approval.' Add the SHA-binding to the glossary definition at requirements.md:43 (currently presence-only) so the glossary matches the load-bearing design behavior. Update Property 6's 'Validates' line (design.md:926) to cite the new 18.4 alongside 7.4/18.1, since neither 7.4 nor 18.1 states the SHA rule.
- _adds EARS/use-case wiring_

**8. Postgres glossary entry omits amendment history and audit log from durable-store contents**  ·  _[db/migrations/007_requirement_versions.sql]_
- **anchor:** - **Postgres**: The managed relational database used as the durable store for requirements, coverage state, traceability links, evidence, and run state.
- **change:** Update the Postgres glossary entry (requirements.md:44) to enumerate the now-eight-table durable store: add 'requirement-amendment history' and 'gate-decision audit log' to the list 'requirements, coverage state, traceability links, evidence, and run state' — both are first-class durable tables post-reconciliation (design.md:427 lists eight tables; migrations 007/008).

**9. Normalize 'timestamp' to created_at in REQ-AUDIT-001 and note it is DB-assigned**  ·  _[db/migrations/008_gate_audit_log.sql]_
- **anchor:** requirements.md:457 `1. THE System SHALL append every gate decision (event, tool, allow/block, reason, requirement ID, actor agent, timestamp) ...`
- **change:** Field-name drift: Req 27.1 lists 'timestamp', the DDL/canonical form name the column 'created_at' (design.md:1164,1170), and the producer append() signature omits it entirely. Change 'timestamp' to 'created_at (timestamp)' in requirements.md:457 and add a note in Req 27.4/27.5 that created_at is DB-assigned (DEFAULT now()) and therefore not a producer argument — one canonical name across all three surfaces.

**10. Fix dangling Z3 'CHECK (audit chain)' citation in the cross-ref matrix**  ·  _[db/migrations/008_gate_audit_log.sql]_
- **anchor:** requirements.md:510 matrix row `| — (audit-log tamper-evidence) | Property 28 | CHECK (audit chain) | Requirement 27 |`
- **change:** The matrix names Z3 group 'CHECK (audit chain)' for Property 28, but no such check exists in verification/formal_verification_merged.py (the new-8 group is CHECK-10a..13b = amendment, resumed-state, checklist, omission; grep for audit/chain/tamper/prev_hash/entry_hash returns zero). design.md:1110 itself says 'no Z3 check — runtime/CI verified'. Change the Z3-group cell from 'CHECK (audit chain)' to '— (runtime/CI, no Z3)' so the matrix matches design.md:1110 and the harness self-count (34 = 14+12+8, no audit check). Also correct the harness path reference to verification/formal_verification_merged.py where cited.

**11. Fix dangling Z3 CHECK placeholder for Property 28 in the cross-reference mapping table**  ·  _[audit_log.py]_
- **anchor:** Line 510: | — (audit-log tamper-evidence) | Property 28 | CHECK (audit chain) | Requirement 27 |
- **change:** Replace the non-existent Z3 group id 'CHECK (audit chain)' in the Property 28 row with an honest runtime/CI marker that matches design.md:1110 ('no Z3 check — runtime/CI verified'). Change the 'Z3 group' cell to '— (runtime/CI: audit_verify.py + Property 28 PBT, task 39.18)'. The harness (formal_verification_merged.py) self-counts 34 = 14 core + 12 Kiro + 8 new (CHECK-10a..13b = amendment/resume/checklist/omission); there is NO audit-chain CHECK, so the current cell points at a CHECK id that does not exist in the harness. Do NOT add a fake CHECK to keep the 34 count honest unless you also bump the self-count.

**12. Reconcile caller-set: REQ-AUDIT-001.4 names 3 hooks but design wires a 4th producer call site**  ·  _[audit_log.py]_
- **anchor:** Line 460: Requirement 27.4 '...SHALL be CALLED by stop_hook.py, pre_tool_use_hook.py, and subagent_stop_hook.py on every allow/block decision'
- **change:** Add a clarifying clause to Requirement 27.4 that subagent_stop_hook.py includes BOTH its evidence-schema decision (design.md:217) and its omission-declaration-guard block decision (design.md:218) as audit-log call sites — i.e. 3 hook files / 4 decision points. As written, the requirement enumerates exactly three call sites while design.md:218 adds a fourth append() invocation (the omission guard), so requirement text and design wiring table disagree on the producer call-site set.

**13. Replace dangling 'CHECK (audit chain)' Z3 citation with runtime/CI marker**  ·  _[audit_verify.py]_
- **anchor:** | — (audit-log tamper-evidence) | Property 28 | CHECK (audit chain) | Requirement 27 |
- **change:** In the cross-reference table (requirements.md:510), change the 'Z3 group' cell for the audit-log tamper-evidence / Property 28 row from 'CHECK (audit chain)' to '— (runtime/CI — no Z3)'. The merged harness ends at CHECK-13b and contains no audit-chain assertion (verified); design.md:1110 and PRD:79 both state tamper-evidence is runtime/CI-verified with no Z3 check.

**14. Non-emptiness inconsistency: omission gate accepts 'absent or null' only, Evidence sibling gate rejects 'absent or empty'; REQ-SPEC-018.3 silent on empty**  ·  _[omission_guard (capability in subagent_stop_hook.py)]_
- **anchor:** Requirement 29 criterion 2 (requirements.md:477): 'THE SubagentStop hook SHALL reject any subagent result whose omission_declaration field is absent or null, with exit 2.' — contrast with Requirement 5.6 (requirements.md:127): 'rejected if any of the four fields is absent or empty', and criterion 29.3 (requirements.md:478) which is silent on empty
- **change:** Decide and state uniformly whether an empty-but-non-null omission_declaration ('', {}, []) blocks. If yes: change 29.2 wording to 'absent, null, or empty', mirror in design.md:148 and design.md:218 and Property 30 (design.md:1128), make 29.3 require non-empty (analogous to the EvidenceRecord additionalProperties:false + non-empty CHECK at design.md:474-477), and add an omissionDeclEmpty predicate to the harness so CHECK-13 covers the empty case. If no: add a one-line note to 29.2 that empty IS accepted by design, to make the divergence from the Evidence gate explicit and intentional.

**15. Fix B15→B16 baseline-block collision for Requirement 31 (DAST) in both matrix row and header**  ·  _[zap-baseline.yml]_
- **anchor:** Line 418 matrix row `| 31 DAST Baseline Security Scan | B15 | REQ-SEC-008 |` AND line 487 header `**Baseline block:** B15 | **PRD references:** REQ-SEC-008` (Requirement 31)
- **change:** Reassign Requirement 31 / REQ-SEC-008 from B15 to B16. B15 is 'Durable storage' (line 397, mapped to Requirement 16 / REQ-STORE-001..003); B16 is 'Supply-chain & security gates' (line 398, REQ-SEC-001..005) which is the correct home for a DAST security scan. Change BOTH the traceability-matrix row (line 418) and the Requirement-31 header (line 487) from `B15` to `B16`. Do NOT use B17 ('Human-in-the-loop approval') — the kill-switch (Req 32) is dual-tagged B16/B17 only because it spans gates+approval; a pure DAST scan belongs to B16 alone. B16 is already defined in the baseline-block list (line 398), so no list change is needed.

**16. Kill-switch ≤30s polling interval missing from Requirement-20 threshold registry**  ·  _[flagd]_
- **anchor:** Requirement 20.3 list (requirements.md:356-359): '- **Cost/token budget** = 1,000,000 tokens per Slice ... - **Reasoning-loop K** = 3 ... - **Retry budget** = 3 per Slice — OWNER: the implementer loop ...'
- **change:** Register the kill-switch polling interval as a new bullet under Requirement 20.3: '- **Kill-switch flag-propagation interval** = ≤30 s (configurable) — OWNER: the Delivery Owner / Operator (owns the flagd deployment); the affected agent capability is disabled within this interval without a process restart (REQ-CTRL-001 / Requirement 32.2).' This restores the REQ-20.2 'all numeric DEFAULTs are configurable with an owner' discipline.

**17. B16/B17 coverage matrix never updated to show Requirement 32 extends those blocks**  ·  _[flagd]_
- **anchor:** Domain-Baseline Coverage Matrix rows: '| B16 | Supply-chain & security gates | Requirement 17 (REQ-SEC-001..005) | MAPPED |' and '| B17 | Human-in-the-loop approval | Requirement 18 (REQ-HITL-001..003) | MAPPED |' (requirements.md:398-399)
- **change:** Make the B16/B17 trace internally consistent with the addendum extension table (requirements.md:419). Either (a) add Requirement 32 to the B16 and B17 rows, e.g. 'Requirement 17 (REQ-SEC-001..005); extended by Requirement 32 (REQ-CTRL-001)', or (b) add a footnote marker on both rows pointing to the addendum baseline-traceability table at line 405-419. Keeps the declarative 'extends those same blocks, no requirement orphaned' assertion (requirements.md:403) reflected in the actual matrix.

**18. Reconcile EARS pattern count: glossary 'six' vs Req 1.4 'five'**  ·  _[initializer.md]_
- **anchor:** - **EARS**: Easy Approach to Requirements Syntax — the requirements-expression standard used throughout. Patterns: Ubiquitous, Event-driven, State-driven, Unwanted behaviour, Optional, Complex.
- **change:** Amend the glossary EARS entry to state the five base patterns plus 'Complex' as an explicit composition, not a sixth enum value: '...Patterns: five base patterns — Ubiquitous, Event-driven, State-driven, Unwanted behaviour, Optional — plus Complex (a documented composition of the five base patterns, not a separate enum value; the feature_list.json ears_pattern enum and spec_validator.py operate only over the five).' This aligns the glossary with Req 1.4's 'exactly one of the five EARS patterns', Property 16's five-value enum, and the reconciliation note already in design.md:1187-1188.

**19. Make Requirement 1.2 cite the canonical vague-adjective set**  ·  _[initializer.md]_
- **anchor:** 2. IF a candidate requirement contains a non-deterministic adjective (e.g., "fast", "secure", "scalable", "optimized") without a quantified numeric bound, THEN THE Spec_Compiler SHALL reject it and require a quantified criterion before acceptance.
- **change:** Change Req 1.2's parenthetical from the 4-example illustrative list to the canonical 7-member set used by the spec_validator scanner (tasks.md:68) and Property 17 (design.md:1012), or explicitly mark it non-exhaustive: 'a non-deterministic adjective (the canonical reject-set is {fast, secure, scalable, optimized, efficient, reliable, performant}; the validator may extend it)'. As written, the 4-example requirement implies a smaller reject-set than the validator the initializer routes through actually enforces.

**20. omission_declaration scope omits the implementer; resolve 18.1-vs-18.3 inconsistency at the implementer SubagentStop boundary**  ·  _[implementer.md]_
- **anchor:** 1. WHEN a subagent (initializer, research, verifier) completes a discovery, research, or verification pass, THEN its output SHALL include a non-null `omission_declaration` field
- **change:** REQ-SPEC-018.1 (requirements.md:476) enumerates only '(initializer, research, verifier)' while REQ-SPEC-018.3 (requirements.md:478) says 'THE JSON schema for EACH subagent output type SHALL include omission_declaration as a required, non-nullable field' and the SubagentStop omission guard (design.md:218) rejects any subagent result lacking it. This leaves the implementer's SubagentStop return contract ambiguous: the implementer legitimately emits NO Evidence_Record, so subagent_stop_hook (tasks.md:177-183, REQ-VERIFY-005/9.5) and the omission guard need an explicit rule for the implementer→verifier handoff. Resolve by either (a) explicitly listing the implementer in 18.1 with its own omission_declaration obligation, or (b) stating in 18.1/design.md:218 that the implementer subagent is exempt from the evidence-field and omission-declaration checks at SubagentStop (it produces a commit, not evidence) — and reflect the chosen rule in subagent_stop_hook.py task 8.1 and the omission_guard component (design.md:148).
- _adds EARS/use-case wiring_

**21. Reconcile Phase-0 five-layer claim with Phase 1/3 perf_a11y_verifier deferral**  ·  _[verifier.md]_
- **anchor:** Requirement 9.1 (requirements.md:186) 'verify each Slice across five layers ... performance + accessibility'; perf_a11y_verifier Phase '1/3' (design.md:146, design.md:1182); task 51 (tasks.md:966)
- **change:** requirements.md:186 asserts the Verifier runs all five layers unconditionally, but the fifth layer (perf_a11y_verifier) is Phase 1/3 (design.md:146, design.md:1182) and its implementation is task 51 in the Phase-4-era added-task block (tasks.md:966), while the Verifier itself ships in Phase 0. The Phase-0 spine therefore cannot deliver the fifth layer. Scope requirements.md:186 to note the performance/accessibility layer activates in Phase 1/3 (the structural/semantic/behavioral/security layers ship in Phase 0), OR move perf_a11y_verifier to Phase 0. Keep consistent with the design Component Inventory Phase column.

### 🟡 MISSING-WIRING (3)

**22. Wire the 18.2 missing-context 'notes' injection to a consumer or drop the phrasing**  ·  _[plan-approved.json]_
- **anchor:** Requirement 18.2 (requirements.md:327, 'present them in plan mode for human approval and missing-context injection'); design.md:768-769 `notes` field
- **change:** Req 18.2 makes 'missing-context injection' a SHALL obligation but the only realization is a free-text `notes` field consumed by nothing. EITHER specify a consumer in design.md (e.g. the Initializer reads plan-approved.json.notes on next session and injects it into run_state / the Implementer's context, with a task to do so) OR strike 'and missing-context injection' from the 18.2 criterion so the SHALL no longer promises an unimplemented behavior. As written the obligation is unaddressed, not merely 'partial'.
- _adds EARS/use-case wiring_

**23. Kill-switch toggles produce no audit-log / OTel trail**  ·  _[flagd]_
- **anchor:** Requirement 32 acceptance criteria (requirements.md:494-496); cross-ref Requirement 27 gate_audit_log (requirements.md:455-462) and REQ-OBS (requirements.md:236)
- **change:** Add an acceptance criterion to Requirement 32: 'WHEN a kill-switch flag is set or cleared, THE System SHALL append a gate_audit_log entry (event, actor_agent/operator, decision, reason, requirement_id, timestamp) via tools/audit_log.append(...) AND emit an OTel gate-decision event to the same trace endpoint as other gate decisions, so the toggle is in the tamper-evident hash chain (REQ-AUDIT-001..003) and observable (REQ-OBS-003).' Mirror in design.md by listing the kill-switch toggle among audit_log.append callers in the new Kill-Switch subsection.
- _adds EARS/use-case wiring_

**24. Bind Spec_Compiler glossary entry to its realizing component initializer.md**  ·  _[initializer.md]_
- **anchor:** - **Spec_Compiler**: The component responsible for transforming product intent into atomic, individually-testable requirements with IDs and acceptance criteria.
- **change:** Append a realization clause to the Spec_Compiler glossary entry so a requirements-only reader can trace the abstract component to its Phase-0 implementation: '(realized by `.claude/agents/initializer.md` — Phase 0; see design.md Subagent Definitions).' This is the only place the abstract→concrete binding lives outside design.md/tasks.md.
- _adds EARS/use-case wiring_

### ⚪ UNDERSPECIFIED (17)

**25. State the authoritative 7-element vague-adjective set in the requirements layer**  ·  _[spec_validator.py]_
- **anchor:** requirements.md:63 (Req 1.2) 'non-deterministic adjective (e.g., "fast", "secure", "scalable", "optimized")'
- **change:** Req 1.2 gives a 4-element 'e.g.' example set {fast, secure, scalable, optimized}, but the implemented Property 17 (design.md:1012) and Task 4.4 (tasks.md:87) use the 7-element set {fast, secure, scalable, optimized, efficient, reliable, performant}. The requirement never states the authoritative full set, so the scanner's coverage is only test-backed. State the 7-element set once in Req 1.2 (or the Req 20 threshold registry) and reference it from Property 17 and Task 4.4 so the implemented set is requirement-backed.

**26. Operationalize REQ-AUDIT-003 retention (window / archival / immutability)**  ·  _[db/migrations/008_gate_audit_log.sql]_
- **anchor:** requirements.md:459 `3. THE System SHALL retain the hash-chained gate-decision log as the execution-proof complement to SLSA build provenance ...`
- **change:** REQ-AUDIT-003 asserts 'SHALL RETAIN' the log as a long-lived compliance substrate but no retention window, archival/partitioning strategy, storage location, or immutability-of-archived-segments is defined anywhere in requirements.md/design.md/tasks.md. Add a concrete retention sub-clause to Req 27 (requirements.md:459) and a corresponding design note near design.md:1156-1177: define the minimum retention duration, whether/how old segments are archived or partitioned, and that archived segments remain hash-verifiable (the chain extends across archive boundaries), then wire it as a task under task 52.
- _adds EARS/use-case wiring_

**27. Add requirements-doc implementation pointer for tools/state_integrity.py**  ·  _[state_integrity.py]_
- **anchor:** ### Requirement 23: Resumed-State Integrity (requirements.md:435-438), acceptance criteria 1-2 — never reference tools/state_integrity.py by name
- **change:** Requirement 23 / REQ-STATE-005 describes the behavior ('compute the resumed-state hash', 'PreToolUse integrity guard') but never names the owning artifact, so a reader of requirements.md alone cannot trace it. Add an implementation pointer such as 'Implementation: tools/state_integrity.py (see design Component Inventory; tasks.md task 49).' to Requirement 23, mirroring how other requirements cite their references.
- _adds EARS/use-case wiring_

**28. Define the central REQ-STATE-005 domain terms in the glossary**  ·  _[state_integrity.py]_
- **anchor:** ## Glossary (requirements.md:17-48) — defines Coverage_Model, Postgres, SessionStart_Hook, PreToolUse_Hook, etc., but has no entry for 'resumed-state hash', 'durable store's recorded hash', or 'operator reconciliation'
- **change:** The three concepts on which Requirement 23 correctness rests are undefined glossary terms, which is what makes the hash inputs and the reconciliation action ambiguous at the requirements layer. Add glossary entries for 'Resumed-State Hash' (the sha256 over the canonical durable+file state — cross-ref the design hash-algo spec), 'Recorded State Hash' (the baseline persisted in run_state.state_hash), and 'Operator Reconciliation' (the human-driven re-record-and-unblock action that flips resume_integrity_ok true).

**29. Specify a retention/archival policy (or accept unbounded) for REQ-AUDIT-003**  ·  _[audit_log.py]_
- **anchor:** Line 459: Requirement 27.3 'THE System SHALL retain the hash-chained gate-decision log as the execution-proof complement to SLSA build provenance.'
- **change:** REQ-AUDIT-003 says 'retain' and append-only but bounds neither growth nor retention duration; the gate_audit_log DDL (design.md:1156-1167) has no retention column or policy. An append-only hash chain cannot prune the middle without breaking verification. Add a retention clause: either (a) a chain-compatible checkpoint-and-archive of a verified prefix that records the archived prefix's tip hash as the new genesis-equivalent prev_hash, or (b) an explicit statement that unbounded retention is accepted for v1.

**30. Add a glossary term for the secrets-scanner tool family**  ·  _[secrets-scan.yml]_
- **anchor:** Glossary, after line 47 ('Semgrep_CodeQL: The SAST tools used in CI ...')
- **change:** The secrets-scanner is the only security-gate tool family with no glossary entry (Semgrep_CodeQL, Playwright, OPA_Conftest, SLSA all defined). Add: 'Secrets_Scanner: the CI tool (gitleaks by default; trufflehog as an acceptable alternative) used to detect committed secrets/credentials/tokens in a PR diff and fail the build (REQ-17.2).'

**31. Resolve REQ-SPEC-018.1 structured-content enforcement gap (categorical [Gap] enumeration is unenforced; mechanism is null/non-null boolean only)**  ·  _[omission_guard (capability in subagent_stop_hook.py)]_
- **anchor:** Requirement 29 Structured Omission Declaration, criterion 1 (requirements.md:476): 'WHEN a subagent ... SHALL include a non-null omission_declaration field listing, by EARS scenario category ... using a [Gap] marker for each omitted class.' vs criterion 2 (requirements.md:477) + CHECK-13a/13b which only model a single boolean omissionDeclNull
- **change:** State explicitly which of two paths governs the structured-content half. EITHER (a) add a sentence to 29.1/29.2 clarifying the SubagentStop guard enforces presence/non-null ONLY, and the per-category [Gap] enumeration is a non-gating authoring convention (downgrading the machine-checked claim); OR (b) extend 29.3 / the subagent_output schema to require the six category keys (each an array) plus a [Gap]-marker format constraint, and add a property-test obligation for it. Pick one and record it, so that omission_declaration: "" or {} omitting categories cannot silently satisfy a criterion the harness never checks.

**32. Glossary lacks an omission_declaration term and the SubagentStop_Hook term omits the omission-declaration responsibility**  ·  _[omission_guard (capability in subagent_stop_hook.py)]_
- **anchor:** Glossary entry (requirements.md:24): '**SubagentStop_Hook**: The Claude Code SubagentStop hook that validates evidence schema before accepting a subagent's result.'; and Evidence_Record term (requirements.md:29) — there is no omission_declaration term
- **change:** Extend the SubagentStop_Hook glossary term to mention BOTH responsibilities: '...validates evidence schema AND enforces the omission-declaration gate (rejects any subagent result whose omission_declaration is absent/null) before accepting a subagent's result.' Add a new glossary term '**omission_declaration**: A structured field on initializer/research/verifier subagent outputs, keyed by the six EARS scenario categories (Primary/Alternate/Exception/Recovery/Non-Functional/Edge-Case), each a list of [Gap]-marked uncovered scenario classes; non-null and required (REQ-SPEC-018).' — mirroring how Evidence_Record is fully glossed.

**33. Register the 0.8/0.7 eval thresholds in the Requirement-20 threshold registry with an owner and override path**  ·  _[deepeval_gate.py]_
- **anchor:** 3. THE System SHALL register the following additional DEFAULT thresholds in the threshold registry *(Reconciliation 2026-06-15: numeric defaults added)*:
- **change:** Add two entries to the Req 20.3 registry: 'faithfulness threshold = 0.8 (DEFAULT, configurable) — OWNER: the quality owner (Req 9)' and 'answer-relevancy threshold = 0.7 (DEFAULT, configurable) — OWNER: the quality owner (Req 9)', each with an explicit operator-override path, and drop the 'e.g.' qualifier at requirements.md:483/484 so 0.8/0.7 are authoritative DEFAULTs not illustrations. Req 20.2 mandates ALL numeric thresholds be configurable DEFAULTs requiring operator override; the eval metrics currently appear only as 'e.g.' examples and are unowned, violating that invariant.

**34. Specify the eval input/dataset source and reconcile criterion-2 WHEN-guard with the unconditional REQUIRED check**  ·  _[deepeval_gate.py]_
- **anchor:** 2. WHEN a CI run includes LLM output evaluation, THEN the eval step SHALL gate on configured metric thresholds (e.g., faithfulness ≥ 0.8, answer relevancy ≥ 0.7) using DeepEval `assert_test()`, failing the build if any threshold is not met.
- **change:** Name the producer of the evaluable output and the dataset source for the DeepEval LLMTestCase fields (input / actual_output / retrieval_context) — e.g., a golden-set fixture under tests/eval/ or evidence captured from Verifier runs — and reconcile the WHEN-guard with criterion 3: state what happens when a CI run contains NO LLM output (the REQUIRED `deepeval-gate` check must define whether it passes vacuously or is skipped, so the unconditional REQUIRED status at criterion 3 is not either always-green-and-meaningless or blocking-with-nothing-to-evaluate). Faithfulness and answer-relevancy are RAG-style metrics needing (input, actual_output, retrieval_context) tuples; no producer is sourced anywhere in the spec.
- _adds EARS/use-case wiring_

**35. Make REQ-EVAL-001 traceability concrete: add a verification mechanism (smoke test) and/or a cross-ref-table row**  ·  _[deepeval_gate.py]_
- **anchor:** | — (omission-declaration gate) | Property 30 | CHECK-13a/13b | Requirement 29 |
- **change:** Add a row to the property↔requirement cross-reference table (requirements.md:505-512) for Requirement 30 stating it carries NO Z3 Property/CHECK and is verified by CI behavior (the `deepeval-gate` named tool) — making the PRD:130 traceability claim ('every new requirement carries a Z3 check OR a named verification tool') concrete and killing the Property-30-vs-Requirement-30 collision trap (Property 30 belongs to Requirement 29; Requirement 30 has no property). Pair with a tasks.md smoke/integration test task asserting `deepeval-gate` fails the build when a metric is below threshold, since the harness stops at CHECK-13b and no PBT property covers REQ-EVAL-001.
- _adds EARS/use-case wiring_

**36. Define eval metric terms in the requirements glossary**  ·  _[deepeval_gate.py]_
- **anchor:** - **Playwright**: The E2E test framework used to produce behavioral proofs (captured traces/screenshots) as evidence.
- **change:** Add glossary entries defining the DeepEval-specific terms that gate a REQUIRED merge check: 'faithfulness', 'answer relevancy', and 'eval-gate' (deepeval-gate). The requirements.md glossary (lines 15-48) defines OTel, SLSA, Semgrep_CodeQL, etc., but none of the three eval terms that a REQUIRED check depends on — leaving the gate's core metrics undefined.

**37. Document whether deepeval-gate decisions are captured in the gate_audit_log, or why CI-status-check gates are out of its scope**  ·  _[deepeval_gate.py]_
- **anchor:** 4. **Producer:** a new `tools/audit_log.py` SHALL expose `append(event, tool, decision, reason, requirement_id, actor_agent)`, and it SHALL be CALLED by `stop_hook.py`, `pre_tool_use_hook.py`, and `subagent_stop_hook.py` on every allow/block decision; `tools/audit_verify.py` SHALL recompute the chain.
- **change:** Add a one-line scope note to REQ-AUDIT (Req 27) stating that the hash-chained gate_audit_log captures Claude Code hook decisions, and that CI-status-check gates (deepeval-gate, zap-baseline, coverage-gate, secrets-scan, audit-chain-verify) are recorded via GitHub check-run history / SLSA rather than the hook audit log — OR, if a deepeval-gate block should be in the chain, add it to the producer caller list. Today a REQUIRED merge-gate decision (deepeval-gate) is captured by neither the audit-log caller list nor an explicit out-of-scope note, leaving a hole in the 'we can prove what happened' chain.
- _adds EARS/use-case wiring_

**38. Add Glossary entry for 'DAST'**  ·  _[zap-baseline.yml]_
- **anchor:** Glossary block, requirements.md:15-48 (defines Semgrep_CodeQL at line 47 and SLSA at line 37, but not DAST; 'DAST' is used in the Requirement 31 title at line 486 and criterion 31.2 at line 489)
- **change:** Add a Glossary entry after the Semgrep_CodeQL entry (line 47): '**DAST**: Dynamic Application Security Testing — black-box scanning of a running application; here the OWASP ZAP baseline passive scan (REQ-SEC-008).' DAST is currently used in the Requirement 31 title/criterion without a definition, unlike the peer security terms Semgrep_CodeQL and SLSA which are defined.

**39. Annotate REQ-SEC-008 as a [DET] CI-gate obligation explicitly outside the formal-verification harness scope**  ·  _[zap-baseline.yml]_
- **anchor:** Requirement 31 header/criteria, requirements.md:486-490 (no Property, no Z3 CHECK; harness formal_verification_merged.py has zero hits for zap/dast/REQ-SEC-008 — only REQ-SEC-004 at line 144)
- **change:** Add a one-line note at the Requirement 31 header (line 486) marking REQ-SEC-008 as a [DET] CI-gate obligation outside the Z3 formal-verification harness scope (as is implicitly true for secrets-scan, coverage-gate, deepeval-gate), so the absence of a Property/CHECK is documented intent rather than an oversight. This resolves the ambiguity flagged by the harness-coverage EARS item.

**40. No accountable owner/role named for kill-switch flag flips**  ·  _[flagd]_
- **anchor:** Requirement 32 acceptance criteria (requirements.md:494-496)
- **change:** Add an acceptance criterion to Requirement 32 naming the accountable human role and binding the action: 'THE System SHALL restrict kill-switch flag toggles to the Delivery Owner / Operator role; each toggle SHALL be authenticated and recorded.' This binds the B17 (Human-in-the-loop) mapping to an actor, analogous to how plan-approval binds to plan-approved.json written by a named human. Pair with the audit-log wiring edit so the toggle is recorded in the tamper-evident chain.
- _adds EARS/use-case wiring_

**41. Define REQ-SPEC-013..015 (and REQ-SPEC-010..012) as explicit EARS lines or document the alias mapping**  ·  _[research.md]_
- **anchor:** ### Requirement 3: Domain-Baseline Checklist Sourcing — **PRD references:** REQ-SPEC-013..015 (requirements.md:87); and Requirement 2 — REQ-SPEC-010..012 (requirements.md:73)
- **change:** REQ-SPEC-013/014/015 (cited for Requirement 3) and REQ-SPEC-010/011/012 (cited for Requirement 2) appear ONLY as label ranges (requirements.md:73, 87, 384, 401) and are never defined as individual EARS statements; the ID-to-criterion mapping is implicit and unverifiable. Add an explicit mapping note under Requirement 2 and Requirement 3 stating that REQ-SPEC-010=2.1, 011=2.2, 012=2.3 and REQ-SPEC-013=3.1, 014=3.2, 015=3.3 (or render them as explicit EARS sub-IDs). This is a cross-component issue shared with the Proactive-Discovery component (Requirement 2).


## design.md — 147 edits

### 🔴 DEFECT (19)

**42. Add conditional schema rule enforcing Evidence_Record when status=proven**  ·  _[feature_list.json]_
- **anchor:** definitions.CoverageItem JSON block (design.md:322-385); required list at line 324; evidence property at 381-383
- **change:** Add an allOf/if-then to CoverageItem in the design.md schema block: `"allOf": [{"if": {"properties": {"status": {"const": "proven"}}, "required": ["status"]}, "then": {"required": ["evidence"]}}]` so a schema-valid item cannot be status=proven with no evidence object. Update the Phase-0 schema test row (design.md:623) to also assert a status=proven item with NO evidence object is rejected (today it only rejects a missing output_hash inside an existing evidence object). Add a note in Req 5.3/5.6 and Property 2 that the four-field gate is jointly schema- AND hook-enforced.
- _adds EARS/use-case wiring_

**43. Block COMPLETE/merge on an empty (zero-item) feature_list.json**  ·  _[feature_list.json]_
- **anchor:** items array (no minItems) design.md:315-319; evaluate_stop completion gate design.md:728-736; OPA zero-evidence policy
- **change:** tools/feature_list_init.py writes `items: []` (tasks.md:239) and the items array has no minItems, so an all-empty coverage model trivially satisfies the Stop gate (not_proven is empty → allow → COMPLETE) and the OPA zero-evidence policy, reading as COMPLETE before discovery has run. Add a precondition: gates (Stop completion path and OPA merge policy) must additionally require that discovery has run / items is non-empty (or that no domain-baseline item is UNMAPPED) before a COMPLETE verdict; document that `items: []` is a valid INIT state but not a valid COMPLETE state, and add a test asserting an empty file blocks COMPLETE.
- _adds EARS/use-case wiring_

**44. Define MAX_TURNS_PER_SLICE constant and reconcile run_state.budget_exceeded / token_budget_usd against the DDL**  ·  _[stop_hook.py]_
- **anchor:** evaluate_stop pseudocode lines 699-707: `if run_state.iteration_count >= MAX_TURNS_PER_SLICE:` and `if run_state.budget_exceeded:           # token_cost_usd >= token_budget_usd`
- **change:** MAX_TURNS_PER_SLICE is used at design.md:699/701 but never defined as a module constant (only N_PROGRESS_WINDOW=3 and SPEC_COMPLETION_HARD_CAP=7 are). Add a module-constant block near line 654 (e.g. `MAX_TURNS_PER_SLICE = 25  # DEFAULT, bound to the task-44 execution-bounds config module`). Also `run_state.budget_exceeded` and `token_budget_usd` (line 704) have NO backing column: the run_state DDL (481-496) defines only `token_cost_usd NUMERIC(10,4)`. Either add a `token_budget_usd` column to the run_state DDL, or replace `run_state.budget_exceeded` with a computed predicate `run_state.token_cost_usd >= TOKEN_BUDGET` where TOKEN_BUDGET is a config constant bound to the Requirement-20 registry (req:357). Bind MAX_TURNS_PER_SLICE and the budget threshold to task 44 (Execution-bounds config module).

**45. Fix systematic Property→Requirement off-numbering (Property 26 mislabeled Req 27)**  ·  _[session_start_hook.py]_
- **anchor:** design.md:1090 '**Validates: Requirements 27 (REQ-STATE-005). Machine-checked: CHECK-11...**' (and the same family: L1080 Property 25, L1100 Property 27, L1110 Property 28)
- **change:** The canonical addendum mapping (requirements.md:507-512) makes REQ-STATE-005 = Requirement 23, but Property 26 cites 'Requirements 27' (Req 27 is the Tamper-Evident Audit Log). This mislabels THIS component's own correctness Property. Change 'Requirements 27' → 'Requirement 23' on L1090. The off-by-N is systematic across the block: Property 25 'Requirements 26' → Requirement 22 (REQ-COV-007); Property 27 'Requirements 25' → Requirement 24 (REQ-SPEC-016); Property 28 'Requirements 28' → Requirement 27 (REQ-AUDIT). Fix all four to match the requirements.md:505-512 cross-reference table.

**46. Fix validate_spec() signature gap — Completeness/UNMAPPED check needs the domain-baseline item set as input**  ·  _[spec_validator.py]_
- **anchor:** design.md:133 / tasks.md:66 signature 'validate_spec(requirements: list[dict]) -> dict'; design.md:559 Completeness check 'for each domain-baseline item'
- **change:** The Completeness check (design.md:559) flags UNMAPPED 'for each domain-baseline item', but the domain-baseline checklist set is NOT a parameter of validate_spec(requirements: list[dict]) (signature at design.md:133, tasks.md:66) — it lives in domain_baseline_checklists / the checklist file. As written the validator cannot compute 'uncovered' from its inputs. Either extend the signature to validate_spec(requirements: list[dict], baseline_items: list[dict]) -> dict (and update Task 4.1, Property 14, the design.md:632 test) or specify how validate_spec loads the baseline set, so the uncovered/UNMAPPED field has a defined source.

**47. Fix the formal_verification_merged.py CI-invocation path mismatch**  ·  _[coverage_query.rego]_
- **anchor:** Phase 0 Component List row 10 — '`python3 formal_verification_merged.py` exits 0 (all 34 checks pass)' (design.md:632); same bare path in tasks.md:252 and tasks.md:301
- **change:** Update the invocation path from `python3 formal_verification_merged.py` to `python3 verification/formal_verification_merged.py` in design.md:632 and tasks.md:252-254 / task 17.1 (tasks.md:301), because the harness actually lives at `verification/formal_verification_merged.py` (verified: no file at repo root). The spec wires a bare repo-root path into CI (task 13.1) that does not match the file's real location — a CI-wiring defect that would make the formal-verification required check fail to find the harness.

**48. Add if/then so evidence is required when status==proven**  ·  _[feature_list.schema.json (schema/)]_
- **anchor:** design.md:381-384 (CoverageItem.evidence $ref with prose 'Required when status = proven') and design.md:372-374 (status enum)
- **change:** Add a draft-07 conditional to the CoverageItem definition: `"if": {"properties": {"status": {"const": "proven"}}}, "then": {"required": ["evidence"]}`. Without it an item with status:proven and no evidence object validates, leaving Property 2 (design.md:890-894) and Req 5.3/5.6 unenforceable by the schema alone. Update tasks.md:25-29 (task 2.1) acceptance to include this clause.

**49. Add minLength:1 to EvidenceRecord string fields (non-empty enforcement)**  ·  _[feature_list.schema.json (schema/)]_
- **anchor:** design.md:392-409 (EvidenceRecord.properties test_file/test_name/collected_at) and Property 2 'present and non-empty' at design.md:892
- **change:** Add `"minLength": 1` to test_file, test_name, and collected_at (output_hash and acceptance_criteria items are already de-facto non-empty via pattern/minItems). Req 5.6 and Property 2 demand each of the four fields be 'present AND non-empty', but `required`+`type:string` accepts the empty string. EITHER add minLength here OR change Property 2 (design.md:890-894) to state non-emptiness is delegated to evidence_collector.validate_evidence_record (tasks.md:46) rather than the schema — pick one and make schema/Property consistent.

**50. actor_agent forbidden by additionalProperties:false but NOT NULL in Postgres + Property 24**  ·  _[feature_list.schema.json (schema/)]_
- **anchor:** design.md:387-390 (EvidenceRecord required=4 fields, additionalProperties:false) vs design.md:473 (evidence_records.actor_agent TEXT NOT NULL) vs design.md:1066-1072 (Property 24, Validates Req 9.2)
- **change:** Add `actor_agent` (type:string, minLength:1) to the JSON-Schema EvidenceRecord properties so the schema and the Postgres NOT NULL column agree, OR change additionalProperties to allow it. As written, an EvidenceRecord carrying actor_agent (required by the DB CHECK and by Property 24's 'actor_agent must be the Verifier') FAILS jsonschema.validate, and one that passes the schema cannot satisfy the NOT-NULL column — a real schema/DB/Property-24 three-way divergence. Reconcile in design.md:387-411 and note that EvidenceRecord field count becomes 5 (the four-field gate stays the proven-transition gate; actor_agent is provenance).

**51. Resolve plan-approved.json against repo root, not cwd**  ·  _[plan-approved.json]_
- **anchor:** `plan_path = Path("plan-approved.json")` and `Path("feature_list.json")` (design.md:781, 791); inventory notation `/plan-approved.json` (design.md:139); worktree cwd isolation (design.md:514-527)
- **change:** The hook resolves plan-approved.json (and feature_list.json) cwd-relative, but the system deliberately runs agents inside per-slice git worktrees whose cwd is NOT the repo root — so the root-level marker is missed and the gate mis-fires. Pin resolution to the repo root explicitly (e.g. `repo_root = Path(subprocess.check_output(['git','rev-parse','--show-toplevel']).decode().strip()); plan_path = repo_root / 'plan-approved.json'`) for BOTH plan-approved.json and feature_list.json, and reconcile the inventory's `/plan-approved.json` notation (design.md:139) with the resolved path used in code.

**52. Properties 25/26/27 cite the wrong Validates requirement NUMBER (PRD codes are right)**  ·  _[db/migrations/ (all 8 tables 001-008)]_
- **anchor:** Property 25 line 1080 'Validates: Requirements 26 (REQ-COV-007)'; Property 26 line 1090 'Validates: Requirements 27 (REQ-STATE-005)'; Property 27 line 1100 'Validates: Requirements 25 (REQ-SPEC-016)'
- **change:** Correct the requirement numbers to match the PRD codes and the cross-reference table at requirements.md:507-509: Property 25 → 'Validates: Requirements 22 (REQ-COV-007)'; Property 26 → 'Validates: Requirements 23 (REQ-STATE-005)'; Property 27 → 'Validates: Requirements 24 (REQ-SPEC-016)'. As written they point at Req 26 (Reasoning-Loop), Req 27 (Audit Log), and Req 25 (Performance/Accessibility) respectively — a self-contradiction against requirements.md and the harness CHECK-10/11/12 PRD tags.

**53. design.md Property 25 cites wrong requirement number AND non-canonical CHECK token**  ·  _[db/migrations/007_requirement_versions.sql]_
- **anchor:** **Validates: Requirements 26 (REQ-COV-007). Machine-checked: CHECK-10 (`formal_verification_merged.py`).**
- **change:** Rewrite the Property 25 Validates line (design.md:1080) to: '**Validates: Requirement 22 (REQ-COV-007). Machine-checked: CHECK-10a/10b (`formal_verification_merged.py`).**' — REQ-COV-007 is Requirement 22 (requirements.md:409,432,507), NOT Requirement 26 (which is REQ-OBS-006 Reasoning-Loop Detection, requirements.md:413). The canonical Z3 identifier is the CHECK-10a/10b pair (formal_verification_merged.py:422,430; requirements.md:507), not the bare 'CHECK-10'.

**54. Amendment status-reset (proven→unproven) is blocked by the PreToolUse status guard's permitted-edge set**  ·  _[db/migrations/007_requirement_versions.sql]_
- **anchor:** | `PreToolUse` — status guard | Any write to `feature_list.json` | Validates proposed status transition; checks evidence schema completeness *(Reconciliation 2026-06-15: `unproven→failed` and `failed→unproven` are now allowed; only `*→proven` requires a complete Evidence_Record)* | Transitions into `proven` without a complete Evidence_Record; any transition not in {`unproven→proven`, `unproven→failed`, `failed→unproven`} | REQ-COV-002, REQ-COV-003, REQ-COV-006 |
- **change:** The amendment flow resets `coverage_items.status` from `proven` to `unproven` (design.md:1078,1152; Property 25), but `proven → unproven` is NOT in the permitted-edge set {unproven→proven, unproven→failed, failed→unproven} listed at design.md:215, design.md:374, design.md:900, requirements.md:123, and requirements.md:129 — so the status guard would BLOCK the amendment reset. Add `proven → unproven` (human-authored / amendment-triggered) to the permitted-transition set in all five locations, scoped so it is allowed only on a requirement-amendment event (e.g. gated on a corresponding requirement_versions insert), and update Property 3 accordingly.
- _adds EARS/use-case wiring_

**55. Define first-run / no-baseline behavior so the guard never blocks a fresh run**  ·  _[state_integrity.py]_
- **anchor:** run_state DDL line design.md:492: "resume_integrity_ok BOOLEAN, -- ... PreToolUse integrity guard blocks the first write when FALSE (REQ-STATE-005)" (plain BOOLEAN, no SQL DEFAULT) and PreToolUse integrity-guard row design.md:212
- **change:** resume_integrity_ok is a plain BOOLEAN with no default; on a fresh (non-resumed) session there is no recorded baseline, so the comparison is undefined and a NULL/FALSE flag would make the PreToolUse guard block the first legitimate write. State explicitly that on a session with no prior durable state_hash, state_integrity.py sets resume_integrity_ok=TRUE (and/or the PreToolUse guard at design.md:212 treats NULL/absent-baseline as ALLOW), so only an actual recorded-vs-recomputed mismatch blocks. Mirror this with a unit/property case in task 39.16 asserting a first-run session is never blocked by the integrity guard.

**56. Fix Property 28 'Validates' requirement number (28 -> 27)**  ·  _[audit_verify.py]_
- **anchor:** **Validates: Requirements 28 (REQ-AUDIT-001/002). Runtime mechanism: `audit_verify.py` chain verifier (no Z3 check — runtime/CI verified).**
- **change:** Change 'Validates: Requirements 28 (REQ-AUDIT-001/002)' to 'Validates: Requirement 27 (REQ-AUDIT-001/002)'. The tamper-evident audit log is Requirement 27 (requirements.md:455); Requirement 28 is the Capability-Role Taxonomy Extension (REQ-ORCH-004). Property 28's design 'Validates' pointer currently names the wrong requirement number.

**57. Add a subtype discriminator field to CoverageItem so REQ-VERIFY-007/008 triggers are evaluable**  ·  _[perf_a11y_verifier (capability in verifier.md)]_
- **anchor:** "type": {
          "type": "string",
          "enum": ["functional", "NFR", "WIRING"],
- **change:** REQ-VERIFY-007 (requirements.md:447-448) gates 'WHEN an NFR item of subtype performance/accessibility', but the CoverageItem schema (design.md:322-385) has no subtype field — `type` is only {functional,NFR,WIRING} and `ears_pattern` is the five EARS patterns, so there is no representable discriminator on which the Verifier can dispatch perf vs a11y vs ui-screen-render. Add an optional `subtype` enum field to CoverageItem, e.g. enum ["performance","accessibility","ui-screen"] (valid only when type==NFR/WIRING), AND mirror it as a `subtype TEXT` column on the coverage_items Postgres table (design.md:443-451). Note the ui-screen value covers the REQ-VERIFY-008 render branch (per critic findings: subtype alone solves perf-vs-a11y but the render branch also needs a discriminator). The Verifier dispatches the fifth layer on this field. Without it the WHEN/Ubiquitous conditions are unrealizable.
- _adds EARS/use-case wiring_

**58. Fix REQ-ID mis-citation on secrets-scan.yml inventory row (17.5 -> 17.2)**  ·  _[secrets-scan.yml]_
- **anchor:** Line 147: | `secrets-scan.yml` | `.github/workflows/secrets-scan.yml` | 1 | CI secrets-scanning workflow using **gitleaks** [K17]; fails the build on a detected secret (REQ-17.5). |
- **change:** Change the trailing criterion reference from `(REQ-17.5)` to `(REQ-17.2 / REQ-SEC-002)`. REQ-17.5 (requirements.md:314) is the PreToolUse runtime prohibition on secrets in prompts/spans/URLs (enforced by PreToolUse_Hook, validated by Property 20); the CI diff-scan obligation is REQ-17.2 (requirements.md:311: 'IF a secret ... is detected in a diff, THEN ... block the commit or merge'), which is what task 40.2 (tasks.md:698) and task 45 (tasks.md:957) already cite. Append a parenthetical clarifying that REQ-17.5 is the distinct PreToolUse commit-side/pre-emission prevention gate covered by Property 20, so the merge-side (17.2/secrets-scan) and commit-side (17.5/PreToolUse) halves of 'block the commit or merge' are explicitly split.

**59. Resolve or strip undefined [K17]/[K9]/[K12] citation markers**  ·  _[secrets-scan.yml]_
- **anchor:** Line 147 [K17] ('gitleaks [K17]'); line 213 [K9] ('domain_baseline_checklists [K9]'); line 265 [K12] ('coverage.py) [K12]')
- **change:** The bracketed K-tags [K17], [K9], [K12] have no resolver: there is no K-citation legend/table anywhere in the spec set or docs (verified: only these 3 occurrences exist, all in design.md). Either add a 'K-citations' reference table to design.md defining K9/K12/K17 (and any others) with their Kiro/source provenance, or strip the three [K9]/[K12]/[K17] markers as noise. Apply one resolution consistently to all three.

**60. flagd unreachable behavior (fail-open vs fail-closed) unspecified — conflicts with fail-closed invariant**  ·  _[flagd]_
- **anchor:** New 'Agent Kill-Switch (REQ-CTRL-001)' subsection (and Requirement 32 in requirements.md:492-496)
- **change:** Specify the unreachable-flagd behavior explicitly as FAIL-CLOSED: when the self-hosted flagd server / flag source is unavailable, the gated agent capability defaults to DISABLED (not enabled). Document this in the new design subsection and reconcile it with REQ-GATE-005 (requirements.md:207: 'ambiguous states SHALL resolve to blocked, not passed'). Add a corresponding acceptance criterion to Requirement 32 (e.g. 32.4: 'IF the flagd flag source is unreachable, THEN the affected agent capability SHALL default to disabled (fail-closed), consistent with REQ-GATE-005.').

### 🟠 INCONSISTENCY (33)

**61. Resolve status/in_scope required-vs-default contradiction in CoverageItem schema**  ·  _[feature_list.json]_
- **anchor:** CoverageItem.required at design.md:324; status default 'unproven' at 373; in_scope default true at 378
- **change:** A JSON-Schema `default` never populates a value for a `required` property, so 'status defaults to unproven' (Req 5.1/Property 1) and 'in_scope DEFAULT true' (Req 5.7) are not realized by the file schema even though they ARE realized by the Postgres DEFAULTs (design.md:447-448), making a hand/tool-written file that omits status schema-INVALID while the equivalent DB row is valid-and-defaulted. Pick one: (a) drop `status` and `in_scope` from CoverageItem.required and let the schema `default` annotate, OR (b) keep them required, remove the misleading `default` keywords, and reword Req 5.1/Property 1 to 'every item is WRITTEN with status=unproven by the Initializer' and Req 5.7 likewise. Note the DB-vs-file default divergence so the conservative-merge rule (tasks.md:730) stays consistent.

**62. Reconcile feature_list_sha raw-byte binding with the expected in-place status-flip mutation**  ·  _[feature_list.json]_
- **anchor:** plan-approval gate compute_sha256(read_bytes()) design.md:791-792; 'status flip is the expected mutation' design.md:288
- **change:** The plan-approval gate binds approval to sha256 over the literal file BYTES (design.md:791-792), but the design EXPECTS in-place status flips unproven→proven (design.md:288) which change those bytes — so after the first legitimate status flip the SHA no longer matches and every subsequent implementation Write is re-blocked, forcing a fresh human approval; whitespace/key-reorder changes also re-block. Specify a canonical comparison: compute feature_list_sha over a canonicalized projection of the item set (e.g. sorted (id,type,priority,dependencies,acceptance_criteria,in_scope) excluding the mutable status/evidence fields), so status flips do NOT invalidate approval while id/type/AC/scope changes do. Update Property 6 and design.md:789-801 accordingly.

**63. Amend Property 5 and design rows 209/624 to include the HANDOFF-allow carve-out**  ·  _[stop_hook.py]_
- **anchor:** Property 5 (design.md:914-916 'Termination is permitted only when zero items are `unproven`'); Hook-Wiring row design.md:209 'Termination while any in-scope item is `unproven` or `violation_count > 0`'; Phase-0 test row design.md:624 'With one unproven item → blocks'
- **change:** Property 5, the Stop hook-wiring 'What it blocks' cell (209), and the Phase-0 test-to-pass row (624) all state the pure 'unproven blocks' rule WITHOUT the REQ-LOOP-005 HANDOFF override. At HANDOFF (cap/budget/no-progress) in-scope items are EXPECTED to remain unproven yet the hook MUST allow (exit 0); a naive implementer following Property 5/row-624 alone reproduces the infinite-block defect. Amend all three to read: 'blocks termination while any in-scope item is unproven AND no HANDOFF trigger (cap/budget/no-progress) is active.' tasks.md:948 already extends this caveat to Properties 8/9 — extend the same caveat to Property 5 and update the property-test spec at tasks.md:104-108 (5.2) which currently asserts unconditional exit-2 on any unproven mix.

**64. Mark the Stop-hook audit_log.append call as a Phase-2 addition (guarded/no-op until task 52.3)**  ·  _[stop_hook.py]_
- **anchor:** Hook-Wiring table Stop row, design.md:209 'calls `audit_log.append(...)` on the allow/block decision'
- **change:** design.md:209 and Req 27.4 present `audit_log.append(...)` as always-on in the Phase-0 Stop hook, but `audit_log.py` (the producer) is Phase 2 and its wiring into stop_hook.py is task 52.3 (Phase 2). A Phase-0 hook cannot call a Phase-2 producer. Annotate the Stop row (and the SubagentStop/PreToolUse audit rows at 217/218/210) to say the audit_log.append call is a Phase-2 addition (no-op/absent until task 52.3 wires it), matching the staging already implied by tasks.md:970. Keeps the Phase-0 spine self-consistent.

**65. Fix Property 26 dangling cross-reference 'Validates: Requirements 27'**  ·  _[pre_tool_use_hook.py]_
- **anchor:** Property 26, 'Validates: Requirements 27 (REQ-STATE-005)' (design.md:1090)
- **change:** Change 'Validates: Requirements 27 (REQ-STATE-005)' to 'Validates: Requirements 23 (REQ-STATE-005)'. Requirement 27 is the Tamper-Evident Gate-Decision Audit Log; REQ-STATE-005 is Requirement 23 (Resumed-State Integrity), which is the requirement Property 26 / the PreToolUse integrity guard actually validates. The parenthetical REQ-ID is correct; the bare requirement number is wrong.

**66. Annotate SessionStart hook-wiring row + inventory that integrity compute is a Phase-2 augmentation**  ·  _[session_start_hook.py]_
- **anchor:** Hook Wiring Table SessionStart row (design.md:219) and Component Inventory rows for session_start_hook.py (design.md:129, Phase 0) and state_integrity.py (design.md:143, Phase 2)
- **change:** The SessionStart hook-wiring row presents the hash-compute as part of SessionStart from day one, but state_integrity.py is Phase 2 while session_start_hook.py is Phase 0. A reader building the Phase-0 spine would believe resume_integrity_ok is written from the start. Annotate the row's 'What it does' cell: 'load (git/progress/coverage): Phase 0; integrity compute + resume_integrity_ok write: Phase 2 via state_integrity.py'. Mirror the note in the session_start_hook.py inventory row (design.md:129).

**67. Fix omission_guard phase label (Phase 0 -> phase of task 54)**  ·  _[subagent_stop_hook.py]_
- **anchor:** Component Inventory row `| `omission_guard` | (capability within `.claude/hooks/subagent_stop_hook.py`) | 0 | Omission-declaration gate ... |` (design.md:148); subagent_stop_hook.py inventory row labelled '0/1' (design.md:130)
- **change:** Re-label the `omission_guard` Phase from `0` to the phase of task 54, which is scheduled in dependency-graph wave 45 (tasks.md:923, :974) — effectively post-Phase-4, NOT Phase 0. Split/annotate the subagent_stop_hook.py inventory row (design.md:130, currently '0/1') so the Phase-0 deliverable is strictly the base evidence-schema validator (task 8), while the omission_guard (task 54), the audit_log.append wiring (task 52.3, Phase 2/3), and the OTel hook.decision span (task 36.1, Phase 3) are flagged as LATER additions. The hook is genuinely a 0/2/3 multi-phase component and the single '0/1' label understates this.

**68. PostToolUse intentionally NOT an audit-log producer — state it explicitly to reconcile with OTel forwarding**  ·  _[post_tool_use_hook.py]_
- **anchor:** Component Inventory, post_tool_use_hook.py row (design.md:131); cross-ref audit producer list at design.md:144 (audit_log.py) and req 27.4 (requirements.md:460)
- **change:** Req 27.4 (requirements.md:460) and task 52.3 (tasks.md:970) wire audit_log.append into stop_hook.py, pre_tool_use_hook.py, and subagent_stop_hook.py only — PostToolUse is deliberately excluded — yet task 36.1 forwards PostToolUse events to OTel, so the audit log and the OTel trace appear to disagree on whether PostToolUse events are recorded. State explicitly (in the design.md:131 inventory row or the audit_log.py row at design.md:144) that PostToolUse is intentionally NOT a gate-audit-log producer because it issues no allow/block gate decision, and that its OTel forwarding is informational feedback telemetry (paired with the 'hook.feedback' span from the OBS-003 fix), not a gate-decision audit entry.

**69. Resolve PreCompact phase contradiction (Phase 1 vs Phase 2)**  ·  _[pre_compact_hook.py]_
- **anchor:** design.md:132 row `| `pre_compact_hook.py` | `.claude/hooks/pre_compact_hook.py` | 2 |` AND design.md:15 `| **Phase 2 (durable state)** | Postgres traceability graph + PreCompact checkpoints + SLSA attestations |`
- **change:** Pick ONE phase for pre_compact_hook.py. The build task (task 25) sits under '### Phase 1 — Verification Depth' (tasks.md:306, tasks 18-26), Notes line 725 bounds 'Phase 1 task (18-26)', and the wave graph schedules 25.1 in wave 17 (tasks.md:810-812) between Phase-1 wave 16 (21.2-24.1) and Phase-2 wave 19 (27.1). Since PreCompact depends only on file+git artifacts (no Postgres), make it Phase 1: change design.md:132 '2'->'1' and remove 'PreCompact checkpoints' from the Phase-2 overview row (design.md:15). Alternatively move task 25 into Phase 2 (after task 26) to match design.md — but the wave graph + Notes already commit it to Phase 1, so renaming design.md is the lower-churn fix.

**70. Reconcile spec 7-pass cap vs 25-turn slice cap — Stop hook keys HANDOFF on the wrong counter for the lex-specialis tiebreak**  ·  _[spec_validator.py]_
- **anchor:** design.md:699 'if run_state.iteration_count >= MAX_TURNS_PER_SLICE' vs design.md:842 / requirements.md:110 keying on 'iteration_count < SPEC_COMPLETION_HARD_CAP'
- **change:** Introduce a distinct run_state field spec_pass_count (separate from iteration_count which counts the 25-turn MAX_TURNS_PER_SLICE) and add a Stop-hook (or Initializer-loop) HANDOFF branch firing when spec_pass_count >= SPEC_COMPLETION_HARD_CAP (7), independent of the MAX_TURNS_PER_SLICE check at design.md:699. Fix design.md:842 and requirements.md:110 (Req 4.6) which currently say the exit-2 block applies while 'iteration_count < SPEC_COMPLETION_HARD_CAP' — iteration_count is the 25-turn counter (design.md:487), so as written the 7-pass lex-specialis tiebreak is not realizable. Name the same counter in the tiebreak that the cap-HANDOFF branch checks.

**71. Specify variable-construction mechanism for the Independence check inside validate_spec(), or scope it out of the per-spec checks**  ·  _[spec_validator.py]_
- **anchor:** design.md:561 Independence: 'confirms gate decisions are independent of prediction variables by asserting predA != predB AND gateA = gateB is SAT'
- **change:** Independence is listed as one of the four per-spec checks run by validate_spec (design.md:561), but a plain requirements: list[dict] contains no gate/prediction variables, and design.md:561 describes it only generically with no mechanism to construct gateA/gateB/predA/predB from an arbitrary EARS spec. The harness CHECK-5 (formal_verification_merged.py:255-261) tests this against fixed completion-gate variables, NOT against validate_spec output, and Property 4 is tested via test_completion_gate.py (tasks.md:658-662), not test_spec_validator.py. Either specify how validate_spec derives gate/prediction variables from a requirements list, or move Independence out of the four per-spec validate_spec checks and document it as the static design-time harness check it actually is (design.md:561 / CHECK-5), reconciling design.md:554-563 with the harness scope.

**72. Resolve the feature_list.json-vs-evidence_records input-model contradiction for the Rego policy**  ·  _[coverage_query.rego]_
- **anchor:** ### Property 22: OPA Zero-Evidence Policy at Merge — '*For any* merge attempt, the OPA/Conftest policy shall deny the merge if the query `SELECT COUNT(*) FROM evidence_records WHERE requirement_id = ANY(approved_req_ids)` returns fewer rows...' (design.md:1052)
- **change:** Pick the feature_list.json Conftest-document model as canonical (it matches the actual `conftest test feature_list.json` invocation at tasks.md:264 and all integration tests at tasks.md:407-408). REWRITE Property 22's statement to express the rule over `input.items` (Conftest document input) rather than a SQL `SELECT ... FROM evidence_records`: e.g. 'the policy shall deny the merge if any in_scope item in input.items is not status=="proven", or any proven item lacks a complete four-field Evidence_Record.' Also rewrite tasks.md:354 first bullet ('deny merge if any requirement ID in the approved set has zero rows in `evidence_records`') to the same document model, and add one sentence to the design.md:137 Component-Inventory row (or a new subsection) stating the single canonical input is the feature_list.json document evaluated by Conftest, not a live Postgres table.

**73. Make provenance required in schema to match NOT NULL Postgres column and Req 2.3**  ·  _[feature_list.schema.json (schema/)]_
- **anchor:** design.md:324 (CoverageItem.required omits provenance) and design.md:365-369 (provenance enum) vs design.md:438 (requirements.provenance TEXT NOT NULL CHECK)
- **change:** Add `provenance` to CoverageItem.required (design.md:324) so the JSON schema matches the NOT NULL Postgres column (design.md:438) and Req 2.3 (requirements.md:79: 'SHALL mark inferred-vs-stated provenance on each requirement'). Today a schema-valid item can omit provenance entirely while the DB rejects it, so the file and DB disagree on whether provenance is mandatory. (Note: governing requirement is Req 2.3, not Req 4.3.) Alternatively document explicitly that provenance lives only in Postgres and is intentionally optional in the JSON file.

**74. Correct Property 1 'Validates' citation (drop dangling 5.4)**  ·  _[feature_list.schema.json (schema/)]_
- **anchor:** design.md:886 (Property 1 'Validates: Requirements 5.1, 5.4')
- **change:** Change Property 1's 'Validates' line to Requirements 5.1 (and add 5.7 for the in_scope default if desired) and drop 5.4. Property 1's body (non-empty id, type enum, ≥1 acceptance criterion, status default unproven) maps to Req 5.1; Req 5.4 is the WIRING/NFR-first-class requirement, which Property 1 does not assert, so the 5.4 citation is dangling. Also align task 2.2 (tasks.md:33) and task 2.1 (tasks.md:29), which disagree (2.2 says 5.1,5.4; 2.1 says 5.1,5.3,5.6).

**75. evidence_complete CHECK does not enforce the sha256 output_hash format the JSON schema enforces**  ·  _[db/migrations/ (all 8 tables 001-008)]_
- **anchor:** evidence_complete CONSTRAINT at design.md:474-477 (`output_hash <> ''`) vs the feature_list JSON schema output_hash pattern `^sha256:[a-f0-9]{64}$` at design.md:402
- **change:** Either add a CHECK that `output_hash ~ '^sha256:[a-f0-9]{64}$'` to the evidence_complete constraint (and to task 28.4) so the DB matches the JSON-schema format, OR add an explicit note that output_hash format enforcement is delegated to the JSON-schema / evidence_collector layer. As-is, a malformed hash passes the DB CHECK (non-empty only) but fails JSON-schema validation — a DB/JSON enforcement asymmetry.

**76. Postgres store cannot reconstruct acceptance_criteria / dependencies / priority — REQ-16.3 fidelity gap**  ·  _[db/migrations/ (all 8 tables 001-008)]_
- **anchor:** requirements DDL design.md:431-440 (has priority, lacks acceptance_criteria & dependencies) and coverage_items DDL design.md:443-451 (lacks priority & dependencies) vs feature_list JSON schema required set `[id, type, priority, dependencies, acceptance_criteria, status, in_scope]` at design.md:324; REQ-16.3 at requirements.md:298
- **change:** Resolve the source-of-truth fidelity gap one of two ways: (a) add `acceptance_criteria` and `dependencies` storage (TEXT[]/JSONB columns or a child table) to requirements, and add `priority`+`dependencies` to coverage_items, so the DB can reconstruct Property 1 (≥1 acceptance criterion) and Property 7 prior-slice ordering (priority/dependencies, design.md:336-358, 932); OR (b) explicitly scope REQ-16.3 (requirements.md:298) so feature_list.json is canonical for acceptance criteria/dependencies/priority and Postgres mirrors only the coverage-status subset. Pick one and make design + tasks + requirements consistent. This single root cause covers both the missing acceptance_criteria and the missing slice-ordering dependencies/priority.

**77. Property 22 prose claims a Postgres COUNT but the built Conftest policy reads feature_list.json**  ·  _[db/migrations/ (all 8 tables 001-008)]_
- **anchor:** Property 22 EARS prose at design.md:1052 (`SELECT COUNT(*) FROM evidence_records WHERE requirement_id = ANY(approved_req_ids)`) vs task 21.1 (tasks.md:355 'Accept feature_list.json as Conftest input; policy reads items[_].status') and Phase-0 stub task 14.1 (tasks.md:264-265)
- **change:** Reconcile Property 22's store: either (a) rewrite the Property 22 prose so the gate is expressed over the feature_list.json embedded evidence object that Conftest actually reads (since no task wires Conftest to Postgres), keeping the evidence_records SQL as an optional/secondary check; OR (b) add a task that exports/queries evidence_records into the Conftest input. As written, design asserts a SQL COUNT against evidence_records that the realized merge gate never performs — feature_list.json is the store the gate queries.

**78. Property 22 counts approved requirements without excluding out-of-scope (Req 5.7) and evidence_records has no in_scope linkage**  ·  _[db/migrations/ (all 8 tables 001-008)]_
- **anchor:** Property 22 prose design.md:1050-1052 (counts over approved_req_ids, no in_scope filter) vs Req 5.7 'all completion and Stop gates SHALL count ONLY in_scope items' (requirements.md:128); coverage_items.in_scope at design.md:448
- **change:** Make Property 22 honor Req 5.7: restrict the approved-requirement set (and the evidence count) to in_scope coverage items by joining to coverage_items.in_scope (or pre-filtering approved_req_ids to in_scope ones). evidence_records itself has no in_scope column, so the query must reach in_scope via coverage_items. Without this, an out-of-scope-but-approved requirement with zero evidence either wrongly fails the merge or the gate silently ignores the in_scope rule.

**79. Error Handling 'Postgres unavailable' row is thinner than the N-25 fallback contract it should mirror**  ·  _[db/migrations/ (all 8 tables 001-008)]_
- **anchor:** Error Handling table row 'Postgres unavailable | Fall back to feature_list.json as sole source of truth for gate decisions. Log warning to OTel.' (design.md:815)
- **change:** Expand the design.md:815 Error Handling row (or cross-reference tasks.md:730 N-25) so the design states the full fallback contract that already lives in tasks: every gate must reach the SAME allow/block decision on file-backed state (never fail-open), DB and file are kept as duplicates, the more conservative (blocking) result wins on divergence, and the gate_audit_log producer appends to a file-backed log when the DB is down and reconciles on reconnect. The requirement-level contract is captured in tasks.md:730 but the design Error Handling row reduces it to a one-liner.
- _adds EARS/use-case wiring_

**80. Correct shuffled requirement numbers in design.md Property 25–28 'Validates' lines**  ·  _[db/migrations/008_gate_audit_log.sql]_
- **anchor:** design.md:1110 `**Validates: Requirements 28 (REQ-AUDIT-001/002). ...**` (and design.md:1080 Property 25, :1090 Property 26, :1100 Property 27)
- **change:** The Property 25–28 'Validates' lines carry shuffled bare requirement numbers that contradict requirements.md headings and the requirements.md:507-510 matrix: design.md:1080 Property 25 says 'Requirements 26' but amendment/REQ-COV-007 = Requirement 22 (26 = Reasoning-Loop Detection); design.md:1090 Property 26 says 'Requirements 27' but resumed-state/REQ-STATE-005 = Requirement 23 (27 = the audit log); design.md:1100 Property 27 says 'Requirements 25' but checklist/REQ-SPEC-016 = Requirement 24 (25 = Perf/A11y); design.md:1110 Property 28 says 'Requirements 28' but audit/REQ-AUDIT = Requirement 27 (28 = Capability-Role Taxonomy). The parenthetical REQ-* tags are correct; only the numbers are wrong. Fix to: Property 25 -> Requirement 22, Property 26 -> Requirement 23, Property 27 -> Requirement 24.1, Property 28 -> Requirement 27 — matching requirements.md:431/435/440/455 and the matrix at requirements.md:507-510. (Property 28 line is the one directly on this component.)

**81. Fix systematic Property→Requirement mis-numbering (Properties 25-28)**  ·  _[state_integrity.py]_
- **anchor:** Property 'Validates' lines: design.md:1080 'Validates: Requirements 26 (REQ-COV-007)'; design.md:1090 'Validates: Requirements 27 (REQ-STATE-005)'; design.md:1100 'Validates: Requirements 25 (REQ-SPEC-016)'; design.md:1110 'Validates: Requirements 28 (REQ-AUDIT-001/002)'
- **change:** Off-by-N requirement numbers. Per the authoritative cross-reference table (requirements.md:505-512): Property 25 validates Requirement 22 (not 26); Property 26 validates Requirement 23 (not 27); Property 27 validates Requirement 24.1 (not 25); Property 28 validates Requirement 27 (not 28). Fix all four 'Validates' lines. (Property 29→24.2 at design.md:1120 and Property 30→29 at design.md:1130 are already correct — do not touch.) This is broader than the single Property-26 collision the draft flagged.

**82. Stop overstating CHECK-11 as machine-checking state_integrity.py**  ·  _[state_integrity.py]_
- **anchor:** Component-inventory row design.md:143: "`state_integrity.py` ... (REQ-STATE-005 / CHECK-11)." and the addendum bullet design.md:1180: "recompute resumed-state hash on `SessionStart`; block proceed on mismatch (REQ-STATE-005 / CHECK-11)."
- **change:** CHECK-11a/11b (formal_verification_merged.py:444-461) is a 2-Boolean abstraction over `resumeHashMismatch`/`runProceeds` proving only Implies(mismatch, ¬proceeds); it never references state_integrity.py, resume_integrity_ok, the hash inputs, or the SessionStart-computes/PreToolUse-enforces split. Reword design.md:143 and design.md:1180 to 'logic-validated by CHECK-11a/11b; implementation/wiring validated by Property 26 PBT (tasks.md task 39.16)', mirroring the honest Property 28 caption 'no Z3 check — runtime/CI verified' (design.md:1110).

**83. Scope design.md:143 row to state_integrity.py only (non-blocking; flag-based block)**  ·  _[state_integrity.py]_
- **anchor:** Component-inventory row design.md:143: "Recomputes the resumed-state hash on `SessionStart` and writes `run_state.resume_integrity_ok`; the PreToolUse integrity guard blocks on mismatch (REQ-STATE-005 / CHECK-11)."
- **change:** The row conflates two components and implies state_integrity.py blocks 'on mismatch'. It does not: per design.md:212 the PreToolUse guard blocks when run_state.resume_integrity_ok == false (a flag), not on a live recompare. Reword to scope the row to state_integrity.py only ('computes the hash and writes the flag; non-blocking') and let the existing PreToolUse-integrity-guard row (design.md:212) own the block, stated as 'blocks when resume_integrity_ok == false' rather than 'on mismatch'.

**84. Resolve design-internal contradiction: spine row 4 vs hook-wiring row on SessionStart duties**  ·  _[state_integrity.py]_
- **anchor:** Phase 0 Component List spine row 4 (design.md:626): "SessionStart hook | ... | Loads git status + progress file + coverage model | ..." — no integrity computation; contradicts hook-wiring row design.md:219 and Property 26 design.md:1088 which say SessionStart computes the resumed-state hash
- **change:** design.md is internally inconsistent about the SessionStart hook's responsibilities: spine row 4 (design.md:626) lists only 'Loads git status + progress file + coverage model' (matching task 7.1) while design.md:219 and Property 26 say it also computes the resumed-state hash and writes resume_integrity_ok. Update spine row 4 to add 'computes the resumed-state hash via state_integrity.py and writes run_state.resume_integrity_ok (non-blocking)' so the spine row agrees with design.md:219.

**85. Pin audit_verify.py to a single phase in New/changed-components bullet**  ·  _[audit_verify.py]_
- **anchor:** - `audit_log.py` + `audit_verify.py` (Phase 2/3) — append hash-chained gate-decision entries; verify the chain (REQ-AUDIT-001..003).
- **change:** Change the bullet at design.md:1181 from 'audit_log.py + audit_verify.py (Phase 2/3)' to 'audit_log.py (Phase 2) + audit_verify.py (Phase 3)' to match the Component Inventory which lists audit_log.py at Phase 2 (design.md:144) and audit_verify.py at Phase 3 (design.md:145). The ambiguous '(Phase 2/3)' wording contradicts the single-phase inventory assignments.

**86. Resolve the self-contradictory Phase 1/3 tag and place task 51 in a phase**  ·  _[perf_a11y_verifier (capability in verifier.md)]_
- **anchor:** | `perf_a11y_verifier` | (capability within `.claude/agents/verifier.md`) | 1/3 |
- **change:** The Component Inventory tags perf_a11y_verifier 'Phase 1/3' (design.md:146 and the addendum line 1182), but its host Verifier is a Phase-0 spine component (design.md:154, Phase-0 list line 629), and its only build task (51) carries no phase header and sits in wave 41 of the dependency graph. A reader cannot tell which phase builds it. Pick one phase: change the inventory Phase cell from '1/3' to '1' (and the addendum line 1182 'Phase 1/3' to 'Phase 1'), and add task 51 to the Phase-1 section of tasks.md with an explicit dependency on task 10.3 (the verifier.md author).

**87. Make scanner choice consistent between design.md and tasks.md**  ·  _[secrets-scan.yml]_
- **anchor:** design.md:147 ('using **gitleaks**') vs tasks.md:696 / tasks.md:957 ('gitleaks (or trufflehog)')
- **change:** design.md fixes the scanner as gitleaks while tasks.md offers 'gitleaks (or trufflehog)' with no selection criterion. Pick one default (gitleaks, per design.md) and demote trufflehog to an explicit 'acceptable alternative' note in a single place, or state the selection criterion. Make design.md:147 and tasks.md (696, 957) say the same thing about which scanner is the default.

**88. Audit-call inconsistency: omission-guard wiring row calls audit_log.append only 'on the block decision', not on allow (violates REQ-AUDIT-001 / task 52.3 'every allow/block')**  ·  _[omission_guard (capability in subagent_stop_hook.py)]_
- **anchor:** Hook Wiring Table row `| SubagentStop — omission-declaration guard | ... | Checks omission_declaration field in the subagent output; calls audit_log.append(...) on the block decision ... |` (design.md:218)
- **change:** Reword 'calls audit_log.append(...) on the block decision' to 'calls audit_log.append(...) on the allow/block decision' to match REQ-AUDIT-001 (requirements.md:457, 'every gate decision'), task 52.3 ('on every allow/block decision', tasks.md:970), and the sibling SubagentStop Evidence row (design.md:217, which already reads 'on the decision'). As written the allow path of the omission guard skips the audit append.

**89. Name the evaluator (judge) LLM and reconcile the LLM-as-judge gate with the governing 'predictions never gate' invariant**  ·  _[deepeval_gate.py]_
- **anchor:** | `deepeval_gate.py` | `tools/deepeval_gate.py` | 3 | DeepEval pytest-native eval step; calls `assert_test()` on configured metric thresholds (faithfulness ≥ 0.8, answer relevancy ≥ 0.7); runs as REQUIRED CI status check `deepeval-gate` at merge (REQ-EVAL-001). *(P3 market-research 2026-06-15.)* |
- **change:** Add a design subsection (or expand the component row) for deepeval_gate.py that: (a) names the evaluator model + provider and pins it with a fixed temperature/seed for reproducibility; and (b) explicitly reconciles this LLM-as-judge gate with the governing invariant at design.md:7 / requirements.md:7 ('Model self-assessment and probabilistic predictions only inform; they never gate'). DeepEval faithfulness/answer-relevancy are LLM-judge metrics, so a REQUIRED merge gate computing them directly contradicts the top-level invariant unless reclassified (e.g., advisory-quality vs the deterministic gates, or scoped to non-self-assessment of agent output) — currently the contradiction is unresolved and no judge model is named.

**90. Token/cost budget cap expressed in incompatible units (tokens vs USD) with no run_state column**  ·  _[implementer.md]_
- **anchor:**     token_cost_usd    NUMERIC(10,4) NOT NULL DEFAULT 0,
- **change:** requirements.md:357 sets the cost/token budget = 1,000,000 TOKENS per Slice, but run_state stores only `token_cost_usd NUMERIC(10,4)` (USD) and the Stop-hook budget check (design.md:704) compares against `token_budget_usd` — a column that does not exist in the run_state DDL (design.md:481-496). Pick one unit and close the loop: either add a `token_budget` (integer tokens) column and have the budget check compare tokens, or add a `token_budget_usd` column plus a documented token→USD model-price assumption converting the 1,000,000-token default. Reconcile requirements.md:357 with design.md:488,704 and add the chosen column to migration 005 (task 28.5).

**91. Resolve Verifier write-scope inconsistency: 'status field only' vs 'Evidence_Records to feature_list.json'**  ·  _[verifier.md]_
- **anchor:** design.md:267 'Read/write on `tests/`, `feature_list.json` status field only' vs design.md:629 (Phase-0 component #7 test) 'can write Evidence_Records to `feature_list.json`'
- **change:** design.md:267 limits the Verifier to writing the feature_list.json status field only, while the Phase-0 spine acceptance test at design.md:629 says it 'can write Evidence_Records to feature_list.json' (the status+evidence sub-object). tasks.md:214 echoes 'status field only'. Reconcile to one scope: state the Verifier writes the status field AND the attached evidence sub-object (since a status→proven flip requires writing the Evidence_Record per the schema at design.md:381-384). Align design.md:267, design.md:629, and tasks.md:214.

**92. Add REQ-SPEC-016/017/018 obligations to research.md key behaviors and task 10.4 traceability**  ·  _[research.md]_
- **anchor:** #### `.claude/agents/research.md` — Domain-Baseline Checklist Sourcer  →  **Key behaviors:** block (design.md:273-278)
- **change:** Extend the research.md **Key behaviors** list (design.md:273-278) with three obligations the agent that is the named subject of REQ-SPEC-016/017/018 currently omits: (a) [REQ-SPEC-017] every external claim in a draft checklist SHALL carry a source URL and an authority-tier label (primary/standard/peer-reviewed vs blog/vendor/social), and SHALL pass an independent fact-check pass before presenting for human review; (b) [REQ-SPEC-016] do NOT use a checklist for discovery until its `approved_at` is non-null (DRAFT is unusable) — already partially stated as 'does NOT use the checklist until approved', make the approved_at linkage explicit; (c) [REQ-SPEC-018] the research output SHALL include a non-null `omission_declaration` field enumerating, by EARS scenario category, every scenario class not covered, using `[Gap]` markers. Mirror this in tasks.md task 10.4: change its `_Requirements: 3.1, 3.2, 3.3_` line to `_Requirements: 3.1, 3.2, 3.3, 24.1 (REQ-SPEC-016), 24.2 (REQ-SPEC-017), 29.1/29.3 (REQ-SPEC-018)_` and add the corresponding behavior bullets (source-URL/authority-tier/fact-check + omission_declaration) to the task 10.4 body.
- _adds EARS/use-case wiring_

**93. Fix Property 27 requirement mis-citation (Requirement 25 → Requirement 24)**  ·  _[research.md]_
- **anchor:** **Validates: Requirements 25 (REQ-SPEC-016). Machine-checked: CHECK-12 (`formal_verification_merged.py`).** (design.md:1100)
- **change:** Change 'Validates: Requirements 25 (REQ-SPEC-016)' to 'Validates: Requirement 24.1 (REQ-SPEC-016)'. REQ-SPEC-016 maps to Requirement 24.1 (requirements.md:441-442 and the cross-ref table at requirements.md:509); Requirement 25 is Performance/Accessibility (requirements.md:445), an unrelated requirement. Property 29 at design.md:1120 already correctly cites 'Requirements 24 (REQ-SPEC-017)', so only line 1100 is wrong.

### 🟡 MISSING-WIRING (32)

**94. Add append-only ordering/length preservation check and property**  ·  _[feature_list.json]_
- **anchor:** 'Never deleted or reordered' (design.md:126, 288); Property 3 Identity-Mutation Blockade (design.md:898-902)
- **change:** Property 3 blocks mutation of id/type/acceptance_criteria of an existing item but does NOT catch a pure reorder or a deletion that leaves surviving items' id/type/AC intact. Extend Property 3 (and the PreToolUse status/artifact guard at design.md:214-215) to diff the incoming items[] id-sequence and length against the prior committed version of feature_list.json and block on any deletion, truncation, or reorder. Add the corresponding assertion to tasks.md task 6.1 (check_artifact_guard / a new check_append_only) and its property test (tasks.md 6.4 / Property 3).
- _adds EARS/use-case wiring_

**95. Extend amendment re-entry to flip the feature_list.json item, not only Postgres coverage_items**  ·  _[feature_list.json]_
- **anchor:** 'On amendment: insert a version row AND reset coverage_items.status to unproven' (design.md:1152); REQ-COV-007 / Property 25
- **change:** REQ-COV-007/Property 25/CHECK-10 reset only Postgres coverage_items.status; on a Postgres-down amendment (the file is the Phase-0 source of truth and DB-down fallback, design.md:815) the feature_list.json item stays proven while the model is logically unproven. Extend Req 22 / the amendment design step / task 48 to ALSO flip the corresponding feature_list.json item to unproven and invalidate its plan-approval binding (re-block via feature_list_sha) on amendment, and add a Postgres-down amendment test under task 39.15.
- _adds EARS/use-case wiring_

**96. Add an NFR subtype field so the Verifier can route performance vs accessibility deterministically**  ·  _[feature_list.json]_
- **anchor:** CoverageItem schema (design.md:331-385), type enum at 331-335; Req 25.1/25.2 (requirements.md:447-448)
- **change:** Req 25.1/25.2 route an NFR item to k6/Lighthouse vs axe-core by subtype `performance`/`accessibility`, but CoverageItem has only `type ∈ {functional,NFR,WIRING}` and no machine-readable subtype, so perf_a11y_verifier cannot select its evidence tool from the coverage model. Add an optional `nfr_subtype` property (enum e.g. {performance, accessibility, reliability, security}) to CoverageItem, required-when type=='NFR' via an allOf, mirror it in the Postgres requirements table, and reference it from Req 25.1/25.2 and the verifier component (design.md:146).
- _adds EARS/use-case wiring_

**97. Tie WIRING-type items to an integration-test Evidence_Record at the schema/Property level**  ·  _[feature_list.json]_
- **anchor:** type description 'WIRING items require integration-test evidence' (design.md:334); EvidenceRecord schema (design.md:387-411); Req 8.3 (requirements.md:174)
- **change:** Req 8.3 demands an integration test exercising the real execution path as the Evidence_Record for a proven WIRING item, but EvidenceRecord is a uniform 4-field shape with no marker distinguishing an integration test from a unit test, and no schema rule/Property ties WIRING→integration evidence. Add an `evidence_kind`/`test_kind` enum field (e.g. {unit, integration, behavioral, perf, a11y}) to EvidenceRecord, add an allOf requiring evidence_kind=='integration' when the item type=='WIRING' transitions to proven, and add a Property (or extend Property 2) asserting it. Wire it to task 47.
- _adds EARS/use-case wiring_

**98. Wire with_reentrancy_guard into a stop_hook main() entrypoint**  ·  _[stop_hook.py]_
- **anchor:** `def with_reentrancy_guard(fn):` block at design.md:741-753 (immediately after the evaluate_stop pseudocode)
- **change:** with_reentrancy_guard(fn) is defined but no code composes it with evaluate_stop, so the guard is dead. Add a `main()` entrypoint showing `result = with_reentrancy_guard(lambda: evaluate_stop(event)); save_run_state(run_state); sys.exit(0 if result.decision=='allow' else 2)`. This wires Req 10.4 (REQ-GATE-004) into the component. Mirror in tasks.md:100/126 (task 5.1 names with_reentrancy_guard and task 5.5 names the reentrancy unit test, but neither shows the composition).
- _adds EARS/use-case wiring_

**99. Add a live PreToolUse secret-block gate (REQ-SEC-005 / 17.5) to the hook table and task 6**  ·  _[pre_tool_use_hook.py]_
- **anchor:** Hook Wiring Table, after the `PreToolUse` — status guard row (design.md:215); and tasks.md task 6.1 check list (tasks.md:131-136)
- **change:** Add a 7th PreToolUse sub-gate row to the hook-wiring table: `PreToolUse` — secret-block guard | Trigger: any tool call carrying a prompt/span-attribute/URL/Bash-arg | scans args+prompt against secret regexes (API keys, tokens, passwords, connection strings) | blocks (exit 2) any call that would carry a secret | REQ-SEC-005 (17.5), Property 20. Then add `check_secret_block(event)` to task 6.1's chained checks and cite 17.5 in task 6's `_Requirements:` line. This is the live prevention gate 17.5 mandates ('NOT only via an offline test'); Property 20 (offline test, task 39.11) and task 45 (CI gitleaks, 17.2) do not satisfy it.
- _adds EARS/use-case wiring_

**100. Author a per-subagent output schema so REQ-SPEC-018 (29.3) has a concrete mechanism**  ·  _[subagent_stop_hook.py]_
- **anchor:** Component Inventory rows for `omission_guard` (design.md:148) and `schema/` (design.md:138); plus tasks.md task 54 'add omission_declaration as a required non-nullable field to each subagent output schema (initializer, research, verifier)' (tasks.md:974)
- **change:** Add a `schema/subagent_output.schema.json` (or per-role schemas) to the Component Inventory and a task to author it, declaring `omission_declaration` as a required, non-nullable field per requirement 29.3 (requirements.md:478). Define the subagent-result ENVELOPE once in the Data Models section with documented top-level keys (`evidence[]`, `omission_declaration`, `actor_agent`, `requirement_id`) so BOTH the evidence-schema gate and the omission_guard read the same documented JSON path from the stdin payload. Reference this schema from subagent_stop_hook.py. This closes the gap that 29.3 mandates a schema field with no schema artifact to validate against, AND the field-location drift (no canonical statement of WHERE omission_declaration lives) between task 8.1 and task 54.
- _adds EARS/use-case wiring_

**101. Enforce Property 24 (actor_agent != implementer) at the SubagentStop acceptance gate**  ·  _[subagent_stop_hook.py]_
- **anchor:** Property 24 (design.md:1066-1070); evidence_records.actor_agent column with 'MUST be the Verifier, never the Implementer' (design.md:473); SubagentStop wiring row (design.md:217); PBT task 39.13 (tasks.md:652-656)
- **change:** Add an actor_agent runtime check to subagent_stop_hook.py: BLOCK (exit 2) any evidence-bearing subagent result whose `actor_agent` is `implementer.md`, enforcing Property 24 ('implementer cannot self-verify') at the acceptance choke point rather than only at the DB column (design.md:473) + property test (task 39.13). Document this in the SubagentStop wiring row (design.md:217) and add it to task 8.1 / the new design subsection. The SubagentStop gate is the natural runtime enforcement point and currently nothing tasks the hook with this check.
- _adds EARS/use-case wiring_

**102. Req 8.1 wiring-analysis EARS story not mapped to the PostToolUse trigger role**  ·  _[post_tool_use_hook.py]_
- **anchor:** Hook Wiring Table, PostToolUse row (design.md:216), 'Requirement enforced' column currently 'REQ-VERIFY-004, REQ-STEER-002'
- **change:** Add REQ-EXEC-010 (Req 8.1) to the PostToolUse hook-wiring row's enforced-requirement list and note the trigger/engine split: PostToolUse invokes wiring_checker.py on changed files (task 18.1, tasks.md:313) which performs the static call-graph/dead-code analysis and emits WIRING coverage-item candidates (task 19, tasks.md:325-331). The hook is the trigger; wiring_checker is the engine. Integration test 26.1 (tasks.md:404-410) already exercises the hook+wiring_checker path together under Req 8.1/8.2, so the mapping is testably bound — make it explicit in the wiring row rather than leaving Req 8.1 traced only through the wiring_checker engine task.
- _adds EARS/use-case wiring_

**103. Req 20.1 SAST-threshold EARS story: PostToolUse reports HIGH/CRITICAL but does not enforce — mapping unstated**  ·  _[post_tool_use_hook.py]_
- **anchor:** Hook Wiring Table, PostToolUse row (design.md:216), 'What it blocks' column currently 'Nothing (exit 1 = non-blocking)'
- **change:** Clarify the Req 20.1 (0 HIGH/CRITICAL SAST findings) mapping for PostToolUse: the hook filters semgrep output for HIGH/CRITICAL findings (task 18.1, tasks.md:312) but only REPORTS them via next-turn feedback (exit 1) — it does NOT enforce/block on the 0-HIGH/CRITICAL threshold. Enforcement of that threshold is the CI SAST gate (REQ-SEC-001, CodeQL/Semgrep workflows, task 23). Add a clause to the PostToolUse 'What it blocks' cell (and a task 18.1 bullet) stating PostToolUse surfaces SAST findings as feedback only; the 0-HIGH/CRITICAL threshold (Req 20.1) is enforced at the CI gate, not at edit time.
- _adds EARS/use-case wiring_

**104. Wire REQ-STATE-001 (persist run state outside model context) to PreCompact as a contributing writer**  ·  _[pre_compact_hook.py]_
- **anchor:** design.md:220 PreCompact row (Requirement enforced column currently lists only REQ-STATE-002)
- **change:** PreCompact partially realizes REQ-STATE-001 (requirements.md:219: 'persist all mutable run state outside model context, in files, git history') — it is the write-side that persists progress + evidence to files and git before compaction. The current PreCompact row maps only REQ-STATE-002. Add REQ-STATE-001 to the PreCompact row's Requirement-enforced column (as a contributing writer alongside REQ-STATE-002) so the file+git persistence obligation is traced to this component, not only to the durable-Postgres path. (Note: this is a partial mapping — Postgres persistence under REQ-STATE-001 is covered by Phase 2 tasks 31.x.)
- _adds EARS/use-case wiring_

**105. Specify the producer that copies validate_spec() violation_count into run_state.violation_count**  ·  _[spec_validator.py]_
- **anchor:** design.md:231-232 initializer Key behaviors: 'Runs spec_validator.py after every elaboration pass' / 'Loops (bounded to DEFAULT=7 passes) until violation_count == 0'
- **change:** Add an explicit producer step to the Initializer key-behaviors (design.md:231-232) AND to Task 10.1 (tasks.md:204): on each elaboration pass call save_run_state(run_state with violation_count = validate_spec(...)['violation_count']) — including the -1 timeout sentinel — so the Stop hook's run_state.violation_count (design.md:490, read at design.md:722-725, REQ-SPEC-021) has a specified writer. Currently validate_spec RETURNS violation_count and stop_hook READS run_state.violation_count, but no step wires producer→consumer (Task 31.1 only wires save_run_state mechanically, never with the validator's output).
- _adds EARS/use-case wiring_

**106. Assign prior-pass violation_count storage (prev_violation_count) for Property 15 convergence**  ·  _[spec_validator.py]_
- **anchor:** design.md:231 'Runs spec_validator.py after every elaboration pass'; Property 15 (design.md:994-998); run_state schema (design.md:481-496, no prev field)
- **change:** Property 15 (strict-decrease-or-HANDOFF, design.md:994-998) and Req 4.3 require comparing the current pass's violation_count against the prior pass's, but no run_state field stores the prior value (run_state schema design.md:481-496 has violation_count but no prev_violation_count). Add a run_state.prev_violation_count field and specify in the Initializer key-behaviors (design.md:231) / Task 10.1 that the loop compares current vs prior to enforce strict-decrease-or-HANDOFF. Mirror with a column in migration 005_run_state.sql (Task 28.5).
- _adds EARS/use-case wiring_

**107. Criterion 8.2 (Verifier marks WIRING item failed on unreachable symbol) has no mechanism**  ·  _[wiring_checker.py]_
- **anchor:** verifier.md key-behaviors list, design.md:257-266 (Runs structural/semantic/behavioral/security/perf checks) — no WIRING/unreachable behavior listed
- **change:** Add a verifier.md key-behavior bullet: 'On each slice, the Verifier consumes wiring_checker.py findings (or re-runs it); for any WIRING item whose symbol is reported unreachable from a real execution path, performs the unproven→failed transition (permitted by the PreToolUse status guard, design.md:215).' Pair it with a tasks.md subtask wiring the checker's unreachable-symbol finding to a Verifier-authored unproven→failed transition, citing REQ-EXEC-011. Currently the Verifier definition never reads wiring_checker output, never mentions WIRING items, and no task connects the finding to a failed transition.
- _adds EARS/use-case wiring_

**108. State whether a non-byte amendment (Postgres-only) leaves a stale plan-approved.json validly unlocking writes**  ·  _[plan-approved.json]_
- **anchor:** `plan-approved.json` Gate (SHA over feature_list.json bytes only, design.md:791); requirement_versions reset (design.md:1152); Property 25 Amendment Monotonicity (design.md:1074-1080); Req 22 (requirements.md:433)
- **change:** Document the interaction between Req 22 / Property 25 amendment versioning and the plan gate: when a requirement is amended and the coverage item re-enters `unproven` by mutating ONLY the Postgres requirement_versions / coverage_items tables (not feature_list.json bytes), the feature_list_sha still matches, so the existing plan-approved.json stays valid and continues to unlock implementation writes against a now-stale coverage model. Either require amendments to also rewrite feature_list.json (forcing a SHA re-block) or have the plan gate additionally check for any post-approval amendment / unproven re-entry. State the chosen rule so the stale-approval hole is closed.
- _adds EARS/use-case wiring_

**109. run_state DDL has no budget threshold column the Stop hook compares against**  ·  _[db/migrations/ (all 8 tables 001-008)]_
- **anchor:** CREATE TABLE run_state (...) block at design.md:481-496; evaluate_stop branch at design.md:704 `if run_state.budget_exceeded:  # token_cost_usd >= token_budget_usd`
- **change:** Add a `token_budget_usd NUMERIC(10,4) NOT NULL` (operator-set DEFAULT) column to the run_state DDL, and define `budget_exceeded` as the derived predicate `token_cost_usd >= token_budget_usd` (either a generated column or documented as computed in run_state_manager). Cross-reference the 1,000,000-token-per-slice budget (requirements.md:357, REQ-LOOP-001/002) and the design.md:823 circuit breaker. Also add the budget column to task 28.5 (currently lists only status CHECK, stop_hook_active, no_progress_n). Currently the Stop hook reads `run_state.budget_exceeded` against a threshold that has no schema backing.
- _adds EARS/use-case wiring_

**110. Add run_state.state_hash baseline column + a producer that records it**  ·  _[state_integrity.py]_
- **anchor:** -- run_state: per-session execution state for resumption (design.md:481-496); specifically the column list ending '... resume_integrity_ok BOOLEAN, ... last_commit_sha TEXT,' — no state_hash / recorded-hash column exists
- **change:** Add a `state_hash TEXT` column to the run_state DDL (design.md:481-496) to hold the recorded baseline that state_integrity.py compares its recompute against — requirements.md:437 says it compares to 'the durable store's recorded hash' but no column holds it. ALSO name the PRODUCER and moment: the PreCompact hook-wiring row (design.md:220, currently 'Checkpoints claude-progress.txt, current evidence state, and feature_list.json to git') and the per-Slice commit must additionally WRITE run_state.state_hash. Without an assigned producer the 'durable store's recorded hash' is never created by any specified writer (the comparison input does not exist).
- _adds EARS/use-case wiring_

**111. Add a 'first write of a resumed session' detection mechanism to run_state**  ·  _[state_integrity.py]_
- **anchor:** PreToolUse integrity-guard row design.md:212 ('First `Write`/`Edit` of a resumed session ...') and Property 26 design.md:1088 (scoped to 'the first Write/Edit of a resumed session'); run_state DDL design.md:481-496 (no is_resume / first_write_done / session-origin column)
- **change:** The guard is scoped to the FIRST implementation write of a RESUMED session, but the PreToolUse hook is registered with no matcher (design.md:168-173, fires on every tool) and run_state has no field letting the guard recognize that moment. Add durable state — e.g. an `is_resume BOOLEAN` (set by SessionStart when a prior run_state row exists) and a `first_write_done BOOLEAN DEFAULT FALSE` (set true after the guard passes the first write) — and have task 49.1's guard logic key on (is_resume AND NOT first_write_done). Otherwise the guard cannot implement the 'first write of a resumed session' scope that Property 26 and Requirement 23.2 require.
- _adds EARS/use-case wiring_

**112. Annotate PreToolUse sub-gate rows (or add a blanket rule) as audit_log.append producers**  ·  _[audit_log.py]_
- **anchor:** Hook Wiring Table rows 211-216 (scope gate / integrity guard / checklist-approval guard / artifact guard / status guard / PostToolUse) — only row 210 (plan gate) lists audit_log.append
- **change:** REQ-AUDIT-001 (line 457) says append fires on EVERY allow/block gate decision, but only the plan-gate row (210) is annotated as a producer. Add a one-line rule directly under the Hook Wiring Table: 'Every PreToolUse sub-gate that returns allow/block (scope/integrity/checklist-approval/artifact/status) calls audit_log.append(...) on its decision; PostToolUse is exempt because it never gates (exit 1, non-blocking).' This both wires the un-annotated blocking sub-gates (integrity guard REQ-STATE-005 and checklist guard REQ-SPEC-016 are exit-2 BLOCKING and conspicuously un-audited) and documents the PostToolUse exclusion as deliberate.
- _adds EARS/use-case wiring_

**113. Add a correctness property (or explicit no-Z3 note) for the REQ-17.2 CI secrets diff-scan**  ·  _[secrets-scan.yml]_
- **anchor:** Properties section near Property 20 (design.md:1034-1038, which validates 17.5 not 17.2); 'no Z3 check' note pattern at design.md:1110 (Property 28) and 1120 (Property 29)
- **change:** secrets-scan.yml (REQ-17.2) has neither a correctness Property nor a Z3 CHECK nor a documented 'no formal check' rationale (verified: formal_verification_merged.py has no 17.2/secrets check; Property 20 covers only 17.5). Add a property: 'For any PR diff containing a known secret pattern, the secrets-scan check fails and merge is blocked; for a clean diff it passes' validated against Requirements 17.2, OR add an explicit '(no Z3 check — runtime/CI verified by fixture test)' note mirroring Properties 28/29. Cite the chosen mechanism from the design inventory row (design.md:147).
- _adds EARS/use-case wiring_

**114. Add a subagent-output JSON Schema artifact making omission_declaration required & non-nullable (REQ-SPEC-018.3 has no target)**  ·  _[omission_guard (capability in subagent_stop_hook.py)]_
- **anchor:** Component Inventory table — row `| `schema/` | `schema/feature_list.schema.json` | 0 | JSON Schema for feature_list.json. PreToolUse validates against this. |` (design.md:138); and Subagent Definitions section (design.md:226-281) which declare behaviors but no output schema
- **change:** Add a new component-inventory row `| schema/subagent_output.schema.json | schema/subagent_output.schema.json | 0 | JSON Schema for initializer/research/verifier subagent outputs; omission_declaration is a required, non-nullable property. SubagentStop omission-guard validates against this. |`. Add a Data Models subsection 'Subagent Output Schema' defining the object with omission_declaration required (type object/non-null, six keys Primary/Alternate/Exception/Recovery/Non-Functional/Edge-Case, each an array of [Gap]-marked strings). Reference this schema from the initializer.md/research.md/verifier.md subagent definitions (add an 'Output contract' line to each). This gives REQ-SPEC-018.3 ('JSON schema for each subagent output type SHALL include omission_declaration as required, non-nullable') a concrete target artifact, mirroring how feature_list.schema.json is the target for EvidenceRecord.
- _adds EARS/use-case wiring_

**115. Two gates in one host hook with no specified ordering, combined exit-code semantics, or joint failure reporting**  ·  _[omission_guard (capability in subagent_stop_hook.py)]_
- **anchor:** Hook Wiring Table rows design.md:217 (SubagentStop Evidence_Record gate) and design.md:218 (SubagentStop omission-declaration guard); and Phase 0 Component List row #5 (design.md:627): 'SubagentStop hook | Validates Evidence_Record has all four fields | Missing output_hash → blocks. All four fields present → proceeds.'
- **change:** Add prose (in a new 'SubagentStop omission-declaration gate' design subsection, or adjacent to rows 217-218) specifying: (1) evaluation order of the two co-resident gates inside subagent_stop_hook.py (Evidence_Record four-field validation and the omission guard); (2) that exit 2 is returned if EITHER gate fails, with both failure reasons surfaced to stderr; (3) how a single exit code is composed when both fail. Add an omission-path row/clause to the Phase 0 build-order acceptance test at design.md:627 (e.g. 'null/absent omission_declaration → blocks; present → proceeds').
- _adds EARS/use-case wiring_

**116. Host SubagentStop hook fires on EVERY subagent result, but omission scope is three subagents (initializer/research/verifier) — no subagent-type discriminator specified**  ·  _[omission_guard (capability in subagent_stop_hook.py)]_
- **anchor:** Hook Wiring Table: omission-guard TRIGGER scoped to '(initializer, research, verifier)' (design.md:218) vs the unconditional SubagentStop command registration (design.md:181-186) and the unmatched Evidence SubagentStop row (design.md:217); implementer results are out of scope but nothing says how the hook distinguishes them
- **change:** Specify the discriminator the omission guard uses to scope itself to initializer/research/verifier results and EXCLUDE implementer results: e.g. read a subagent-type/agent-name field from the SubagentStop payload (or the subagent_output schema's role field) and skip the omission check when the producing agent is implementer.md. State this in the omission-guard wiring row (design.md:218) and in the new omission-guard design subsection, and add it to the subagent_output schema (a required `agent_role` enum). Without it, an implementer result with no omission_declaration would be wrongly blocked or the scope is unenforceable.
- _adds EARS/use-case wiring_

**117. Create subagent-output JSON schema with required omission_declaration (Req 29.3)**  ·  _[initializer.md]_
- **anchor:** | `schema/` | `schema/feature_list.schema.json` | 0 | JSON Schema for feature_list.json. PreToolUse validates against this. |
- **change:** Add a Component-Inventory row for a new `schema/subagent_output.schema.json` (Phase 0) that defines the initializer/research/verifier output envelope, and specify in it that `omission_declaration` is a REQUIRED, non-nullable field (array of `[Gap]`-marked entries keyed by EARS scenario category) per Requirement 29.3 / REQ-SPEC-018. Reference this schema from the SubagentStop omission-declaration guard (design.md:218) as the producer-side contract the gate enforces. Currently Req 29.3 mandates 'THE JSON schema for each subagent output type SHALL include omission_declaration as required, non-nullable' but no such schema file or inventory row exists; only feature_list.schema.json is defined.
- _adds EARS/use-case wiring_

**118. Specify Research-claim epistemic precondition (Req 24.2) on the initializer's discovery pass**  ·  _[initializer.md]_
- **anchor:** - Expands intent against the domain-baseline checklist; flags any UNMAPPED items
- **change:** Add a precondition to the initializer's discovery behavior tying it to Requirement 24.2 / Property 29: the initializer (the CONSUMER of the domain-baseline checklist) may run proactive discovery only against a checklist that is both human-approved (non-null approved_at — already gated by the PreToolUse checklist-approval guard / CHECK-12) AND whose sourced claims carry source-URL + authority-tier labels and have passed the independent fact-check pass. State that an approved-but-unlabeled/unverified checklist is not usable for discovery. This makes the Req 24.2 epistemic gate an explicit precondition on the initializer beyond the bare DRAFT/approved bit.
- _adds EARS/use-case wiring_

**119. No subagent-registration/discovery binding for the four agents**  ·  _[implementer.md]_
- **anchor:** ### Hook Configuration (`settings.json`)
- **change:** The settings.json block (design.md:159-201) registers ONLY hooks; nothing documents how the runtime discovers/loads/invokes .claude/agents/*.md. Either (a) add a sentence stating Claude Code auto-discovers `.claude/agents/*.md` by convention (cite the substrate version), or (b) add an explicit agent-registration entry to the settings.json design. Then add a smoke-test row to task 9.2 (test_hook_config.py) asserting the four agents (initializer, implementer, verifier, research) are discoverable/registered.
- _adds EARS/use-case wiring_

**120. Wire the implementer-loop retry budget AND per-slice token/cost budget into implementer.md and task 10.2**  ·  _[implementer.md]_
- **anchor:** **Key behaviors:**
- Reads `feature_list.json` to identify the single highest-priority `unproven` item
- **change:** requirements.md:359 names 'the implementer loop' as OWNER of the retry budget (run_state.retry_count, DEFAULT 3) and REQ-LOOP-001 (14.1) puts the per-slice token/cost budget on the same loop, but implementer.md key behaviors (design.md:244-249) and task 10.2 (tasks.md:208-211) mention neither. Add a key behavior: 'Increments run_state.retry_count on a failed slice; on retry_count >= 3 stops and routes to HANDOFF (REQ-LOOP-003); the per-slice token/cost budget (REQ-LOOP-001) and its REQ-LOOP-002 HANDOFF trigger are enforced for the slice it owns.' Add the matching bullet to task 10.2 citing 14.1/14.3.
- _adds EARS/use-case wiring_

**121. Property 24 has neither a Z3 check nor a 'no-Z3-check' annotation**  ·  _[implementer.md]_
- **anchor:** ### Property 24: Subagent Role Separation (Implementer Cannot Self-Verify)
- **change:** Property 24 (design.md:1066-1070) carries only '**Validates: Requirements 9.2**' — no machine-check line. The harness has zero actor_agent / role-separation / self-verification assertion (grep of formal_verification_merged.py returns nothing). Either (a) add a Z3 CHECK to formal_verification_merged.py asserting (evidenceCreated ∧ actorIsImplementer) is UNSAT and cite it here, or (b) annotate Property 24 with an explicit '(no Z3 check — runtime/test verified via tasks.md 39.13)' line exactly as Properties 28 and 29 are annotated (design.md:1110, 1120). Pick one and apply.
- _adds EARS/use-case wiring_

**122. Per-worktree write confinement: cross-reference implementer.md to the existing sandbox mechanism**  ·  _[implementer.md]_
- **anchor:** **Permissions:** Write to implementation source files in its assigned worktree only. No write access to `tests/`, `feature_list.json` schema, CI config, or other worktrees.
- **change:** implementer.md's narrative permission (design.md:251) names 'worktree only' but cites no enforcement mechanism, while the enforcing mechanism ALREADY exists at design.md:514-527 (Sandbox & Worktree Isolation, REQ-17.4, task 46) — design.md:524-527 confines filesystem writes to the per-slice worktree and explicitly ties it to 'the one-slice/one-worktree discipline from the Implementer subagent definition.' Add a cross-reference from this Permissions line to the Sandbox & Worktree Isolation section so the confinement claim names its enforcer; OR, if sandbox is deferred, add a PreToolUse check that blocks writes whose path is outside the active slice's worktree. State which mechanism enforces design.md:251.
- _adds EARS/use-case wiring_

**123. Add runtime enforcement mechanism for Property 24 (implementer-cannot-self-verify)**  ·  _[verifier.md]_
- **anchor:** Property 24: Subagent Role Separation (design.md:1066-1070); evidence_records.actor_agent column comment (design.md:473)
- **change:** Property 24 has no enforcement: actor_agent is only a SQL column comment, and no hook/tool validates actor_agent != implementer at evidence-write time. Add an actor-identity check to the design: in subagent_stop_hook.py (or the PreToolUse status guard), block any feature_list.json status→proven write or evidence_records insert whose actor_agent equals the implementer that produced the slice. Cite this new mechanism from Property 24, and add the corresponding Hook Wiring Table row (REQ-VERIFY-002 / Req 9.2). Verified: grep of formal_verification_merged.py returns zero hits for actor_agent/verifier/implementer/role — no Z3 check models role separation; CHECK-13 uses a generic subagentAccepted13 boolean with no per-subagent identity. Optionally add a Z3 CHECK modeling actor_agent==verifier for any proven transition.
- _adds EARS/use-case wiring_

**124. Add Verifier WIRING-failed transition behavior consuming wiring_checker output (Req 8.2)**  ·  _[verifier.md]_
- **anchor:** Verifier key-behaviors list (design.md:257-265); Req 8.2/REQ-EXEC-011 'THE Verifier SHALL mark the corresponding WIRING item failed'
- **change:** Req 8.2 names the Verifier as the actor that marks unreachable WIRING items failed, and wiring_checker.py (task 19) emits WIRING candidates, but the Verifier's key-behaviors list (design.md:257-265) never mentions WIRING handling and no design step connects the Verifier to consume wiring_checker output or perform the unproven→failed transition. Add a Verifier key-behavior: ingest wiring_checker.py findings and set unreachable WIRING items to failed (unproven→failed edge). Reference REQ-EXEC-011 and requirements.md:129 ('unproven→failed (Verifier on a dead check)').
- _adds EARS/use-case wiring_

**125. Add Verifier WIRING integration-test evidence behavior (Req 8.3 / task 47)**  ·  _[verifier.md]_
- **anchor:** Verifier key-behaviors list (design.md:257-265); Req 8.3/REQ-EXEC-012; tasks.md:959 task 47
- **change:** Req 8.3 requires an integration test exercising the real execution path as the attached Evidence_Record when a WIRING item is claimed proven, and task 47 (tasks.md:959) adds this evidence requirement, but the Verifier's key-behaviors never describe the WIRING-integration-test special case that the Verifier must attach. Add a Verifier behavior: when proving a WIRING item, require/attach an integration-test Evidence_Record exercising the real path. Cross-reference task 47 and REQ-EXEC-012.
- _adds EARS/use-case wiring_

### ⚪ UNDERSPECIFIED (63)

**126. Enforce ears_pattern (and conditionally provenance) at the file level or relocate the guarantee to spec_validator**  ·  _[feature_list.json]_
- **anchor:** CoverageItem.required at design.md:324; ears_pattern at 345-349; provenance at 365-369
- **change:** `ears_pattern` and `provenance` are schema-optional, yet Req 1.4/Property 16 require exactly one EARS pattern per item and Req 2.3 requires provenance on every inferred requirement, so both guarantees are unenforced at the file level. Either add `ears_pattern` to CoverageItem.required and add an allOf making `provenance` required when provenance=='inferred' is in play (or unconditionally), OR explicitly state in Req 1.4 and Req 2.3 that these are enforced by spec_validator.py at authoring time (not by the file schema) and add the corresponding spec_validator check + task. Pick one and make schema, requirements, and Property 16 agree.
- _adds EARS/use-case wiring_

**127. Add a run_state.phase discriminator and specify per-phase cap/no-progress routing in evaluate_stop**  ·  _[stop_hook.py]_
- **anchor:** run_state DDL design.md:481-496 (no `phase` column); evaluate_stop pseudocode design.md:685-737 (single undifferentiated cap/no-progress path)
- **change:** stop_hook serves BOTH the spec-completion loop (Req 4: violation_count, cap=7 passes, pass-level strict-decrease no-progress) AND the implementation loop (Req 10/14: slice completion, cap=25 turns/slice, slice-level N=3 no-progress, design.md:840). The design never states how one Stop hook discriminates the two phases, yet they have different caps and different no-progress semantics. Add a `phase TEXT CHECK (phase IN ('spec','implementation'))` column to the run_state DDL and branch evaluate_stop: in spec phase apply SPEC_COMPLETION_HARD_CAP=7 + pass-level no-progress; in implementation phase apply MAX_TURNS_PER_SLICE=25 + slice-level check_no_progress. Cite design.md:832-842.

**128. State that an in-scope item in `failed` status also blocks the Stop completion gate**  ·  _[stop_hook.py]_
- **anchor:** evaluate_stop completion gate design.md:731 `not_proven = [i for i in in_scope_items if i.status != "proven"]` and Property 5 (design.md:914-916)
- **change:** With Req 5.8 / Property 3 making `failed` a first-class status, the completion gate `i.status != "proven"` means a `failed` in-scope item ALSO blocks termination (it is not-proven), exactly like `unproven`. Property 5 and the Stop hook-wiring row (design.md:209) describe the gate only in terms of `unproven`, never stating that `failed` likewise blocks. Update Property 5 / the design narrative to say 'any in-scope item whose status is not exactly `proven` (i.e. `unproven` OR `failed`) blocks termination', so the implementer does not special-case only `unproven`. Cite requirements.md:129; design.md:731,900.

**129. Pin the artifact guard's protected-path globs and destructive-Bash regex set canonically**  ·  _[pre_tool_use_hook.py]_
- **anchor:** Hook-wiring artifact-guard row (design.md:214) and task 6.3 (tasks.md:134)
- **change:** Define an explicit canonical PROTECTED_PATHS glob set (e.g. feature_list.json, schema/feature_list.schema.json, tests/**, .github/workflows/**, .github/policies/**) and a DESTRUCTIVE_BASH regex set covering bypass-equivalent forms (rm -rf, DROP TABLE, git reset --hard, git push --force/-f, truncate, find ... -delete, > redirection over protected paths) in design.md, and reference them from task 6.3. Also reconcile the membership disagreement: the design hook-table row (design.md:214) lists only `rm -rf` and `DROP TABLE` while task 6.3 (tasks.md:134) adds `git reset --hard` — make the two sources agree. Add a property test enumerating bypass-equivalent destructive commands.

**130. Specify the resumed-state hash inputs / algorithm / baseline-store location**  ·  _[session_start_hook.py]_
- **anchor:** Property 26 (design.md:1088-1090), Hook Wiring SessionStart row (design.md:219), state_integrity.py inventory + 'New / changed components' note (design.md:143, design.md:1180); Requirement 23 (requirements.md:437-438)
- **change:** Neither requirements.md nor design.md defines WHICH state is hashed (feature_list.json items only? + run_state fields? + git HEAD?), the hash algorithm, or where the baseline 'durable store's recorded hash' it is compared against is persisted — yet SessionStart is credited with the compute. Add a short spec near Property 26 / the state_integrity.py note: exact byte-set (canonical JSON of in_scope feature_list.json items + named run_state fields), algorithm sha256 (consistent with the evidence/audit-log sha256 convention), and the run_state column holding the baseline hash, so SessionStart's compute is implementable from the spec alone.

**131. Note REQ-STATE-003 base load is example-test-verified only (no invariant)**  ·  _[session_start_hook.py]_
- **anchor:** Property 26 block (design.md:1084-1090) and the Hook Wiring SessionStart row (design.md:219); REQ-STATE-003 / criterion 11.3 (requirements.md:221)
- **change:** The reconciliation-added integrity half has Property 26 + CHECK-11, but the component's PRIMARY purpose — REQ-STATE-003 base load (git status + progress + coverage so a session re-orients) — has NO correctness Property and NO Z3 CHECK; it is exercised only by example tests (7.2, 16.1, 17.1). Either add a property ('for any populated feature_list.json, the SessionStart summary reports the exact unproven/proven counts') or add a one-line design note that REQ-STATE-003 is intentionally example-test-verified only (no invariant), so the absence is documented rather than an oversight.

**132. Scope the base evidence-schema gate to verifier results (skip-when-no-evidence)**  ·  _[subagent_stop_hook.py]_
- **anchor:** Hook Wiring Table row: `| `SubagentStop` | Subagent result returned | Validates Evidence_Record schema (all four fields present and non-empty); calls `audit_log.append(...)` ... | REQ-VERIFY-005, REQ-COV-006, REQ-AUDIT-001 |` (design.md:217)
- **change:** Clarify that the base evidence-schema gate applies ONLY to subagent results that CARRY an `evidence` field (verifier results): the hook locates evidence field(s) and, when none are present (initializer/research results, which produce specs/checklists not Evidence_Records), it SKIPS the four-field validation rather than failing closed. State explicitly that REQ-VERIFY-005's 'required evidence markers' for NON-verifier subagents are enforced by the omission-declaration guard (omission_declaration field), not the four-field schema. Mirror this in tasks.md task 8.1 (add a 'skip when result has no evidence field' bullet). This resolves the universal-over-'a subagent's result' wording (requirements.md:190) vs the verifier-only schema gate (design.md:217).
- _adds EARS/use-case wiring_

**133. Add a dedicated design subsection assembling the full subagent_stop_hook.py obligation set**  ·  _[subagent_stop_hook.py]_
- **anchor:** No dedicated subsection exists for subagent_stop_hook.py (contrast the 'No-Progress Watchdog' subsection at design.md:640 which fully narrates stop_hook.py); only inventory line :130 + wiring rows :217-218
- **change:** Add a dedicated design subsection for subagent_stop_hook.py (parallel to 'No-Progress Watchdog') specifying: the stdin parse contract; skip-when-no-evidence semantics; ordering of the omission_guard check vs the evidence-schema check; the audit_log.append call site/ordering; and exception handling (exit 2 fail-closed). Currently the hook's full obligation set is scattered across tasks 8, 52.3, and 54 with no single assembling task, and task 8.1 tags only requirement 9.5. Update tasks.md task 8.1 to also reference REQ-COV-006, REQ-SPEC-018, and REQ-AUDIT-001, and cross-link tasks 52.3 and 54 as completing the same component.

**134. PostToolUse Semgrep-only scope (CodeQL is CI-only) is nowhere stated**  ·  _[post_tool_use_hook.py]_
- **anchor:** Hook Wiring Table, PostToolUse row (design.md:216): "Runs lint, type check, SAST (Semgrep), wiring check on changed files"
- **change:** Add a clause to the PostToolUse hook-wiring row (and mirror in task 18.1, tasks.md:312) stating the in-loop hook runs Semgrep ONLY for SAST; CodeQL is the CI gate (task 23 / REQ-SEC-001), not run at edit time. This prevents the glossary term Semgrep_CodeQL (requirements.md:47) and REQ-VERIFY-001 (requirements.md:186) from being misread as requiring the hook to run CodeQL. Suggested: append '(Semgrep only at edit time; CodeQL runs in CI per REQ-SEC-001)'.

**135. No dedicated PostToolUse design subsection: changed-file parsing, stdout feedback schema, and MultiEdit handling unspecified**  ·  _[post_tool_use_hook.py]_
- **anchor:** After the '`plan-approved.json` Gate' subsection (ends ~design.md:801), before '## Error Handling' (design.md:805) — mirror depth of 'No-Progress Watchdog' / 'plan-approved.json Gate'
- **change:** Add a 'PostToolUse Feedback Hook' design subsection (parallel to the Stop-hook and PreToolUse sections) defining: (a) how changed-file path(s) are parsed from the PostToolUse event payload for Write/Edit/MultiEdit — every sibling hook documents its input parsing (e.g. subagent_stop_hook task 8.1, tasks.md:178 'Parse subagent result from stdin (JSON)'; pre_tool_use reads tool input) but task 18.1 (tasks.md:309-316) uniquely says only 'on changed files' with no stdin-parsing step; (b) the JSON/text schema of the structured feedback emitted to stdout that the agent consumes next turn; (c) how a MultiEdit batch (multiple files in one event) is fanned out to the per-file lint/type/SAST/wiring runs. Also add a matching bullet to task 18.1 spelling out the stdin payload parse step.

**136. No timeout specified for PostToolUse despite shelling out to four external processes**  ·  _[post_tool_use_hook.py]_
- **anchor:** Error Handling > Hook Failure Modes table, '`post_tool_use_hook.py` crashes' row (design.md:813)
- **change:** Add a timeout behavior for post_tool_use_hook.py. The Req 20.1 60s validator timeout (requirements.md:354) is documented only for spec_validator.py (design.md:814) and silent for this hook, yet semgrep --config auto on a large changeset can exceed 60s. Add a Hook Failure Modes row (or extend the existing post_tool_use_hook row) and a task 18.1 bullet: apply the 60s validator-timeout default per external tool (or a dedicated post-hook budget); on timeout still exit 1 with a 'post-hook timeout' finding in the structured feedback — never exit 2, never block. This is also the concrete site to connect the N-25 'never fail-open' family note (tasks.md:730): when telemetry/DB is down (task 36.1 OTel forwarding) the hook MUST still exit 1 and not crash the edit loop.

**137. Add pre_compact_hook.py row to the Hook Failure Modes table + pin the commit worktree**  ·  _[pre_compact_hook.py]_
- **anchor:** design.md:809-816 Hook Failure Modes table (currently covers stop_hook.py, pre_tool_use_hook.py, post_tool_use_hook.py, spec_validator.py, Postgres, plan-approved.json — no PreCompact row)
- **change:** Add a `pre_compact_hook.py` failure-mode row: 'On any exception (git index lock, detached HEAD, mid-rebase, unreadable feature_list.json), log to stderr and exit 0 — a failed checkpoint must NEVER block the run (non-blocking by REQ-STATE-002).' Also specify which ref/worktree the checkpoint commits to: the MAIN worktree, never a per-slice worktree, so the `chore: PreCompact checkpoint [skip ci]` commit (tasks.md:394) cannot collide mid-slice with the one-atomic-commit-per-slice invariant (REQ-EXEC-003 / requirements.md:158) or the implementer's 'exactly one atomic commit' rule (design.md:248). Note the checkpoint commit carries no requirement-ID trailer and is exempt from the REQ-TRACE-002 commit-trailer gate (it is a chore, not a slice commit).

**138. REQ-STATE-002 has no Z3 check and no Correctness Property — state the verification basis explicitly**  ·  _[pre_compact_hook.py]_
- **anchor:** design.md:220 PreCompact row (REQ-STATE-002) AND the Correctness Properties list (design.md ~1084-1130, which contains Property 26 for REQ-STATE-005 but no PreCompact/REQ-STATE-002 property)
- **change:** REQ-STATE-002 is the sole reason this component exists yet is verified by nothing in formal_verification_merged.py (a grep shows only the CHECK-11 family covers REQ-STATE, and that maps to REQ-STATE-005, not 002) and by no Correctness Property. This is defensible (a non-blocking checkpoint has no SAT/UNSAT gating invariant) but must be stated, not silent. Add a one-line note near the PreCompact design row / Property list: 'REQ-STATE-002 is verified by unit + integration test (task 25.2), not Z3 — a checkpoint write has no gating invariant.' Optionally add an integration test asserting that after a simulated PreCompact the checkpoint files + commit exist and SessionStart reads them back.

**139. Replace harness-exit proxy test-to-pass with a direct validate_spec() fixture assertion**  ·  _[spec_validator.py]_
- **anchor:** design.md:632 row 10 Test to pass: 'python3 formal_verification_merged.py exits 0 (all 34 checks pass)'
- **change:** The Phase-0 test-to-pass for spec_validator.py (design.md:632, component #10) is the STATIC 34-assertion harness exit code; the harness contains zero validate_spec/spec_validator references (grep confirms), so it never exercises the NLP parse, EARS encoding, the four consistency/completeness/vacuity/independence checks, or the four-field return. Change the test-to-pass to invoke validate_spec() on a fixture spec containing a known contradiction, a known UNMAPPED item, and a vague adjective, asserting the returned {contradictions, ambiguities, uncovered, violation_count} matches expected counts; keep the harness run as a separate check.

**140. Define the analysis-pass → four-return-field mapping in the EARS→SMT section**  ·  _[spec_validator.py]_
- **anchor:** design.md:554-563 'Consistency Checks Run Per Spec' (four checks) vs design.md:133 four-field return {contradictions, ambiguities, uncovered, violation_count}
- **change:** The four analysis passes (Consistency/Completeness/Vacuity/Independence, design.md:554-563) are never mapped to the four return fields (design.md:133), and violation_count's composition is undefined. Add a one-line mapping after design.md:563: contradictions = unsat-core count (Consistency); uncovered = UNMAPPED count (Completeness); ambiguities = vacuity hits + vague-adjective hits; violation_count = sum of the three (>=0), or -1 on Z3 timeout (per design.md:814). This fully defines the four-field contract from Property 14 / Req 4.1.

**141. Define the 'without a numeric bound' bound-detection predicate for the vague-adjective scanner**  ·  _[spec_validator.py]_
- **anchor:** requirements.md:63 'without a quantified numeric bound'; design.md:1010-1014 (Property 17 'without an associated numeric bound'); tasks.md:68,87 'without a numeric bound'
- **change:** Req 1.2, Property 17 (design.md:1010-1014), and Task 4.4 (tasks.md:68,87) all hinge the scanner's accept/reject on a vague adjective appearing 'without a numeric bound', but no doc defines how the presence of a numeric bound is detected (digit regex? unit grammar? proximity window to the adjective? a parsed comparison predicate?). Specify the bound-detection rule in the EARS→SMT section (e.g. a numeric literal with an optional unit within the same clause / N-token window of the flagged adjective) so the core accept/reject decision is reproducible rather than implementation-defined.

**142. Add a design subsection defining output_hash byte-source, canonicalization, and artifact retention**  ·  _[evidence_collector.py]_
- **anchor:** Component inventory row evidence_collector.py (design.md:134) and Phase 0 list row 11 (design.md:633); no narrative subsection exists
- **change:** design.md covers evidence_collector in only inventory cells (134, 263, 633) plus one test line (859) with no narrative on the load-bearing hashing semantics. Add a design subsection (near the EvidenceRecord data model, design.md:387-423) defining: (1) the exact byte source for output_hash — raw test stdout vs the Playwright trace file vs a normalized artifact; (2) a canonicalization rule so non-deterministic test output (timestamps, ordering, temp paths) rehashes identically across runs; (3) whether/where the raw artifact is retained (blob store vs hash-only) — requirements.md:297 says 'artifact OR content-addressed reference', leaving retention unspecified. Without this, output_hash is not reproducibly verifiable and OPA's zero-evidence query cannot detect a fabricated hash (design.md:1052).

**143. Requirement 8 / wiring_checker.py is pinned by no Z3 CHECK and no correctness Property**  ·  _[wiring_checker.py]_
- **anchor:** Correctness Properties section (Properties 1-30, design.md:882-1130) — none addresses call-graph/dead-code/wiring; and verification/formal_verification_merged.py has no REQ-EXEC-010..012 check (grep: zero wiring/dead-code/reachability assertions, CHECKs run only 1..13b)
- **change:** Pick one and apply: (a) add a new Property (e.g. 'Property 31: Wiring / Dead-Code Detection — for any file with a defined-but-never-called non-test symbol, wiring_checker.py emits exactly one WIRING candidate naming that symbol; for any reachable handler, zero candidates') with a Hypothesis test seeded in the PBT suite; OR (b) explicitly document in design.md that wiring detection is a runtime/example-tested property with no Z3 model (mirroring the audit-log note at design.md:1110 'no Z3 check — runtime/CI verified'). Today the absence is silent while every other major gate carries a CHECK or Property; the integration claim rests solely on example tests 19.2 and 26.1.

**144. No design narrative defines reachability scope (per-file vs repo-wide) for wiring_checker.py — cross-file false-positive risk**  ·  _[wiring_checker.py]_
- **anchor:** Component inventory single row design.md:135 (`wiring_checker.py` ... 'Static call-graph / dead-code analysis; emits WIRING coverage items') — no B07/Requirement-8 narrative subsection anywhere
- **change:** Add a B07 design subsection for wiring_checker.py specifying: (1) the definition of 'real execution path'; (2) entry-point seeding (CLI mains, route/handler decorators, registered callbacks; test files excluded); (3) whether reachability is whole-repo or per-file — and reconcile against tasks.md:327 which says 'build a call graph for each file'. A per-file AST graph structurally cannot distinguish 'defined-but-never-called-in-this-file' from 'called-from-another-file', so per-file scope false-positives on every public/exported symbol; the analysis likely must be repo-wide, or per-file with an explicit exported-symbol exemption. Today there is zero narrative/interface/algorithm description.

**145. Add a design.md subsection for orphan_detector defining its full contract**  ·  _[orphan_detector.py]_
- **anchor:** Component inventory row (design.md:136) 'orphan_detector.py ... Detects implementation units with no requirement ref and vice versa'; add a new subsection peer to 'No-Progress Watchdog' (design.md:640)
- **change:** Add a design.md subsection (peer to the No-Progress Watchdog / plan-approved gate subsections) for orphan_detector, since the component currently has only one inventory line plus Property 11 — no CLI signature, no output schema, no caller, no blocking semantics, no data source, and it is absent from the Phase 0 spine list. The subsection must define: the CLI signature; the structured JSON report schema (forward_orphans[], backward_orphans[]); the data source for each orphan class (feature_list.json for forward; traceability_links/evidence_records for backward per the source-of-truth edit); the caller (the traceability-gate CI check); the exit-code-to-block mapping; the run scope and excludes. This consolidates the dedup'd detail edits into one design home.

**146. Document the deliberate assurance level for orphan detection (no Z3, audit-log routing if it blocks)**  ·  _[orphan_detector.py]_
- **anchor:** New orphan_detector design subsection (peer to No-Progress Watchdog, design.md:640); Property 11 (design.md:962-966)
- **change:** State orphan detection's assurance level explicitly. It has zero Z3 coverage (confirmed: no orphan/trace/6.3 hits in formal_verification_merged.py) and, being unwired, produces no gate_audit_log entry (audit producers are stop/pre_tool_use/subagent_stop hooks only, design.md:144,460). Document one of: (a) runtime-only hypothesis Property 11 is the deliberate assurance level (weaker than peer Z3-backed gates) — state this in design.md; OR (b) if orphan detection becomes a blocking gate per the traceability-gate edit, route its allow/block decision through audit_log.append(...) like the other gate producers so the block is tamper-evidently logged.

**147. Specify fail-closed / default-deny posture for the Rego merge policy**  ·  _[coverage_query.rego]_
- **anchor:** Error Handling → Hook Failure Modes table (design.md:809-816); add a coverage-gate row / note near design.md:137 or 815
- **change:** Add a design note giving coverage_query.rego the same fail-closed posture REQ-GATE-005 mandates (requirements.md:207) and that design.md:811-816 already specifies for stop_hook.py / pre_tool_use_hook.py. State default-deny semantics explicitly: an empty `input.items` array, a parse failure, or a missing/malformed feature_list.json MUST produce a deny / non-zero Conftest exit; the coverage-gate.yml CI step MUST treat any Conftest error (non-zero for reasons other than a clean pass) as a merge block, never default-allow. Closes REQ-GATE-005 (ears_needing_work 10.5, status=gap) for the OPA leg.
- _adds EARS/use-case wiring_

**148. Add a dedicated 'Coverage Merge Policy' design subsection for coverage_query.rego**  ·  _[coverage_query.rego]_
- **anchor:** Component Inventory row '`coverage_query.rego` | `.github/policies/coverage_query.rego` | 1 | OPA/Conftest policy; denies merge if any requirement has zero passing evidence.' (design.md:137) — the ONLY dedicated mention; no subsection exists
- **change:** Add a 'Coverage Merge Policy' design subsection bringing coverage_query.rego to parity with the stop_hook.py / plan-approved.json code-bearing subsections. It must state: (1) the canonical input model (feature_list.json Conftest document); (2) an example Rego policy body with the two deny rules over `input.items`; (3) the in_scope filter, the `status != "proven"` rule (covers failed+unproven), and the four-field Evidence_Record completeness rule; (4) default-deny error semantics (empty/missing/parse-error => deny); (5) the workflow wiring (coverage-gate.yml co-tenancy + required-check registration). This consolidates dedup_keys coverage-rego-input-model / in-scope-filter / failed-status / four-field-evidence / fail-closed into one authoritative location; the individual tasks.md edits remain the task-level mirrors.

**149. Add ID pattern to dependencies items (or note referential integrity is ungated)**  ·  _[feature_list.schema.json (schema/)]_
- **anchor:** design.md:354-358 (dependencies array, items {type:string}, no pattern) vs design.md:326-329 (id with pattern ^[A-Z]+-[A-Z]+-[0-9]{3}$)
- **change:** Apply the same `"pattern": "^[A-Z]+-[A-Z]+-[0-9]{3}$"` to the items of dependencies so a dependency at least has a well-formed ID shape, OR add an explicit note that dependency referential integrity (the referenced ID actually exists and is proven) is delegated to the hook/verifier and is intentionally not schema-enforceable. Today a dependency can be any string, including a malformed or non-existent ID, and still validate, which the artifact never acknowledges.

**150. Specify canonical (deterministic-JSON) form for feature_list_sha**  ·  _[plan-approved.json]_
- **anchor:** ## `plan-approved.json` Gate — line 791: `current_sha = compute_sha256(Path("feature_list.json").read_bytes())` and line 801 prose
- **change:** Replace the raw-bytes SHA with a canonical-form SHA: compute feature_list_sha over a deterministic/sorted-key JSON serialization of feature_list.json (mirroring the gate_audit_log 'Canonical form = deterministic JSON' rule at design.md:1170), so that whitespace-only or key-reordering rewrites the human did not review do not break the bind. State this canonical rule in the `plan-approved.json` Gate subsection AND add it to the glossary entry (requirements.md:43). Update the hook snippet to canonicalize before hashing (e.g., `compute_sha256(canonical_json(json.loads(Path('feature_list.json').read_text())))`).

**151. Bind or explicitly demote plan-approved.json.spec_version**  ·  _[plan-approved.json]_
- **anchor:** `plan-approved.json` example, `"spec_version": "1.0.0"` (design.md:767); hook never reads it (design.md:790-798)
- **change:** Resolve spec_version dead-metadata: EITHER bind it (check_plan_approval also compares approval.spec_version against the spec version recorded in the durable store / spec header and blocks on mismatch, so a stale approval that still matches feature_list_sha cannot unlock) OR explicitly document it as advisory-only metadata in the `plan-approved.json` Gate subsection. State which in design.md.

**152. Specify revocation / staleness semantics for plan-approved.json**  ·  _[plan-approved.json]_
- **anchor:** `plan-approved.json` Gate subsection (design.md:758-801); no expiry/revoke/nonce exists anywhere in design.md or requirements.md
- **change:** Add an explicit operator revoke path (operator deletes/invalidates plan-approved.json to re-block writes) as a documented action, and optionally an `expires_at` or `approved_for_session`/nonce field so a stale approval cannot silently authorize a much later run for the same coverage model. Today the only de-facto revocation is mutating feature_list.json to force a SHA mismatch, which is never specified as an operator action.

**153. gate_audit_log append-only is convention-only — no DB-level mutation prevention**  ·  _[db/migrations/ (all 8 tables 001-008)]_
- **anchor:** gate_audit_log DDL in the Merge Reconciliation Addendum (design.md:1156-1167); requirements.md:457 (REQ-AUDIT-001 append-only)
- **change:** Add to migration 008 / the design DDL a DB-level append-only enforcement: `REVOKE UPDATE, DELETE ON gate_audit_log FROM <app_role>` and/or a `BEFORE UPDATE OR DELETE` trigger that RAISEs. Today append-only relies solely on the hash chain (after-the-fact tamper detection) plus convention; the DB still accepts row mutation, which is weaker than REQ-AUDIT-001 intent. Add the corresponding DDL line to task 28.8.

**154. No index DDL anywhere; migrations component lacks an index/performance design narrative**  ·  _[db/migrations/ (all 8 tables 001-008)]_
- **anchor:** Postgres Schema section (design.md:425-509) and inventory row `db/migrations/` (design.md:140); Property 22 OPA COUNT against evidence_records (design.md:1052)
- **change:** Add a Migrations design subsection (apply order, owning Postgres role, required indexes) and add the index DDL to the relevant 28.x tasks. At minimum: index on `evidence_records.requirement_id` (Property 22 per-requirement COUNT), `traceability_links.requirement_id` (queried in both directions un-indexed), and `coverage_items.requirement_id`. The current schema has zero CREATE INDEX statements.

**155. in_scope flip to false has no DB-level enforcement or audit of the human-authored transition**  ·  _[db/migrations/ (all 8 tables 001-008)]_
- **anchor:** coverage_items.in_scope comment design.md:448 ('leaves scope only via a human-authored transition'); requirements.md:128 (human-authored only); no DB mechanism exists
- **change:** Specify enforcement for the 'human-authored only' in_scope=false rule: either document that the hook/audit layer captures every in_scope flip into gate_audit_log (actor + justification), or add a DB trigger requiring an actor/justification on UPDATE that sets in_scope=false. Today the DB accepts any UPDATE flipping in_scope, and since gates count only in_scope items an unguarded flip silently drops a requirement from all completion gates.

**156. Amendment version monotonicity and initial-version seeding are asserted but not mechanized**  ·  _[db/migrations/007_requirement_versions.sql]_
- **anchor:**     UNIQUE (requirement_id, version)
);
-- On amendment: insert a version row AND reset coverage_items.status to 'unproven'.
- **change:** Property 25 / design.md:1078 require a 'monotonically increasing version', but the only DDL constraint (UNIQUE (requirement_id, version), design.md:1150) permits gapped/out-of-order sequences (e.g. insert version=5 then version=3). Specify the mechanism near design.md:1150 and in task 28.7: compute `version = COALESCE(MAX(version),0)+1` inside the amendment_handler transaction (or a BEFORE INSERT trigger enforcing version = previous_max+1). Also specify seeding semantics: state whether the table holds amendments only (first amendment is version 1, its `prior_text` = the original approved text from requirements.id, hence `prior_text` is non-null after the first amendment) and what a null `prior_text` means (reserve for an optional seed/baseline row, or forbid null in practice).

**157. Append-only 'never overwritten' guarantee has no DDL or named application-layer enforcement**  ·  _[db/migrations/007_requirement_versions.sql]_
- **anchor:** -- On amendment: insert a version row AND reset coverage_items.status to 'unproven'.
-- Completion gate already blocks COMPLETE while any item is 'unproven' (CHECK-10a).
- **change:** design.md:1078 says prior version text 'shall be retained and never overwritten', but requirement_versions has no append-only protection (no REVOKE UPDATE/DELETE, no BEFORE UPDATE/DELETE trigger). Add to the migration spec EITHER an append-only guard (REVOKE UPDATE, DELETE ON requirement_versions, or a trigger that RAISEs on UPDATE/DELETE) OR an explicit note that append-only is enforced at the application layer, naming where (e.g. amendment_handler is the sole writer and never updates). Document alongside design.md:1152.

**158. FK ON DELETE behavior and requirement→coverage_item reset mapping unspecified**  ·  _[db/migrations/007_requirement_versions.sql]_
- **anchor:**     requirement_id TEXT NOT NULL REFERENCES requirements(id),
    version        INTEGER NOT NULL,
- **change:** design.md:1143's `REFERENCES requirements(id)` has no ON DELETE clause (defaults to NO ACTION). Decide and document ON DELETE RESTRICT (history must survive deletion of the requirement). Also clarify the join the amendment relies on: the row references `requirement_id`, but the status reset targets coverage_items — state the cardinality (requirement_id → coverage_item(s), 1:1 or 1:many) and how amendment_handler locates the coverage_item(s) to reset. Add an index on `requirement_id` for amendment-history lookups. Add near design.md:1143-1152.

**159. Pin the write protocol that supplies DB-assigned seq/created_at to the hash producer**  ·  _[db/migrations/008_gate_audit_log.sql]_
- **anchor:** design.md:1173 `--   * Producer: tools/audit_log.append(event, tool, decision, reason, requirement_id, actor_agent),` (and requirements.md:461 Requirement 27.5; tasks.md:968 task 52.1)
- **change:** Resolve the seq/created_at chicken-and-egg: entry_hash is defined to INCLUDE seq (BIGSERIAL, DB-assigned) and created_at (DEFAULT now(), DB-assigned) but append(event,tool,decision,reason,requirement_id,actor_agent) receives neither, so the hash cannot be computed before INSERT without an UPDATE (violating append-only). Pin ONE mechanism in design.md:1168-1177, requirements.md:461 (Req 27.5), and task 52.1: have append() pre-assign seq via `SELECT nextval('gate_audit_log_seq_seq')` and set created_at client-side, then INSERT the row with those exact values so the stored seq/created_at equal the hashed ones; OR drop seq+created_at from the canonical hashed tuple. State the chosen mechanism so audit_log.py and audit_verify.py compute identical hashes.
- _adds EARS/use-case wiring_

**160. Pin the NULL/None canonical-JSON serialization rule for the hash**  ·  _[db/migrations/008_gate_audit_log.sql]_
- **anchor:** design.md:1170-1172 `--   * Canonical form = deterministic JSON of (seq, event_name, tool_name, decision, reason, requirement_id, actor_agent, created_at).` (and requirements.md:461 Req 27.5)
- **change:** tool_name, reason, requirement_id are nullable (design.md:1159,1161,1162) but the canonical-JSON serialization of NULL is never pinned (omit key? json null? empty string?), so producer and verifier may serialize NULL differently and produce divergent entry_hash (spurious tamper failures). Pin in design.md:1168-1172 and Req 27.5 (requirements.md:461): keys ALWAYS present, NULL serialized as JSON `null` literal, `sort_keys=true`, UTF-8, no insignificant whitespace (compact separators), and require audit_log.py (task 52.1) and audit_verify.py (task 52.2) to import ONE shared canonicalizer function.
- _adds EARS/use-case wiring_

**161. Decide FK from gate_audit_log.requirement_id to requirements(id), or document the exception**  ·  _[db/migrations/008_gate_audit_log.sql]_
- **anchor:** design.md:1162 `    requirement_id TEXT,` (inside CREATE TABLE gate_audit_log)
- **change:** Every sibling table FKs requirement_id to requirements(id) (design.md:445,456,467,484,1143) but gate_audit_log.requirement_id is bare nullable TEXT, so a decision can reference a non-existent requirement and still persist — weakening REQ-AUDIT-001 traceability. Either add `requirement_id TEXT REFERENCES requirements(id)` to the DDL (design.md:1162) and task 28.8, OR add a one-line comment in the DDL stating audit rows intentionally allow dangling/absent requirement_id (e.g. plan-gate decisions logged before any requirement is active) so the omission is a documented choice, not drift.

**162. Add verification-access INDEX(es) to gate_audit_log for chain-walk and lookups**  ·  _[db/migrations/008_gate_audit_log.sql]_
- **anchor:** design.md:1156-1167 (CREATE TABLE gate_audit_log) and tasks.md:701 (`Run python3 tools/audit_verify.py to recompute the hash chain over gate_audit_log`)
- **change:** audit_verify.py and the required audit-chain-verify CI check (tasks.md:700-701) full-scan gate_audit_log ordered by seq on an unbounded append-only table (one row per gate decision, written by all three hooks on every allow/block per design.md:209-217). ORDER BY seq is index-backed by the BIGSERIAL PK, but there is no index for per-session or per-requirement_id audit queries, and no spec statement that whole-table re-hash at merge is acceptable at scale. Add to the design.md DDL block and task 28.8 a `CREATE INDEX ON gate_audit_log (requirement_id)` (and/or a session/created_at index), or state explicitly that bounded whole-chain re-hash at merge is acceptable given the retention policy.

**163. Pin the resumed-state hash algorithm (inputs, serialization, hash function)**  ·  _[state_integrity.py]_
- **anchor:** ## Merge Reconciliation Addendum (canonical merge) / New / changed components bullet: "- `state_integrity.py` (Phase 2) — recompute resumed-state hash on `SessionStart`; block proceed on mismatch (REQ-STATE-005 / CHECK-11)." (design.md:1180); and the SQL hash-chain block at design.md:1168-1176 as the sibling pattern to mirror
- **change:** Add a 'Resumed-State Hash' spec subsection (sibling to the gate_audit_log hash-chain spec at design.md:1168-1176) pinning: (a) exact input set — recommend the deterministic JSON of the durable run_state progress fields (current_item_id, iteration_count, no_progress_n, last_commit_sha) PLUS the feature_list.json content SHA PLUS last_commit_sha; (b) canonical serialization = deterministic/sorted-key JSON; (c) state_hash = sha256(canonical_form); (d) WHERE the baseline is recorded (a new run_state.state_hash TEXT column) and WHEN it is written (each PreCompact checkpoint and each per-Slice commit). Currently 'recomputes the resumed-state hash' is asserted in design.md:143/219/1088/1180 with zero algorithm, so the SessionStart recompute and the recorded baseline cannot be guaranteed to hash identical inputs.

**164. Specify the operator-reconciliation / unblock mechanism**  ·  _[state_integrity.py]_
- **anchor:** ## Error Handling → ### Hook Failure Modes table (design.md:805-816), alongside the existing fail-closed rows ('plan-approved.json missing or corrupt | PreToolUse blocks (fail closed)')
- **change:** requirements.md:438 says the guard releases 'only after operator reconciliation flips it true' but no file defines HOW. Add an Error-Handling entry defining a concrete reconciliation entry point, e.g. `python3 tools/state_integrity.py --reconcile --session <id>` that re-records run_state.state_hash from the current accepted state and sets resume_integrity_ok=true, and state what reconciliation entails (re-recording the durable baseline hash). The unblock path is currently a dangling obligation.

**165. Add a 'Resumed-State Integrity' design prose subsection covering failure modes**  ·  _[state_integrity.py]_
- **anchor:** ## Error Handling (design.md:805-816) — there is no dedicated 'Resumed-State Integrity' narrative subsection (contrast the dedicated 'No-Progress Watchdog' (design.md:640) and 'plan-approved.json Gate' (design.md:758) subsections)
- **change:** state_integrity.py — the sole guard against corrupted resume — has no narrative subsection, only three table rows + one Property + a one-line addendum bullet; failure modes (hash recompute fails, durable baseline absent on first run, Postgres down at SessionStart) are discussed nowhere. Add a 'Resumed-State Integrity' subsection covering: hash inputs (cross-ref the hash-algo spec), where/when the baseline state_hash is stored, first-run no-baseline behavior (default resume_integrity_ok TRUE / treat NULL as allow), and SessionStart-time DB-unavailable behavior (fall back to file-backed hash per tasks.md:730).

**166. Add an Error-Handling row for audit_log.append failure (fail-open-for-availability + file fallback)**  ·  _[audit_log.py]_
- **anchor:** ## Error Handling → ### Hook Failure Modes table (lines 809-816), last row 'plan-approved.json missing or corrupt'
- **change:** Add a new row to the Hook Failure Modes table: '| audit_log.append fails (DB write error / hash-compute error) | Append to the file-backed fallback log (per Notes N-25 / tasks.md:730) and DO NOT block the originating gate decision — audit is a durable side-record, not a new availability gate; emit an OTel warning and mark the chain as needing reconciliation on reconnect. |'. This makes the fail-open-for-availability-vs-file-fallback policy explicit; today every other producer/hook has a failure-mode row but gate_audit_log / audit_log.py appears nowhere in the section.
- _adds EARS/use-case wiring_

**167. Add audit_verify.py design subsection: signature, pseudocode, edge cases, error-handling row**  ·  _[audit_verify.py]_
- **anchor:** | `audit_verify.py` | `tools/audit_verify.py` | 3 | Recomputes the `gate_audit_log` hash chain; on-demand verifier plus the merge-time CI required check (REQ-AUDIT-002/003). |
- **change:** Add a design.md subsection for audit_verify.py (it currently has only the one-line inventory row, the New-components bullet, and the Property 28 statement — no algorithm, no interface, no error-handling row, unlike stop_hook.py/pre_tool_use_hook.py which have full pseudocode). Specify: (1) function signature e.g. `verify_chain(rows) -> Result`; (2) recomputation pseudocode mirroring audit_log.py's canonical form `entry_hash = sha256(canonical_row || prev_hash)` over `(seq, event_name, tool_name, decision, reason, requirement_id, actor_agent, created_at)`, genesis `prev_hash = sha256("")`; (3) explicit EMPTY-chain and GENESIS-ONLY handling (both PASS); (4) a new row in the Hook Failure Modes / Error Handling table (design.md:807-816) for audit_verify.py: 'gate_audit_log unreadable / Postgres down with no file segment -> FAIL verification, non-zero exit (fail closed)' consistent with the never-fail-open rule at tasks.md:730.
- _adds EARS/use-case wiring_

**168. Add a correctness Property and Z3 CHECK pinning REQ-VERIFY-007/008**  ·  _[perf_a11y_verifier (capability in verifier.md)]_
- **anchor:** ### Property 30: Omission-Declaration Gate
- **change:** Every other substantive requirement is pinned by a correctness Property and (where logic-checkable) a Z3 CHECK; REQ-VERIFY-007/008 has neither. grep of formal_verification_merged.py (522 lines) confirms zero references to VERIFY-007/008, perf, a11y, axe, k6, or subtype. Add a new Property (e.g. Property 31: Perf/A11y Evidence Required) stating: 'For any NFR item of subtype performance|accessibility marked proven, a perf/a11y Evidence_Record produced by the perf_a11y_verifier must be attached; for any declared ui-screen item, a render-assertion Evidence_Record must be attached.' Add the matching property-test task under task 39 (e.g. 39.20 -> tests/property/test_perf_a11y.py) and, optionally, a Z3 CHECK modeling 'NFR-subtype-perf proven => perf-evidence-present'. Validates Requirement 25 (REQ-VERIFY-007/008).
- _adds EARS/use-case wiring_

**169. Define how k6/Lighthouse/axe-core outputs map into the four-field Evidence_Record**  ·  _[perf_a11y_verifier (capability in verifier.md)]_
- **anchor:** **Evidence_Record** has exactly four required fields with no additional properties allowed:
- **change:** k6/Lighthouse/axe-core emit reports, not pytest results, yet Evidence_Record requires test_file/test_name/output_hash/collected_at with additionalProperties:false (design.md:387-411). How a Lighthouse JSON or axe violation list maps into these four fields is never defined, so the SubagentStop schema gate may reject valid perf/a11y evidence. Add a canonical mapping to the Verifier/perf_a11y_verifier section: test_file = path to the k6/Lighthouse/axe runner script; test_name = the budget id (or screen/state id for ui-screen render); output_hash = sha256:<hex> of the report artifact; collected_at = run timestamp (ISO-8601). This keeps perf/a11y evidence conformant to the four-field schema and the additionalProperties:false constraint.
- _adds EARS/use-case wiring_

**170. Add a declared-screen/state data structure so REQ-VERIFY-008 has an enumerable source**  ·  _[perf_a11y_verifier (capability in verifier.md)]_
- **anchor:** "definitions": {
    "CoverageItem": {
- **change:** REQ-VERIFY-008 (requirements.md:449) is Ubiquitous: 'every declared screen/state (incl. empty, loading, error) SHALL have a behavioral test asserting it renders' — but no schema field enumerates screens/states; there is no screens/states/ui-declaration structure in the feature_list.json schema, so 'every declared screen/state' is not enumerable and the obligation cannot be checked against a defined declaration set. Add a screen/state declaration structure to the coverage model — e.g. a WIRING/NFR item with subtype 'ui-screen' carrying a `declared_states` array (enumerating at least empty/loading/error/ready) — so the render assertion is enforceable. Note (per critic findings): the render assertion is a BEHAVIORAL test and its actual mechanism is the existing Playwright behavioral layer (design.md:260 / task 15) plus the WIRING integration-test-evidence obligation (Req 8.3 / task 47), NOT a perf/a11y tool — the design subsection should route ui-screen render evidence through Playwright/WIRING, not k6/Lighthouse/axe.
- _adds EARS/use-case wiring_

**171. Add a dedicated perf_a11y_verifier design subsection and expand task 51 into subtasks**  ·  _[perf_a11y_verifier (capability in verifier.md)]_
- **anchor:** ## Sandbox & Worktree Isolation (REQ-17.4)
- **change:** Across all three canonical files the capability appears in only 4 design.md lines (146, 255/260-262 area, 1182), 1 tasks.md line (966), and 3 requirements criteria — with no interface signature, no permission delta, no dispatch logic, no CI step, no test, and no threshold ownership. Add a dedicated design.md subsection (modeled on the 'Sandbox & Worktree Isolation' subsection) for perf_a11y_verifier covering: subtype dispatch (perf|accessibility|ui-screen), tool invocation (k6/Lighthouse, axe-core, Playwright for render), Evidence_Record mapping, threshold registry entries + owner (p95 latency / Core Web Vitals budgets, zero WCAG-A/AA), CI wiring, and the Property/test. Expand task 51 into subtasks (51.1 perf, 51.2 a11y, 51.3 UI-screen render, 51.4 property test). This is the umbrella edit; the sub-points are covered by the subtype/evidence-mapping/CI/property/ui-screen edits and should be folded into this one subsection.

**172. Add a dedicated design subsection for secrets-scan.yml (currently one inventory sentence only)**  ·  _[secrets-scan.yml]_
- **anchor:** Component inventory row at line 147 (secrets-scan.yml has exactly one sentence and no dedicated subsection, unlike coverage_query.rego, the hooks, and the audit log)
- **change:** Add a short design subsection for secrets-scan.yml specifying: (a) `on: pull_request` trigger (and whether push to protected branches is also scanned); (b) the gitleaks action/version and config/allowlist path; (c) baseline/allowlist strategy for false positives; (d) the report/SARIF artifact contract; (e) scope = the PR diff vs whole git history; and (f) explicit clarification that REQ-17.2's 'block the commit' clause is satisfied at merge in CI (a CI workflow cannot block a local commit), with the commit-side/pre-emission half delegated to the PreToolUse path (REQ-17.5). This also subsumes the diagram-absence observation: the CI architecture subgraph (design.md:50-55) draws only OPA/SAST/SLSA and equally omits zap-baseline, deepeval-gate, and audit-chain-verify, so the subgraph is illustrative and the inventory+subsection is the canonical home for this gate.

**173. No dedicated design subsection or task-54 subtasks for the omission guard (thin coverage vs the Evidence_Record sibling); audit field-content for the omission decision undefined**  ·  _[omission_guard (capability in subagent_stop_hook.py)]_
- **anchor:** Only artifacts for this capability: component row design.md:148, wiring row design.md:218, Property 30 prose design.md:1124-1130, one build-order line design.md:627. Contrast: Evidence gate has task 8 (8.1/8.2), a build-order test, and round-trip tests
- **change:** Add a design subsection 'SubagentStop omission-declaration gate' specifying: input payload shape (the subagent result JSON), the null/absent(/empty) predicate, the exact stderr message naming `omission_declaration`, allow/block audit behavior, and which audit fields the omission block records — i.e. the requirement_id value (REQ-SPEC-018, since the gate is a structural presence check not tied to a single REQ) and the actor_agent value (the subagent whose result is rejected), closing the REQ-AUDIT-001 seven-field ambiguity for this decision. In tasks.md, add task-54 subtasks mirroring 8.1/8.2 (54.1 implement the omission guard in subagent_stop_hook.py; 54.2 unit tests: null→exit 2 naming omission_declaration, present→exit 0).

**174. Add a fail-mode row for deepeval-gate to the Hook/gate Failure Modes table**  ·  _[deepeval_gate.py]_
- **anchor:** | `spec_validator.py` Z3 timeout | Returns `{violation_count: -1, error: "validator_timeout"}`. Stop hook treats `violation_count < 0` as a blocking error. DEFAULT timeout: 60 seconds. |
- **change:** Add a row to the Error Handling / failure-modes table defining deepeval-gate's behavior on evaluator-LLM failure (API timeout, rate-limit, unavailability, non-determinism/flake): specify fail-closed vs fail-open and any retry/flake mitigation. Every other gate with a non-trivial failure mode (spec_validator Z3 timeout, Postgres unavailable, hook crashes) has an explicit fail-closed row; a REQUIRED merge gate whose judge-LLM can time out has no defined fail behavior.

**175. Define the v1 baseline DAST scan target**  ·  _[zap-baseline.yml]_
- **anchor:** Component-inventory row, design.md:150 `| zap-baseline.yml | .github/workflows/zap-baseline.yml | 2 | OWASP ZAP baseline passive scan ... |` (no target specified anywhere in canonical artifacts)
- **change:** Add a design.md detail (in the inventory row note or a CI-workflows subsection) defining WHAT ZAP scans for v1: how the app is stood up in CI (e.g. spin up the app against a known localhost URL, or scan a deployed preview/ephemeral environment) and how the action's target/URL input is populated. Without a defined target the baseline scan cannot run and task 56 is not implementable as written. Note the rationale at requirements.md:490 ('active scan added post-v1 when an authenticated target exists') implies even the baseline target is currently unsettled — pin the baseline (unauthenticated) target explicitly.

**176. Add zap-baseline (and deepeval_gate, flagd, omission_guard) to the design.md 'New / changed components' Phase-2 reconciliation list**  ·  _[zap-baseline.yml]_
- **anchor:** '### New / changed components' list, design.md:1179-1185 (enumerates state_integrity, audit_log/audit_verify, perf_a11y_verifier, REASONING span, research fact-check, optional role subagents — OMITS the four P3 CI/feature-flag additions)
- **change:** Add bullet entries to the 'New / changed components' list (line 1179) for the four Phase-2 P3 additions that are present in the component-inventory table (design.md:148-151) but missing from this reconciliation list: `zap-baseline.yml` (Phase 2, OWASP ZAP baseline passive scan, trigger/target/fail_action semantics, ruleset registration, REQ-SEC-008); `deepeval_gate.py` (REQ-EVAL-001); `flagd` kill-switch (REQ-CTRL-001); `omission_guard` (REQ-SPEC-018). At minimum add zap-baseline with a one-line description of its trigger, scan target, fail_action semantics, report handling, and ruleset registration, parallel to the audit_log/audit_verify treatment at design.md:1181.

**177. flagd kill-switch has no enforcement-mechanism design subsection**  ·  _[flagd]_
- **anchor:** Component Inventory row: | `flagd` | `flagd/` (self-hosted service, Apache 2.0, CNCF Incubating) | 2 | OpenFeature + flagd agent kill-switch; near-real-time flag updates (≤ 30 s polling) without process restart; self-hosted, no SaaS dependency (REQ-CTRL-001).
- **change:** Author a new design subsection 'Agent Kill-Switch (REQ-CTRL-001)' parallel to the 'Sandbox & Worktree Isolation (REQ-17.4)' subsection (design.md:514). It MUST specify: (a) ENFORCEMENT POINT — each agent entry point (initializer/implementer/verifier subagents) queries the OpenFeature client at start-of-turn; a set kill flag causes the gated capability to hard-refuse, analogous to a PreToolUse exit-2 block; define 'disabled' operationally (refuse the tool call / skip the capability). (b) FLAG SCHEMA — enumerate the kill-switch flag name(s) and the agent capability each gates. (c) PROVIDER CHOICE — pick a default (gRPC streaming for true near-real-time, OR file-based with an explicit poll interval ≤30 s) and tie the ≤30 s propagation guarantee to that provider's mechanism. (d) FAIL-CLOSED behavior when flagd is unreachable (see separate edit). (e) AUDIT/OTel wiring on every toggle (see separate edit). Also add a Hook-Wiring-Table row or equivalent showing where the flag is checked.
- _adds EARS/use-case wiring_

**178. Name the persisted compiled-spec artifact for Requirement 1.3**  ·  _[initializer.md]_
- **anchor:** **Permissions:** Read/write to spec artifacts, `feature_list.json`, domain-baseline checklists. No access to `tests/`, CI config, or implementation source.
- **change:** In the initializer Subagent Definition (design.md:226-238), name the concrete on-disk artifact that satisfies Requirement 1.3 ('persist compiled requirements as version-controlled artifacts, not model context alone'). feature_list.json is the coverage model, not the requirements prose. State the path/format the initializer writes (e.g. `.kiro/specs/<feature>/requirements.md` and/or a machine-readable `specs/<feature>/spec.json`) and reference it as the 'spec artifact' input contract of spec_validator.py (design.md:533, which currently refers to a 'spec artifact' with no named on-disk location).
- _adds EARS/use-case wiring_

**179. Clarify feature_list.json ownership: initializer vs tools/feature_list_init.py and artifact-guard exemption**  ·  _[initializer.md]_
- **anchor:** - Writes `feature_list.json` with all items defaulting to `unproven`
- **change:** Resolve the ownership split for feature_list.json creation. design.md:234 says the Initializer 'Writes feature_list.json' while tasks.md task 12 (tools/feature_list_init.py) owns empty-model initialization. Add a clause: the Initializer CALLS `tools/feature_list_init.py` to seed an empty, schema-valid file (schema_version, product_class, checklist_ref, empty items[]) and then populates items. Also state how this reconciles with the PreToolUse artifact/status guards (design.md:214-215) that protect feature_list.json — i.e. whether the initial seed write is exempt from the artifact guard (the guard protects the schema and identity-mutation, not the initial creation) so the producer is not blocked by its own gate.
- _adds EARS/use-case wiring_

**180. Expand design implementer.md section with concrete frontmatter + tool allowlist + smoke test**  ·  _[implementer.md]_
- **anchor:** #### `.claude/agents/implementer.md` — Coding Subagent
- **change:** The section is ~10 prose lines (design.md:240-251) for a load-bearing spine component while each hook gets a full code block. Add: (1) the exact agent-spec frontmatter (name, description, model, and a tools/allowed-tools allowlist that realizes 'worktree only' — Bash for `git worktree add`, Write/Edit confined to the slice worktree, NO access to tests/, schema/, CI, or other worktrees); (2) the handoff protocol to the Verifier (implementer returns with no Evidence_Record; Verifier is invoked separately); (3) a smoke test analogous to tests/smoke/test_hook_config.py that parses the agent markdown and asserts its permission scope. Add the matching smoke-test task under tasks.md task 10.2.

**181. Highest-priority selection predicate is underspecified (no tie-break, no dependency respect)**  ·  _[implementer.md]_
- **anchor:** - Reads `feature_list.json` to identify the single highest-priority `unproven` item
- **change:** 'highest-priority unproven item' (design.md:245, tasks.md:210) is ambiguous: priority is integer (1=highest, design.md:336-339) with no tie-break among equal-priority items, and CoverageItem.dependencies (design.md:354) is ignored — an item could be highest-priority yet have unproven dependencies. Specify the selection predicate in implementer.md: 'select the lowest priority integer among unproven items whose dependencies are ALL proven; break ties by item id lexical order.' Cite the dependencies field so dependency-respecting selection is explicit.

**182. Specify artifact-guard carve-out allowing the Verifier to write tests/**  ·  _[verifier.md]_
- **anchor:** PreToolUse artifact guard row (design.md:214, protects `tests/`); Implementer denied tests/ (design.md:251); Verifier read/write tests/ (design.md:267)
- **change:** The artifact guard (design.md:214) blocks any tool targeting tests/ as a protected artifact and the Implementer is denied tests/ writes (design.md:251), yet the Verifier has read/write on tests/ (design.md:267) — with no exception path defined for how the Verifier writes through the guard. Specify the carve-out: key the artifact guard on actor_agent (allow verifier.md test writes) OR restrict the guard to test-file deletion/rewrite vs addition, while still blocking the Implementer and destructive ops. Add the qualifier to the design.md:214 artifact-guard row and the design.md:267 Verifier permissions.
- _adds EARS/use-case wiring_

**183. Add Verifier frontmatter (tools allowlist / model / description) to design**  ·  _[verifier.md]_
- **anchor:** Subagent Definitions §`.claude/agents/verifier.md` (design.md:253-267); 'following the Claude Code agent spec' (design.md:224)
- **change:** design.md:224 says each subagent follows the Claude Code agent spec (which requires name/description/tools/model frontmatter), but the verifier.md section gives only prose permissions — no concrete tools allowlist, model, or description. Permission scoping in Claude Code is enforced via the tools allowlist, not prose, so 'no write access to implementation' is currently unenforceable as written. Add the verifier.md frontmatter to the design: explicit tools allowlist (Read, Bash for test runners, the evidence_collector tool; NO Write/Edit/MultiEdit on src/), model selection, and a description. Mirror into tasks.md task 10.3 (anchor: tasks.md:213-216).

**184. Name a concrete owner for the Property 29 fact-check / authority-tier enforcement**  ·  _[research.md]_
- **anchor:** ### Property 29: Research-Claim Authority Labeling ... **Validates: Requirements 24 (REQ-SPEC-017). Test mechanism: fact-check / authority-tier-labeling pass (no Z3 check — test/runtime verified).** (design.md:1114-1120)
- **change:** REQ-SPEC-017's 'independent fact-check pass' and 'authority-tier labeling' have NO concrete executable owner: no fact_check.py / authority_labeler / research_claim_validator appears in the Component Inventory (design.md:124-155) or any task — only a property test (39.19) and a prose behavior. Add a named component (e.g. `tools/research_claim_validator.py` or a SubagentStop-side `research_epistemic_guard` capability) to the Component Inventory table and the 'New / changed components' list (design.md:1184), and cite it from Property 29 and task 50. As written, REQ-SPEC-017 has an enforcing test but no enforcing runtime mechanism.
- _adds EARS/use-case wiring_

**185. Specify the DB-row producer for domain_baseline_checklists vs research.md baselines/-only scope**  ·  _[research.md]_
- **anchor:** -- domain_baseline_checklists: version history of product-class checklists ... CREATE TABLE domain_baseline_checklists (design.md:498-509)
- **change:** No actor is specified for INSERTing the `domain_baseline_checklists` row (the persisted record of research.md's output, carrying sha/version/approved_at/approved_by). research.md has `baselines/`-only filesystem scope and no DB write (design.md:280), so it cannot write Postgres under its current scope. Name the producer — the human-approval action or a SessionStart/PostToolUse hook that records the row on approval — and reconcile it against research.md's permission scope. Add this to the Domain-Baseline Checklist Lifecycle section (design.md:609-613) and/or the checklist-approval guard row (design.md:213).
- _adds EARS/use-case wiring_

**186. Define the per-subagent research-output JSON schema and checklist file-format schema**  ·  _[research.md]_
- **anchor:** Component Inventory + Data Models sections (no research-output schema artifact; design.md:124-155, 286-510)
- **change:** REQ-SPEC-018.3 (requirements.md:478) requires 'THE JSON schema for each subagent output type SHALL include omission_declaration', but no per-subagent output JSON schema artifact for research is named anywhere (only feature_list.schema.json exists), and no checklist file-format schema defines what a `baselines/*.md` must contain (per-item fields, per-claim source/authority-tier/fact-check structure). Add: (1) a research-output JSON schema (e.g. `schema/research_output.schema.json`) carrying a required non-nullable `omission_declaration`, and (2) a checklist-item schema specifying claim/source/authority-tier fields — and cite both from task 10.4 and task 54. Without a defined output format, REQ-SPEC-014/017/018.3 cannot be machine-validated.
- _adds EARS/use-case wiring_

**187. Define the 'new product class' detection mechanism and owning component**  ·  _[research.md]_
- **anchor:** **Lifecycle rules:** 4. The Research_Sub_Agent is triggered only once per product class ... Re-research is triggered only by an explicit operator request. (design.md:613)
- **change:** Req 3.1 says research runs 'WHEN a new product class is encountered for the first time' and design.md:613 says 'only once per product class', but neither defines HOW first-encounter / newness is DETECTED or which component decides. Add: the Initializer detects newness via a lookup against `domain_baseline_checklists` by `product_class` (no approved row for that class ⇒ new ⇒ trigger research); cite this from the research.md trigger behavior (design.md:274 'Triggered by the Initializer when a product class has no existing checklist') and the lifecycle rules.
- _adds EARS/use-case wiring_

**188. Add 006 migration filename to the domain_baseline_checklists CREATE TABLE block in design**  ·  _[research.md]_
- **anchor:** -- domain_baseline_checklists: version history of product-class checklists (design.md:498)
- **change:** The migration index for `domain_baseline_checklists` (006) is canonical only in tasks.md (task 28.6, `db/migrations/006_domain_baseline_checklists.sql`). design.md's component table at line 140 explicitly lists the 007 and 008 migration rows but omits the 006 row, and the CREATE TABLE block at design.md:498-509 names no migration file. Add the migration filename `006_domain_baseline_checklists.sql` as a comment on the CREATE TABLE block (and/or an explicit row in the design.md:140 migration list) so the 006 index is canonical in design, not only in tasks. NOTE: there is NO numeric collision — 006=domain_baseline_checklists, 007=requirement_versions, 008=gate_audit_log are consistent across both files; this is a documentation-completeness fix only.


## tasks.md — 120 edits

### 🔴 DEFECT (19)

**189. Fix Property 24 PBT field-name from acting_agent to actor_agent**  ·  _[feature_list.json]_
- **anchor:** task 39.13 PBT body, tasks.md:655
- **change:** design.md:473 declares 'Canonical name is actor_agent everywhere (no acting_agent)' and Property 24 (design.md:1068) uses actor_agent, but the task 39.13 PBT asserts on `acting_agent` ('Generate evidence chains with arbitrary acting_agent identities; assert any Evidence_Record where acting_agent == "implementer.md"...'), so the Implementer-cannot-self-verify test checks a field the design says does not exist. Replace both occurrences of `acting_agent` with `actor_agent` in tasks.md:655.

**190. Add resume_integrity_ok (and violation_count, retry_count) to migration task 28.5**  ·  _[session_start_hook.py]_
- **anchor:** Task 28.5 'Write migration db/migrations/005_run_state.sql' — bullet 'run_state table with status CHECK {running,complete,handoff,blocked}, stop_hook_active boolean, no_progress_n counter'; _Requirements: 11.1, 16.1_ (tasks.md:441-443)
- **change:** The design run_state SQL (design.md:481-496) declares resume_integrity_ok BOOLEAN, violation_count, and retry_count, but task 28.5 lists ONLY status CHECK + stop_hook_active + no_progress_n, so the durable column SessionStart is spec'd to write is un-tasked. Add 'resume_integrity_ok BOOLEAN (REQ-STATE-005; written by SessionStart, read by the PreToolUse integrity guard), violation_count INTEGER DEFAULT 0, retry_count INTEGER DEFAULT 0' to the column list and add REQ-STATE-005 to the _Requirements:_ line.
- _adds EARS/use-case wiring_

**191. Fix non-canonical field name acting_agent in Property 24 PBT (task 39.13)**  ·  _[subagent_stop_hook.py]_
- **anchor:** Task 39.13 'Generate evidence chains with arbitrary `acting_agent` identities; assert any Evidence_Record where `acting_agent == "implementer.md"` is rejected' (tasks.md:655)
- **change:** Rename `acting_agent` to `actor_agent` in task 39.13 (both occurrences). design.md:473 declares 'Canonical name is actor_agent everywhere (no acting_agent)' and Property 24 (design.md:1068) uses `actor_agent`, but the validating PBT in tasks.md:655 still generates and rejects on the non-canonical `acting_agent` key — so the role-separation test would read the wrong field and fail to enforce the gate. This is a live naming-drift defect.

**192. collect() must emit timezone-aware ISO-8601, not datetime.utcnow()**  ·  _[evidence_collector.py]_
- **anchor:** Task 3.1, tasks.md:45 — 'capture ISO-8601 `collected_at` from `datetime.utcnow()`'
- **change:** Change the collect() spec at tasks.md:45 from `datetime.utcnow()` to `datetime.now(timezone.utc).isoformat()` (timezone-aware, emits a '+00:00' offset). Rationale to add inline: the EvidenceRecord schema requires `format: date-time` (RFC 3339) at design.md:407 and the `evidence_records.collected_at` column is `TIMESTAMPTZ NOT NULL` (design.md:472) with an `evidence_complete` CHECK requiring `collected_at IS NOT NULL` (design.md:476). A naive utcnow() string has no offset, can fail strict date-time validation, and would be coerced to a different instant on Postgres insert — breaking Property 19's 'all four fields shall match the original capture values without loss' (design.md:1028). Mirror the same correction in the unit-test task 3.3 (tasks.md:56-61) so a test asserts the emitted timestamp carries an offset.

**193. validate_evidence_record() must validate output_hash/collected_at formats, not just presence**  ·  _[evidence_collector.py]_
- **anchor:** Task 3.1, tasks.md:46 — 'validate_evidence_record(record: dict) -> bool that checks all four fields present and non-empty'
- **change:** Strengthen validate_evidence_record() at tasks.md:46 to validate `output_hash` against the anchored pattern `^sha256:[a-f0-9]{64}$` and `collected_at` as a parseable ISO-8601/RFC-3339 timestamp (or run jsonschema against the EvidenceRecord definition, design.md:387-411), not merely presence+non-emptiness. Reason: the SubagentStop hook calls this same validator (tasks.md:179), so a non-empty but malformed output_hash (uppercase hex, missing `sha256:` prefix, wrong length) currently passes both the validator and the SubagentStop gate (only the separate JSON-Schema path in the PreToolUse status guard catches it). This also closes the requirements.md:521 deferral rationale, which justifies skipping Pydantic/BAML runtime enforcement by asserting 'the SubagentStop schema-validation gate (REQ-COV-006) already enforces Evidence_Record structure at the hook layer' — a claim that is false while tasks.md:46/179 wire the hook to a presence-only validator. Cite design.md:402 (canonical sha256 pattern).

**194. Fix task 26.1 _Requirements mapping to include 6.3 (the only task that runs orphan_detector)**  ·  _[orphan_detector.py]_
- **anchor:** Task 26.1 _Requirements line (tasks.md:410): '_Requirements: 8.1, 8.2, 9.4, 10.3, 13.2_'
- **change:** Add `6.3` to the _Requirements line of task 26.1. Task 26.1 (tasks.md:404-410) is the ONLY task whose body actually runs orphan_detector.py ('run orphan_detector.py; assert exit non-zero and report contains both orphan types', tasks.md:406) yet its mapping omits 6.3. Conversely task 33.1 lists 6.3 (tasks.md:507) but its body never invokes orphan_detector — it tests parse_commit_trailers/migrations/evidence/run-state only. The spec's own forward 6.3->task link points at the wrong task. Add 6.3 to 26.1; optionally drop 6.3 from 33.1's mapping (or keep for parse_commit_trailers' 6.x coverage but note 26.1 owns the orphan-detection assertion).
- _adds EARS/use-case wiring_

**195. Make the Rego deny rule honor in_scope, so it matches the Stop gate**  ·  _[coverage_query.rego]_
- **anchor:** Task 21.1 — 'policy reads `items[_].status == "unproven"` and denies if any found' (tasks.md:355)
- **change:** Amend task 21.1 (and Property 22 in design.md:1052) so the policy filters `input.items` on `in_scope == true` before applying the status/evidence checks, mirroring evaluate_stop's `in_scope_items = [i for i in feature_list.items if i.in_scope]` (design.md:690) and Requirement 5.7 (requirements.md:128). Without this filter a naive `items[_].status == "unproven"` rule blocks merge on an out-of-scope unproven item, diverging from the Stop gate. State: 'deny rules iterate only over items where in_scope == true.'

**196. Close the fail-open hole on `failed` status: deny if status != proven**  ·  _[coverage_query.rego]_
- **anchor:** Task 21.1 — 'policy reads `items[_].status == "unproven"` and denies if any found' (tasks.md:355)
- **change:** Change the deny rule in task 21.1 (and Property 22) from 'deny if status == "unproven"' to 'deny if status != "proven"' (anything not exactly proven blocks), so a `failed` in-scope item also blocks merge. The status enum is {unproven, proven, failed} (design.md:372) but the policy only references unproven/proven, leaving a `failed` item to silently pass a status==unproven rule. This mirrors the Stop hook's `not_proven = [i for i in in_scope_items if i.status != "proven"]` logic (design.md:731).

**197. Fix the forward-reference defect: 'deferred to Phase 1 task 20' should be task 21**  ·  _[coverage_query.rego]_
- **anchor:** Task 14.1 — 'full Postgres-backed policy deferred to Phase 1 task 20' (tasks.md:265)
- **change:** Change 'deferred to Phase 1 task 20' to 'deferred to Phase 1 task 21' in task 14.1. Task 20 (tasks.md:338) is `orphan_detector.py`, NOT the coverage policy; the full coverage_query.rego policy is task 21 (tasks.md:352). The build chain currently misroutes the component's own forward reference to the wrong task.

**198. REQ-COV-007 requirement citations inconsistent across tasks 28.7, 39.15, and 48**  ·  _[db/migrations/007_requirement_versions.sql]_
- **anchor:** - [ ] 48. Requirement-amendment versioning (**REQ-COV-007**) — `requirement_versions` table (migration `db/migrations/007_requirement_versions.sql`, task 28.7); on amendment re-enter `unproven`; block COMPLETE while any amended item un-reproven. Only `*→proven` requires a complete Evidence_Record. Property 25 (test in task 39.15). _Z3 CHECK-10a/10b._
- **change:** Standardize all REQ-COV-007 task citations to Requirement 22. (a) Append a '_Requirements: 22 (REQ-COV-007)_' line to the currently-uncited parent task 48 (tasks.md:962, which ends at '_Z3 CHECK-10a/10b._' with no _Requirements:_ line). (b) Fix task 28.7's '_Requirements: 16.1 (REQ-COV-007)_' (tasks.md:451) — Req 16 is REQ-STORE-001..003 durable storage; change to '_Requirements: 22 (REQ-COV-007); 16.1 (durable storage)_'. (c) Fix task 39.15's '_Requirements: 5.2 (REQ-COV-007)_' (tasks.md:667) — Req 5 is the coverage model; change to '_Requirements: 22 (REQ-COV-007)_'.

**199. Make append-only DB-enforced in the 008 DDL (REVOKE + trigger), not advisory**  ·  _[db/migrations/008_gate_audit_log.sql]_
- **anchor:** tasks.md:454 (task 28.8 bullet) `- \`gate_audit_log\` append-only table: \`seq\` (monotonic) ...` and requirements.md:457 (Req 27.1) / design.md:1156-1167 DDL
- **change:** append-only is asserted (Req 27.1 requirements.md:457; DDL header design.md:1155) but never enforced — no REVOKE, no trigger, no rule, so a superuser/agent can silently UPDATE/DELETE rows and tamper-evidence is detection-only. Add to task 28.8 and the design.md DDL block (design.md:1156-1167): `REVOKE UPDATE, DELETE ON gate_audit_log FROM <app_role>;` plus a `BEFORE UPDATE OR DELETE` trigger that RAISEs an exception. Update requirements.md:457 to state append-only is DB-enforced (REVOKE + trigger), not merely advisory.
- _adds EARS/use-case wiring_

**200. Fix acting_agent → actor_agent naming-canon violation in Property 24 test task**  ·  _[audit_log.py]_
- **anchor:** Line 655 (task 39.13): 'Generate evidence chains with arbitrary acting_agent identities; assert any Evidence_Record where acting_agent == "implementer.md" is rejected'
- **change:** Replace both occurrences of 'acting_agent' on line 655 with 'actor_agent' to honor the canonical field name declared at design.md:473 ('Canonical name is actor_agent everywhere (no acting_agent).'). audit_log.append's signature and the evidence_records DDL both use actor_agent, so a test keying on acting_agent would silently mismatch.

**201. Flag CI build-order inversion: audit-chain-verify (wave 33) runs verifier built at wave 42**  ·  _[audit_verify.py]_
- **anchor:**     {
      "id": 33,
      "tasks": ["40.1", "40.2", "40.3"]
    },
- **change:** Resolve the dependency-graph ordering contradiction: wave id:33 (tasks.md:875) schedules task 40.3 (`.github/workflows/audit-chain-verify.yml`, which executes `python3 tools/audit_verify.py`) NINE waves before wave id:42 (tasks.md:911) builds task 52.2 (`tools/audit_verify.py`). The required CI check is wired to invoke a tool that does not yet exist at its scheduled wave. Either move 40.3 into a wave at/after wave 42, or split 40.3 so the workflow file is created early but its required-check registration/enforcement is gated until after task 52.2 lands. (Same class of inversion exists for 40.2/secrets-scan vs task 45, worth checking in the same pass.)
- _adds EARS/use-case wiring_

**202. Fix migration 008 requirement traceability: 16.1 -> Requirement 27 / REQ-AUDIT**  ·  _[audit_verify.py]_
- **anchor:**     - _Requirements: 16.1 (REQ-AUDIT-001..003)_
- **change:** Change task 28.8's '_Requirements: 16.1 (REQ-AUDIT-001..003)_' to '_Requirements: 27 (REQ-AUDIT-001..003)_' (or '16.1, 27' if the generic Postgres-persistence obligation is intentionally retained). Requirement 16.1 (requirements.md:296) is 'persist requirements, coverage state, traceability links, Evidence_Records, and run state in a managed Postgres' — wholly unrelated to the audit log. The `gate_audit_log` table audit_verify.py reads maps to Requirement 27 / REQ-AUDIT. Same mis-tag class as the '12.x' on tasks.md:703.

**203. Remove the mis-cited Property 23 / coverage.json clause from task 51**  ·  _[perf_a11y_verifier (capability in verifier.md)]_
- **anchor:** Validates Property 23 line-coverage figures via **pytest-cov** (coverage.py) read from `coverage.json`. _Requirements: REQ-VERIFY-007/008 (was "9.x" — corrected 2026-06-15)._
- **change:** Task 51 (tasks.md:966) claims perf_a11y_verifier 'Validates Property 23 line-coverage figures via pytest-cov ... coverage.json'. Property 23 (design.md:1058-1062) validates Req 9.6 (line-coverage threshold), a wholly distinct concern from REQ-VERIFY-007/008 — this conflates two requirements and gives perf/a11y a borrowed, wrong test reference. Delete the 'Validates Property 23 line-coverage figures via pytest-cov (coverage.py) read from coverage.json' sentence from task 51 and replace it with a reference to the dedicated perf/a11y Property and its test file (e.g. 'Validates Property 31 (perf/a11y evidence) via tests/property/test_perf_a11y.py').

**204. No Property-30 test subtask; task-39 header says '29 properties' while line 717/979 say 30 (count mismatch)**  ·  _[omission_guard (capability in subagent_stop_hook.py)]_
- **anchor:** Task 39 header (tasks.md:569): '- [ ] 39. Complete PBT suite — all 29 correctness properties ...' vs task 42.1 (tasks.md:717) 'Confirm all 30 properties pass' vs tasks.md:979 'extend the PBT suite to 30' listing Property 30; and task 54 (tasks.md:974) 'Property 30 test in subtask' with NO 54.x subtask and NO 39.20
- **change:** Reconcile the task-39 header from 'all 29 correctness properties' to 'all 30 correctness properties' (Properties 1–24 + 25–30) to match tasks.md:717, tasks.md:979-980, and requirements.md:514. Add a concrete test subtask for Property 30 — e.g. 39.20 `tests/property/test_omission.py` (Property 30, CHECK-13a/13b, asserts null/absent omission_declaration → exit 2; present → accepted) OR a 54.x subtask — since task 54 references a Property-30 test 'in subtask' that does not exist (the PBT block 39.15–39.19 covers Properties 25–29 only). Also fix the same '29' wording in the tasks.md:732 Notes line.

**205. Fix harness invocation path: spec assumes repo-root but file lives in verification/**  ·  _[initializer.md]_
- **anchor:**     - Install `z3-solver` via pip; run `python3 formal_verification_merged.py`
- **change:** On disk the harness is at `verification/formal_verification_merged.py`, but design.md:632 and tasks.md:252/258/301 invoke it as repo-root `python3 formal_verification_merged.py`, so CI task 13.1 and smoke task 13.2 would fail as written (file-not-found). Update every invocation to `python3 verification/formal_verification_merged.py` (tasks.md lines 252, 258, 301; design.md line 632), or add a root shim. Also confirm the DEPRECATED `docs/superseded/formal_verification.py` is never invoked.

**206. Author the absent implementer.md spine artifact (task 10.2 unbuilt)**  ·  _[implementer.md]_
- **anchor:** - [ ] 10.2 Write `.claude/agents/implementer.md` — Coding Subagent
- **change:** implementer.md does not exist on disk (.claude/agents/ holds only a 0-byte .gitkeep), yet it is Phase-0 spine component #8 (design.md:630) with acceptance test 'Creates worktree; commits with requirement ID in trailer'. Keep task 10.2 unchecked but make it buildable: add an explicit sub-bullet stating the file MUST be authored per the Claude Code agent spec with YAML frontmatter (name: implementer, description, model, tools/allowed-tools allowlist) before any Phase-0 smoke (task 16.1) or e2e loop can run. Cross-link design.md:240-251 and the spine acceptance row design.md:630.
- _adds EARS/use-case wiring_

**207. Fix acting_agent→actor_agent field name in Property 24 PBT task**  ·  _[verifier.md]_
- **anchor:** Task 39.13: "Generate evidence chains with arbitrary `acting_agent` identities; assert any Evidence_Record where `acting_agent == \"implementer.md\"` is rejected"
- **change:** Replace both occurrences of `acting_agent` on tasks.md:655 with `actor_agent` so the Property 24 (Subagent Role Separation) test targets the canonical evidence_records/gate_audit_log column. design.md:473 states 'Canonical name is actor_agent everywhere (no acting_agent)'; the test currently asserts against a field name the schema forbids.

### 🟠 INCONSISTENCY (32)

**208. Resolve Req 4.3 strict-decrease mechanism: scalar violation_count has no prior-pass comparator in evaluate_stop**  ·  _[stop_hook.py]_
- **anchor:** task 5.1 _Requirements_ line 102 `_Requirements: 4.2, 4.3, 4.4, 10.2, 10.4, 14.1, 14.2_`
- **change:** Req 4.3 (strict-decrease no-progress at spec level → HANDOFF) is listed under stop_hook task 5.1, but evaluate_stop only reads the scalar `run_state.violation_count` and has no access to the PRIOR pass's count to compute strict-decrease (design.md:837-840 describes it as the spec-completion loop's responsibility, NOT the Stop hook's). Either (a) remove 4.3 from task 5.1's _Requirements_ list (the strict-decrease check lives in the spec-completion loop / Initializer, design.md:832-840), OR (b) add a `prev_violation_count` column to the run_state DDL (design.md:481-496) and implement the pass-over-pass comparison inside evaluate_stop. Recommend (a) to keep the Stop hook a pure scalar gate.

**209. Fix task 6.4 property-test acceptance to assert the full widened status-edge set**  ·  _[pre_tool_use_hook.py]_
- **anchor:** Task 6.4, line 'assert all are blocked; assert only `status: proven` writes are allowed' (tasks.md:154)
- **change:** Rewrite the acceptance line to: 'assert the permitted status edges {unproven→proven (requires complete Evidence_Record), unproven→failed, failed→unproven} are allowed and all other transitions (identity mutations, into-proven without evidence, any edge outside the set) are blocked.' The current 'only status: proven writes are allowed' would fail a legitimate unproven→failed / failed→unproven write and contradicts requirements.md:123/129, design.md:215, and Property 3 (design.md:900).

**210. Resolve REQ-AUDIT-001 Phase-0 vs Phase-2 audit-producer wiring split for pre_tool_use_hook.py**  ·  _[pre_tool_use_hook.py]_
- **anchor:** Task 6 (tasks.md:130-137); cross-ref design.md:210 (plan-gate row says it 'calls audit_log.append(...)') and requirements.md:460; producer-wiring lands only in task 52.3 (tasks.md:970)
- **change:** Add an explicit note to task 6 that audit-log production (audit_log.append on every allow/block) is deferred to task 52.3 (Phase 2), and correspondingly soften the design.md:210 plan-gate row so it does not present audit production as an intrinsic Phase-0 capability of the PreToolUse plan gate. As written, design.md:210/217 describe a Phase-0 audit-producer behavior that no Phase-0/1 task delivers (audit_log.py and its hook wiring are Phase 2), so REQ-AUDIT-001 is unmet for the whole Phase-0/1 window with no acknowledgement of the deferral.

**211. Pin the SessionStart stdout contract so the two phase-0 tests assert the same fidelity**  ·  _[session_start_hook.py]_
- **anchor:** Task 7.1 'Inject combined summary into Claude Code context via stdout' (tasks.md:167); smoke test 16.1 'stdout contains git/progress/coverage summary' (tasks.md:286); integration test 17.1 'stdout JSON contains git_status, unproven_count, proven_count' (tasks.md:298)
- **change:** Task 7.1 says 'inject combined summary via stdout' without pinning the output shape, so 16.1 asserts a free-text 'contains' summary while 17.1 asserts structured JSON fields (git_status, unproven_count, proven_count) — two different fidelities for the same hook output. Pin the contract in 7.1 (emit structured JSON with named fields git_status, unproven_count, proven_count) and align 16.1 to assert the same JSON fields (or explicitly state 16.1 is the looser smoke check by design), so the output contract is single-sourced.

**212. REQ-OBS-003 gate-decision forwarding mis-fits PostToolUse (it makes no allow/block decision)**  ·  _[post_tool_use_hook.py]_
- **anchor:** Task 36.1 (tasks.md:539-542): "Extend all six hooks (Stop, PreToolUse, PostToolUse, SubagentStop, SessionStart, PreCompact) to emit OTel gate-decision events" with attribute `decision` (allow/block)
- **change:** PostToolUse never makes an allow/block gate decision (it always exits 1, non-blocking), but task 36.1 sets a `decision`(allow/block) attribute on its span for 'all six hooks' and REQ-OBS-003 (requirements.md:236) is triggered only 'WHEN a hook makes a gate decision'. Fix: in task 36.1 (and the design narrative) emit a distinct span event for PostToolUse — e.g. span name 'hook.feedback' with attributes `hook.event`, `tool.name`, `findings_count` (and severity breakdown) — instead of reusing the allow/block `decision` attribute that is meaningless for a non-gating hook. Clarify that REQ-OBS-003's gate-decision forwarding applies to the five gate-making hooks; PostToolUse forwards informational feedback telemetry, not an audit-logged gate decision.
- _adds EARS/use-case wiring_

**213. Reconcile the checkpointed artifact set across the three specs (and remove the orphan snapshot)**  ·  _[pre_compact_hook.py]_
- **anchor:** tasks.md:392-394 (task 25.1 bullets: append claude-progress.txt; write claude-progress-evidence-snapshot.json; `git add -f ... && git commit`) vs design.md:220 (PreCompact row 'Checkpoints claude-progress.txt, current evidence state, and feature_list.json') vs requirements.md:219-220
- **change:** Make the file list identical in tasks.md:392-394 and design.md:220, and ensure every written file has a reader. As written, tasks.md commits claude-progress.txt + claude-progress-evidence-snapshot.json but NOT feature_list.json, while design.md:220 commits feature_list.json and never names the snapshot; the snapshot filename appears exactly once in the whole spec (tasks.md:393 write) and is read by nothing — SessionStart (tasks.md:165-166) reads only claude-progress.txt + feature_list.json. Recommended fix: drop claude-progress-evidence-snapshot.json from tasks.md:393 and instead commit feature_list.json directly (so SessionStart re-orients from it per design.md:219); add feature_list.json to the `git add`/commit at tasks.md:394; set design.md:220 to the same set {claude-progress.txt, feature_list.json}. If the derived snapshot is kept, add it to design.md:220 AND give it a reader in session_start_hook.py (task 7.1).

**214. PreCompact wrongly included in gate-decision OTel forwarding (makes no allow/block decision)**  ·  _[pre_compact_hook.py]_
- **anchor:** tasks.md:539-541 task 36.1 `Extend all six hooks (Stop, PreToolUse, PostToolUse, SubagentStop, SessionStart, PreCompact) to emit OTel gate-decision events ... attributes: hook.event, tool.name, decision (allow/block), reason, requirement.id`
- **change:** PreCompact makes no gate decision — it always exits 0, has no allow/block, no requirement.id, and the design.md:220 PreCompact row explicitly lists 'Nothing (checkpoint write; non-blocking)' with no audit_log.append (unlike Stop:209, PreToolUse:210, SubagentStop:217-218 which DO call audit_log.append). REQ-OBS-003 (requirements.md:236) scopes the event to 'WHEN a hook makes a gate decision'. Fix task 36.1: either (a) exclude PreCompact from gate-decision forwarding and emit a distinct non-gate span for it (e.g. `hook.checkpoint` with item-count attributes, no decision/requirement.id), or (b) state that for PreCompact the `decision` attribute is a fixed checkpoint marker (not allow/block) and requirement.id is omitted. Update the ears_story for Requirement 12.3 accordingly.

**215. Relabel the REQ-STATE-001 ears-story's 11.4 reference (per-slice commit, not PreCompact)**  ·  _[pre_compact_hook.py]_
- **anchor:** tasks.md:18 task 1 `_Requirements: 11.1, 11.4_`
- **change:** When mapping PreCompact's contribution to durable state, do NOT cite criterion 11.4 as a PreCompact/claude-progress.txt anchor: 11.4 (requirements.md:222) is 'record an incremental commit per completed Slice as the durable execution trail' — owned by the implementer's per-slice commit, NOT by PreCompact. PreCompact's genuine REQ-STATE anchors are 11.1 (persist outside model context) and 11.2 (the checkpoint itself). Keep task 1's tag `11.1, 11.4` as-is (task 1 legitimately scaffolds claude-progress.txt for 11.1 and the git/.gitignore trail relevant to 11.4), but in any PreCompact coverage note label the 11.4 link as 'task 1's scaffolding tag, unrelated to PreCompact' so no false PreCompact->11.4 coverage edge is implied.

**216. store_to_feature_list() and PreToolUse status-guard task text must admit the failed-state transitions**  ·  _[evidence_collector.py]_
- **anchor:** Task 3.1, tasks.md:47 ('flips status to `proven`') and task 6.1, tasks.md:135 ('validate proposed transition is `unproven → proven` only')
- **change:** Update tasks.md:47 and tasks.md:135 to match the 2026-06-15 reconciliation that widened permitted transitions. tasks.md:135 currently says 'validate proposed transition is `unproven → proven` only' — change to: permit {unproven→proven, unproven→failed, failed→unproven}; only any transition *into* proven additionally requires a complete Evidence_Record (unproven→failed and failed→unproven require none). Align tasks.md:47's store_to_feature_list() description to the same edge set. This reconciles the stale task text with requirements.md:123,129, design.md:215 (PreToolUse status guard) and design.md:372,374 (status enum {unproven,proven,failed}) and Property 3 (design.md:900).
- _adds EARS/use-case wiring_

**217. Property 19 over-claims Postgres round-trip while filed as a Phase-0 collector subtask**  ·  _[evidence_collector.py]_
- **anchor:** Task 3.2, tasks.md:50-54 — '**Property 19: Evidence Round-Trip (Store and Retrieve)** ... Validates: Requirements 16.2'
- **change:** Property 19 (design.md:1026-1030) asserts the record is 'stored in Postgres ... retrievable by requirement_id and commit_sha', but task 3.2 is a Phase-0 collector subtask with NO Postgres (store_to_postgres is Phase-2 task 30.1, tasks.md:476). Split the property: keep a Phase-0 in-memory/file round-trip (collect→validate→hash-match, no Postgres) under task 3.2 and validate against 5.3/5.6 not 16.2; add the Phase-2 Postgres store/retrieve round-trip assertion under task 30.2 (tasks.md:480) validating 16.2. Reconcile the 'stored in Postgres' wording in the Phase-0 portion of Property 19 at design.md:1028 accordingly.

**218. Division of labor between wiring_checker.py (AST) and wiring_dead_code.yml (Semgrep, task 24.1) is unresolved**  ·  _[wiring_checker.py]_
- **anchor:** Task 19.1 body tasks.md:328 (AST 'never called' detection) vs task 24.1 tasks.md:385-386 (Semgrep route/handler decorators + registered-but-uninvoked callbacks); both carry _Requirements: 8.1, 8.2_
- **change:** Add a one-line division-of-labor note (in task 19.1 and cross-referenced in 24.1): 'wiring_checker.py (AST) covers plain defined-but-never-called functions; wiring_dead_code.yml (Semgrep) covers decorator-routed handlers/routes and registered-but-uninvoked callbacks; both emit type:WIRING candidates, de-duplicated by symbol before ingestion.' This resolves which tool authoritatively detects the route/handler/callback obligations named in criterion 8.2 and prevents double-reporting. Currently both tools claim the same 8.1/8.2 scope with no de-dup or boundary stated.

**219. Define the authoritative store for the backward-orphan 'maps to no verification' lookup**  ·  _[orphan_detector.py]_
- **anchor:** Task 20.1, bullets at tasks.md:341-342 ('Read feature_list.json for all known requirement IDs' / 'requirement IDs with no source file or verification artifact')
- **change:** Specify the authoritative source for the backward-orphan verification-artifact link. Postgres is the declared single source of truth for coverage queries (requirements.md:298) and the requirement<->evidence graph lives in traceability_links + evidence_records (design.md:453-478), yet task 20.1 reads feature_list.json (tasks.md:341), which can diverge. Add a bullet: query evidence_records / traceability_links (link_type IN ('test','evidence')) for the verification-artifact lookup, with a documented feature_list.json fallback consistent with the Postgres-unavailable fallback rule (design.md:815, tasks.md:730 N-25). Reconcile with REQ-16.3.

**220. Factor commit-trailer ID extraction into one shared helper and state ownership**  ·  _[orphan_detector.py]_
- **anchor:** Task 20.1 bullet (tasks.md:340, commit-trailer scan) and task 29.1 (tasks.md:464-466, parse_commit_trailers / assert_commit_has_req_id)
- **change:** Two tools implement the same `[A-Z]+-[A-Z]+-[0-9]{3}` commit-trailer scan with no division of labor: orphan_detector (tasks.md:340) and traceability_writer.parse_commit_trailers/assert_commit_has_req_id (tasks.md:464,466). Add a bullet factoring trailer-ID extraction into one shared helper (e.g. tools/req_id_scan.py) imported by both, and state authority: traceability_writer.assert_commit_has_req_id owns the commit-has-req-id gate (REQ-6.2 / Property 10); orphan_detector owns bidirectional orphan detection (REQ-6.3 / Property 11). Prevents regex drift / double-maintenance.

**221. Enumerate the four Evidence_Record fields in the Rego proven-without-evidence rule**  ·  _[coverage_query.rego]_
- **anchor:** Task 21.1 — 'Add second rule: deny if any item has `status == "proven"` but `evidence` object is missing or has empty fields' (tasks.md:356)
- **change:** Make task 21.1 Rule 2 assert all four Evidence_Record fields (`test_file`, `test_name`, `output_hash`, `collected_at`) are present and non-empty, mirroring the schema `required` list (design.md:389) and the SubagentStop four-field check (design.md:217, Property 2). Current wording 'evidence object missing or has empty fields' is vague and could pass a proven item carrying a partial 1-field evidence object that the hook layer (Property 2 / Glossary Evidence_Record, requirements.md:29) would reject — an inconsistency between the merge gate and the in-session gate.

**222. Document that coverage-gate.yml is an overloaded workflow gating three tools, not OPA-only**  ·  _[coverage_query.rego]_
- **anchor:** Task 14.2 — required status checks list naming `coverage-gate` as a single required check (tasks.md:270); cross-ref tasks.md:264 (OPA), 274 (Playwright), 371 (SonarQube) all in coverage-gate.yml
- **change:** Add a note (in task 14.2 or task 21) that the single required status check `coverage-gate` co-hosts three independent tools in `.github/workflows/coverage-gate.yml`: OPA/Conftest (task 14.1/15... tasks.md:264), Playwright (task 15.1, tasks.md:274), and SonarQube (task 22.1, tasks.md:371). Because all three run under one check name, a Playwright or SonarQube failure blocks merge under the same name, so the OPA policy is NOT independently gating and REQ-GATE-003's 'THE OPA_Conftest required CI status check SHALL run' is satisfied only as a co-tenant. Either (a) document this co-tenancy explicitly so the OPA leg's gating role is traceable, or (b) split the OPA policy into its own job/required check so it gates independently.

**223. Add Req 5.6 to task 6.1 _Requirements to match design.md:215 status-guard mapping**  ·  _[feature_list.schema.json (schema/)]_
- **anchor:** tasks.md:137 (task 6.1 _Requirements: 5.2, 5.3, 7.4, 7.5, 13.2, 13.3, 13.4, 18.1) vs design.md:215 (status guard enforces REQ-COV-002, REQ-COV-003, REQ-COV-006)
- **change:** Add `5.6` to the _Requirements line of task 6.1 (tasks.md:137). The hook-wiring table at design.md:215 maps the PreToolUse status guard to REQ-COV-002, REQ-COV-003 AND REQ-COV-006, but the building task that implements check_status_transition omits 5.6, so the four-field/non-empty gate (Req 5.6) is mapped to the status guard in design but not traced to it in the task.
- _adds EARS/use-case wiring_

**224. requirement_versions column set in tasks.md 28.7 contradicts the authoritative DDL**  ·  _[db/migrations/ (all 8 tables 001-008)]_
- **anchor:** 28.7 Write migration `db/migrations/007_requirement_versions.sql` — bullet: "`requirement_versions` table: FK to `requirements`, monotonically increasing `version`, `amended_at`, `amendment_reason`, and a flag/marker for whether the amended item has been re-proven"
- **change:** Rewrite the 28.7 column bullet to mirror the design.md DDL (design.md:1141-1151) verbatim: columns `requirement_id` (FK), `version` INTEGER (monotonically increasing), `prior_text`, `new_text` NOT NULL, `author` NOT NULL, `rationale` NOT NULL, `created_at` TIMESTAMPTZ DEFAULT now(), `UNIQUE (requirement_id, version)`. Drop the non-canonical names `amended_at`/`amendment_reason` and the standalone 're-proven flag' (re-proof is tracked via resetting `coverage_items.status` to `unproven`, per design.md:1152). Keep the prose 'on amendment the linked coverage item re-enters unproven and COMPLETE is blocked while un-reproven'.

**225. task 28.7 column set conflicts with canonical requirement_versions DDL**  ·  _[db/migrations/007_requirement_versions.sql]_
- **anchor:** - `requirement_versions` table: FK to `requirements`, monotonically increasing `version`, `amended_at`, `amendment_reason`, and a flag/marker for whether the amended item has been re-proven; on amendment the linked coverage item re-enters `unproven` and COMPLETE is blocked while any amended item is un-reproven
- **change:** Reconcile task 28.7's column list (tasks.md:450) to the canonical DDL at design.md:1141-1151: rename `amended_at`→`created_at`, rename `amendment_reason`→`rationale`, add `prior_text` (nullable) and `new_text TEXT NOT NULL` and `author TEXT NOT NULL`, and DROP the 'flag/marker for whether the amended item has been re-proven' column. State explicitly that re-proof is observed via `coverage_items.status` flipping back (not a column on this table), so the two specs no longer describe different tables.

**226. Fix wrong machine-readable _Requirements_ tag on task 28.8 (16.1 -> 27/REQ-AUDIT)**  ·  _[db/migrations/008_gate_audit_log.sql]_
- **anchor:** tasks.md:456 `    - _Requirements: 16.1 (REQ-AUDIT-001..003)_`
- **change:** Task 28.8 (write 008_gate_audit_log.sql) is tagged '_Requirements: 16.1 (REQ-AUDIT-001..003)_', but 16.1 is the spine-smoke-test requirement (tasks.md:281, 'Write tests/smoke/test_spine_e2e.py') — '16.1' is a boilerplate phase tag copy-pasted across the whole Phase-2 migration block (tasks.md:421,426,431,435,439,447,451,456). Change task 28.8's tag to '_Requirements: 27 (REQ-AUDIT-001..003)_' so the machine-readable pointer matches the actual requirement. (The sibling migration tags carrying boilerplate 16.1 are a broader pattern, but 28.8 is the one for this component.)

**227. Field-align the OTel hook-decision attribute set with the audit-log tuple (add actor_agent)**  ·  _[audit_log.py]_
- **anchor:** Line 540 (task 36.1): 'set attributes: hook.event, tool.name, decision (allow/block), reason, requirement.id'
- **change:** REQ-AUDIT-001 (line 457) records actor_agent in the durable audit tuple, but the OTel gate-decision span attribute set in task 36.1 omits actor_agent, so the 'same decision forwarded to two sinks' is recorded with different field sets. Add 'actor.agent' (or 'actor_agent') to the task 36.1 OTel attribute list so the live OTel span and the durable audit row carry the same per-agent attribution that closes pain-point category 4. Cross-reference REQ-OBS-003 (requirements.md:236).

**228. Remove REQ-OBS-006 from the audit-chain CI gate requirement tag (task 40.3)**  ·  _[audit_log.py]_
- **anchor:** Line 703 (task 40.3): '_Requirements: 12.x (REQ-AUDIT-001..003, REQ-OBS-006)_'
- **change:** Task 40.3 is the audit-chain-verify CI workflow (runs audit_verify.py over gate_audit_log); it has nothing to do with reasoning-loop detection (REQ-OBS-006 lives in observability, design.md:1183). Drop REQ-OBS-006 from the tag and fix the stray '12.x' prefix: change to '_Requirements: REQ-AUDIT-001..003_'. The parent task 52 legitimately bundles both concerns, but the CI gate for the audit chain does not enforce REQ-OBS-006.

**229. Globally fix PBT suite count 29 -> 30**  ·  _[audit_verify.py]_
- **anchor:** - [ ] 39. Complete PBT suite — all 29 correctness properties
- **change:** Fix the PBT suite count from 29 to 30 at the two drift sites — tasks.md:569 (task 39 heading 'all 29 correctness properties' and its parenthetical 'Properties 1–24 plus new 25–29 = 29') and tasks.md:732 ('The 29 correctness properties in Phase 4'). These contradict tasks.md:717 ('all 30 properties'), tasks.md:979 ('extend the PBT suite to 30'), and requirements.md:514 ('30 properties'). Property 28 (audit-log tamper detection) is one of the new properties whose home-suite count is stated inconsistently; the canonical set is 30 (Properties 1–30).

**230. Fix task 52 / 52.2-CI requirement tags: drop '12.x' and REQ-OBS-006 from the chain-verifier CI gate**  ·  _[audit_verify.py]_
- **anchor:**     - _Requirements: 12.x (REQ-AUDIT-001..003, REQ-OBS-006)_
- **change:** On the `.github/workflows/audit-chain-verify.yml` requirements line (tasks.md:703), replace '12.x (REQ-AUDIT-001..003, REQ-OBS-006)' with 'REQ-AUDIT-001..003' (or 'Requirement 27'). '12.x' is not a valid requirement id, and REQ-OBS-006 is the reasoning-loop detector (Requirement 26) which the chain verifier does NOT enforce — it only recomputes the hash chain. The REQ-OBS-006 bundling stems from task 52 conflating audit-log + reasoning-loop work; the audit-chain CI gate should carry only the audit requirement.

**231. Rename Property 28 test file from test_audit_log.py to test_audit_verify.py**  ·  _[audit_verify.py]_
- **anchor:**   - [ ]* 39.18 Write `tests/property/test_audit_log.py` — **Property 28: Audit-Log Tamper Detection (REQ-AUDIT-002)**
- **change:** Rename the Property 28 PBT from `tests/property/test_audit_log.py` to `tests/property/test_audit_verify.py` (the assertion exercises `tools/audit_verify.py` per tasks.md:680, not the producer), OR split into test_audit_log.py (producer append / canonical-form) + test_audit_verify.py (chain recomputation / tamper detection). As authored the test is named after the PRODUCER while testing the VERIFIER, and there is no test_audit_verify.py.

**232. Resolve phase inconsistency: Phase-1 inventory row vs Phase-4/un-phased build tasks**  ·  _[secrets-scan.yml]_
- **anchor:** Task 40.2 at line 695 (under task 40 'Phase 4 hardening'); task 45 at line 957 (un-phased 'Added tasks' block); design.md:147 labels the component Phase 1
- **change:** secrets-scan.yml is labelled Phase 1 in the design inventory (design.md:147) but its only build tasks are task 40.2 (Phase 4 hardening) and task 45 (un-phased). Pick one phase and align all three. Recommended: move the workflow-creation work into Phase 1, co-located with the other CI verification-depth gates (near task 23 codeql.yml / task 24 Semgrep, tasks.md:376-388), and tag task 45 with the same phase. Alternatively relabel design.md:147's Phase column to 4. The chosen phase must be identical in design.md:147, task 40.2, and task 45.

**233. Designate a single canonical required-checks enumeration and a design spec for docs/github-ruleset.md**  ·  _[secrets-scan.yml]_
- **anchor:** Required-checks list triplicated at tasks.md:270 (14.2), 697 (40.2), 957 (45); docs/github-ruleset.md referenced only in tasks.md, never specified in design.md
- **change:** The required-merge-gate list (formal-verification, coverage-gate, secrets-scan, audit-chain-verify, deepeval-gate, zap-baseline, + human reviewer) is enumerated in three task locations with no single canonical source, and docs/github-ruleset.md has no design.md specification of its contents/schema. Designate task 14.2 (tasks.md:270) as the single canonical enumeration of required status checks and have tasks 40.2/45/40.3 reference it instead of re-listing, and add a brief design.md description of docs/github-ruleset.md (what status checks it must enumerate and the reviewer requirement). This is a shared CI-ruleset artifact issue, not secrets-scan-specific.

**234. Phase-label inconsistency: omission_guard is Phase 0 (design.md:148) but its host file is Phase 0/1 (design.md:130) and its only build task (54) sits in the undated P3 'Added tasks' block with no phase**  ·  _[omission_guard (capability in subagent_stop_hook.py)]_
- **anchor:** Task 54 (tasks.md:974) in the '### Added tasks — P3 market-research capabilities' block with no phase label; vs design.md:148 (omission_guard Phase 0) and design.md:130 (subagent_stop_hook.py Phase '0/1')
- **change:** Pin the omission guard's phase explicitly as Phase 0 (per design.md:148) and either (a) fold task 54's hook-extension into the Phase-0 build order (alongside task 8), or (b) annotate task 54 that the omission guard is a Phase-0 capability delivered by this task and must be runnable end-to-end before Phase 1. Align design.md:130's '0/1' label for subagent_stop_hook.py with the Phase-0 placement of both gates it hosts.

**235. Add deepeval-gate (and zap-baseline) to the canonical required-status-checks doc list**  ·  _[deepeval_gate.py]_
- **anchor:** Document (in `docs/github-ruleset.md`) the required status checks: `formal-verification`, `coverage-gate`, `secrets-scan` (task 45), `audit-chain-verify` (task 52), plus human PR reviewer requirement
- **change:** Update the required-status-checks enumeration at task 14.2 to also list `deepeval-gate` (task 55) and `zap-baseline` (task 56), so the single documented source of truth for the ruleset matches requirements.md:484/489 and design.md:149/150, both of which declare these two P3 checks REQUIRED at merge. As written the doc list contradicts those REQUIRED-check declarations by omitting both P3 gates.
- _adds EARS/use-case wiring_

**236. Fix property-count contradiction: tasks.md:732 says '29 correctness properties' but canonical count is 30**  ·  _[zap-baseline.yml]_
- **anchor:** Line 732: 'The 29 correctness properties in Phase 4 ... Properties 1–24 from the original phases; Properties 25–29 authored under tasks 48–52'
- **change:** Change '29 correctness properties' to '30 correctness properties' and 'Properties 25–29 authored under tasks 48–52' to 'Properties 25–30 authored under tasks 48–54' (Property 30 = omission-declaration gate, authored under task 54). The canonical count is 30: requirements.md:514 ('30 properties (Properties 1–24 plus new 25–30)'), tasks.md:979 ('extend the PBT suite to 30'), tasks.md:980 / design.md Property 30 block (line 1124) all define Property 30. Line 732 is the sole '29' outlier and is internally contradicted by line 979 in the same file. NOTE: shared/spec-wide inconsistency, not specific to zap-baseline.

**237. Stale dependency-graph count-note omits flagd wave (id 48 / task 57)**  ·  _[flagd]_
- **anchor:** Reconciliation count-note: '- **Dependency-graph count:** the graph has 36 waves (ids 0–35) for tasks 1–42, plus the appended waves (ids 36–43) that schedule the new tasks 43–53.' (tasks.md:950)
- **change:** Amend the stale prose count-note to match the actual waves JSON, which runs through wave id 48 and schedules tasks 43–57 (the flagd build task 57 is wave id 48; tasks 54/55/56 are waves 45/46/47). Correct text: 'the graph has 36 waves (ids 0–35) for tasks 1–42, plus appended waves (ids 36–48) that schedule the new tasks 43–57.' This is the inverse of the refuted phantom gap — task 57 IS scheduled (wave 48); the DEFECT is that the prose note under-reports by 5 waves and stops at task 53.

**238. acting_agent vs actor_agent naming contradiction in Property 24 test (tasks.md:655)**  ·  _[implementer.md]_
- **anchor:** Generate evidence chains with arbitrary `acting_agent` identities; assert any Evidence_Record where `acting_agent == "implementer.md"` is rejected
- **change:** tasks.md:655 (Property 24 test) uses field name `acting_agent`, contradicting the declared canonical name `actor_agent` used in design.md:473 ('Canonical name is actor_agent everywhere (no acting_agent)') and design.md:1068. Rename both occurrences of `acting_agent` to `actor_agent` in this subtask so the test field matches the schema column.

**239. Fix task 50 traceability line to cite Requirement 24.1/24.2, not 3.1/3.3**  ·  _[research.md]_
- **anchor:** - [ ] 50. ... Properties 27, 29. _Requirements: 3.1, 3.3._ (tasks.md:965)
- **change:** Task 50 implements REQ-SPEC-016 (Requirement 24.1) AND REQ-SPEC-017 (Requirement 24.2), but its traceability line cites only `_Requirements: 3.1, 3.3._` — which are Requirement 3's sourcing criteria, not the requirements task 50 actually closes. Change to `_Requirements: 24.1 (REQ-SPEC-016), 24.2 (REQ-SPEC-017); 3.1, 3.3 (sourcing context)._`. Same correction applies to the test subtasks: 39.17 (tasks.md:677) cites `3.1, 3.3 (REQ-SPEC-016)` and 39.19 (tasks.md:687) cites `3.1, 3.3 (REQ-SPEC-017)` — these should cite 24.1 and 24.2 respectively.

### 🟡 MISSING-WIRING (38)

**240. Add Req 13.5 (Property 21 Hook Exit-Code Contract) to stop_hook task 5.1 _Requirements_**  ·  _[stop_hook.py]_
- **anchor:** task 5.1 _Requirements_ line 102 `_Requirements: 4.2, 4.3, 4.4, 10.2, 10.4, 14.1, 14.2_`
- **change:** Req 13.5 (requirements.md:254 — exit 0 = proceed, exit 2 = blocking via stderr, exit 1 does NOT block) is the exact contract stop_hook.py emits (exit-0 allow/COMPLETE/HANDOFF vs exit-2 block), and Property 21 (design.md:1042-1046) validating it is already wired as a stop-hook-touching property test in test_hooks.py (tasks.md:590-597). But task 5.1's _Requirements_ list omits 13.5 entirely. Add 13.5 to task 5.1's _Requirements_ (and to task 5.5's unit-test _Requirements at line 128, which currently lists only 10.4) so the Hook Exit-Code Contract is traced to its canonical emitter. This wires the EARS criterion to the component.
- _adds EARS/use-case wiring_

**241. Add a Phase-0 implementation task for the PreToolUse checklist-approval guard (REQ-SPEC-016, Property 27, CHECK-12)**  ·  _[pre_tool_use_hook.py]_
- **anchor:** Task 6 (tasks.md:130-137); cross-ref design.md:213 hook-table checklist-approval row and Property 27 (design.md:1094-1100)
- **change:** Add a sub-task (under task 6 or task 50) implementing `check_checklist_approval(event)` in pre_tool_use_hook.py that, for an initializer/discovery Write referencing a domain-baseline checklist, blocks (exit 2) unless the referenced checklist has a non-null `approved_at` in `domain_baseline_checklists`. Cross-reference Property 27 / CHECK-12a-12b. Currently the design hook-table row (design.md:213), Property 27, and CHECK-12 exist but NO build task wires this guard into the hook (task 50 implements the abstract gate logic but does not name the PreToolUse hook check).
- _adds EARS/use-case wiring_

**242. Wire REQ-OBS-003 (12.3) gate-decision telemetry as an obligation on pre_tool_use_hook.py**  ·  _[pre_tool_use_hook.py]_
- **anchor:** Task 6 `_Requirements:` line (tasks.md:137); cross-ref task 36.1 which names PreToolUse (tasks.md:539) and requirements.md:236 (12.3)
- **change:** Add a note to task 6 (and/or cite 12.3 in its `_Requirements:`) that PreToolUse gate decisions must emit an OTel gate-decision event (event, tool, allow/block, reason, requirement ID) per REQ-OBS-003 (12.3), with the actual emission implemented in task 36.1 (Phase 3, which already names PreToolUse). Currently 12.3 is a binding obligation on this component but task 6 omits OTel emission entirely and 12.3 is not cited anywhere in task 6, leaving the Phase-0/1 PreToolUse hook with no decision telemetry and no acknowledged deferral.
- _adds EARS/use-case wiring_

**243. Add the schema-validation obligation for feature_list.json writes to task 6**  ·  _[pre_tool_use_hook.py]_
- **anchor:** Task 6.1 check list (tasks.md:131-135); cross-ref design.md:138 ('PreToolUse validates against this') and design.md:623 (Phase-0 spine: 'JSON Schema that PreToolUse validates writes against')
- **change:** Add a `check_schema_validation(event)` step to task 6.1 that JSON-Schema-validates every feature_list.json write against schema/feature_list.schema.json (blocking on a schema-invalid write, e.g. an item with a proven status but a missing output_hash), and cite this in design's status-guard row. Design.md:138 and the Phase-0 spine table (design.md:623, test 'rejects an example missing output_hash') both state PreToolUse validates feature_list.json writes against the schema, but task 6's check_status_transition validates only transition edges + evidence completeness, never invoking the JSON Schema — so the design-mandated schema-validation behavior has no build step.
- _adds EARS/use-case wiring_

**244. Wire the Postgres-unavailable file-fallback into the PreToolUse status/scope/integrity guards in task 6**  ·  _[pre_tool_use_hook.py]_
- **anchor:** Task 6.1-6.2 (tasks.md:131-135, file-only reads); cross-ref design.md:815 ('Postgres unavailable | Fall back to feature_list.json...') and the reconciliation note (tasks.md:730)
- **change:** Add to task 6 that the scope gate (reads prior-slice statuses), status guard (reads/validates feature_list.json), and integrity guard (reads run_state.resume_integrity_ok) must follow the Postgres-unavailable fallback defined at design.md:815 and tasks.md:730 — falling back to file-backed state (feature_list.json + claude-progress.txt) and reaching the SAME (conservative/blocking) decision, never fail-open. Cite design.md:815 (not just the general note 730). Currently task 6 reads feature_list.json file-only with no DB path or fallback contract, so the gate-consistency-on-fallback obligation (N-25) is unwired for this hook.
- _adds EARS/use-case wiring_

**245. Task the SessionStart→state_integrity compute/write (it is currently untasked)**  ·  _[session_start_hook.py]_
- **anchor:** Task 49 'Resumed-state integrity (REQ-STATE-005)' (tasks.md:963) and its only subtask 49.1; task 7.1 'Implement .claude/hooks/session_start_hook.py' (tasks.md:163-169)
- **change:** design.md:219 and Property 26 (design.md:1088) both state SessionStart 'computes the resumed-state hash via state_integrity.py and writes run_state.resume_integrity_ok', but no task implements this: task 7.1 only does git/progress/coverage load + stdout injection, and task 49/49.1 create state_integrity.py + the PreToolUse guard with NO subtask wiring state_integrity INTO session_start_hook.py. Add a subtask 49.2 (or amend 49) naming the file: 'extend .claude/hooks/session_start_hook.py to import tools/state_integrity.py, call state_integrity.compute_hash(), and write run_state.resume_integrity_ok (Phase-2 extension of the Phase-0 hook; non-blocking, exit 0 always)'. Cross-reference it from task 7 so the Phase-0 component's later mutation is traceable. Extend task 7.2 unit tests with an assertion that resume_integrity_ok is written on resume. _Requirements: 11.3, REQ-STATE-005_.
- _adds EARS/use-case wiring_

**246. Cover where SessionStart writes resume_integrity_ok when Postgres is unavailable (N-25)**  ·  _[session_start_hook.py]_
- **anchor:** Notes bullet N-25 'Postgres-unavailable fallback gate-consistency' (tasks.md:730), which enumerates 'Stop coverage gate, PreToolUse status/integrity guards, OPA merge policy' but not SessionStart's write of resume_integrity_ok
- **change:** N-25 mandates file-backed fallback for the read-side gates but is silent on SessionStart's WRITE of run_state.resume_integrity_ok (a Postgres run_state column per design.md:492) when health_check() fails at session start. If Postgres is down at SessionStart, the spec does not say whether the flag is written to the claude-progress.txt fallback or skipped — yet the downstream PreToolUse integrity guard reads it. Extend N-25 to state where SessionStart records resume_integrity_ok under DB-unavailable (write to the file-backed run_state duplicate, reconcile on reconnect), keeping the conservative/blocking behavior.

**247. Map REQ-STEER-004 (command-type fail-closed) as an explicit obligation on this hook**  ·  _[subagent_stop_hook.py]_
- **anchor:** Task 9.1 registers SubagentStop as "type":"command" (tasks.md:193-194); task 9.2 smoke test asserts all hooks command-type (tasks.md:197-199); requirement 13.4/REQ-STEER-004 (requirements.md:253)
- **change:** Wire REQ-STEER-004 (command-type hooks fail closed; no HTTP/MCP for hard policy, requirements.md:253) explicitly onto subagent_stop_hook.py: add the requirement tag to the SubagentStop registration (task 9.1) and reference the task-9.2 smoke test (tests/smoke/test_hook_config.py) which parses settings.json and asserts EVERY registered hook including SubagentStop is command-type with no http/mcp. This is the reason the hook can fail closed (exit 2) at all; currently it is only an un-tagged behavior note.
- _adds EARS/use-case wiring_

**248. Single out SubagentStop in the Property 21 exit-code-contract test**  ·  _[subagent_stop_hook.py]_
- **anchor:** Task 39.4 'Write property test for hook exit-code contract' / test_hook_exit_code_contract, generic over hooks (tasks.md:593-597); Property 21 (design.md:1042-1046); requirement 13.5 (requirements.md:254)
- **change:** Extend task 39.4 (Property 21, Hook Exit-Code Contract) to explicitly include a SubagentStop case: assert the hook's allow(exit 0)/block(exit 2 with stderr)/fail-closed-on-exception(exit 2) behavior conforms to REQ-STEER-005 (requirements.md:254). The exit-code contract is the foundation of this hook's entire allow/block behavior, yet the validating test (test_hook_exit_code_contract) is generic across hooks and does not single out SubagentStop, leaving its exit-code conformance only transitively covered.
- _adds EARS/use-case wiring_

**249. Fix settings.json owning-file path drift and stale task cross-reference for PreCompact binding**  ·  _[pre_compact_hook.py]_
- **anchor:** tasks.md:16 `Create `.vscode/settings.json` (or `.kiro/settings.json`) stub for hook wiring (populated in task 8)`
- **change:** Two defects on this line: (1) 'populated in task 8' is wrong — task 8 is subagent_stop_hook.py (tasks.md:176); the hook-wiring task that registers PreCompact is task 9 (tasks.md:191-193). Change to 'populated in task 9'. (2) The stub path `.vscode/settings.json` is both inconsistent with task 9.1 / design.md:157 (`.claude/settings.json`) AND actively gitignored (.gitignore:32 ignores `.vscode/`), so a PreCompact binding placed there would never be committed and the runtime would never load it (silent fail-open wiring hole). Standardize the path to `.claude/settings.json` at tasks.md:16. The actual PreCompact command binding `python3 .claude/hooks/pre_compact_hook.py` is specified at design.md:193-198 and registered by task 9.1.
- _adds EARS/use-case wiring_

**250. actor_agent: add as a store_to_postgres parameter (NOT to the 4-field JSON record) and fix stale acting_agent name**  ·  _[evidence_collector.py]_
- **anchor:** Task 30.1, tasks.md:477 ('store_to_postgres(record: EvidenceRecord, req_id: str, commit_sha: str)') and Property 24 test, tasks.md:655 ('acting_agent == "implementer.md"')
- **change:** Property 24 requires actor_agent (= verifier.md, never implementer.md) on every evidence row (design.md:473,1068), but store_to_postgres()'s signature (tasks.md:477) carries no verifier identity, so the role-separation invariant has no enforcement point at the collector boundary. Add `actor_agent` as a parameter: `store_to_postgres(record, req_id, commit_sha, actor_agent)`, asserting it equals 'verifier.md' and rejecting implementer-authored evidence at write time. CRITICAL CONSTRAINT: do NOT add actor_agent to the EvidenceRecord JSON object — that schema is `additionalProperties: false` over exactly the four fields (design.md:390; requirements.md:29), so adding a fifth field would break Property 2/CHECK-7 and be rejected by the SubagentStop and PreToolUse status-guard validators. actor_agent lives only on the 7-column evidence_records Postgres row (design.md:464-478). Separately fix the stale name at tasks.md:655: the Property 24 test still references `acting_agent`, but design.md:473 declares 'Canonical name is actor_agent everywhere (no acting_agent)' — rename `acting_agent` → `actor_agent` in the task-655 test description.
- _adds EARS/use-case wiring_

**251. Wire REQ-EXEC-012 / 8.3 WIRING integration-test evidence into the collector scope**  ·  _[evidence_collector.py]_
- **anchor:** Task 3.1, tasks.md:44-48 (_Requirements: 5.3, 5.6, 9.3_) and task 47, tasks.md:959 (WIRING integration-test evidence)
- **change:** Requirement 8.3 (requirements.md:174: 'WHEN a WIRING item is claimed proven, THE System SHALL require an integration test exercising the real execution path as the attached Evidence_Record') and the schema note 'WIRING items require integration-test evidence' (design.md:334) bear directly on evidence_collector.py — it is the assembler of that integration-test Evidence_Record. Task 47 (tasks.md:959) names the obligation but the collector task 3.1 only frames output_bytes around behavioral/Playwright output. Add 8.3 to task 3.1's `_Requirements:` line and a sub-point that collect() must accept and assemble an integration-test artifact (not only a behavioral/Playwright one), so the WIRING-proof path has an explicit assembly point.
- _adds EARS/use-case wiring_

**252. Wire REQ-VERIFY-007/008 (Req 25.1/25.2) NFR Evidence_Record assembly into the collector**  ·  _[evidence_collector.py]_
- **anchor:** Task 3.1, tasks.md:44-48 (_Requirements: 5.3, 5.6, 9.3_) and task 51, tasks.md:966 (perf_a11y_verifier emits NFR Evidence_Records)
- **change:** Requirements 25.1/25.2 (requirements.md:447-448) require performance verification to 'attach the result as the Evidence_Record' (k6/Lighthouse) and accessibility verification (axe-core) to produce evidence; task 51 (tasks.md:966) emits these as 'NFR Evidence_Records.' These records are assembled by evidence_collector.py, but task 3.1 frames collect()/output_bytes only around behavioral test output. Add REQ-VERIFY-007/008 to the collector's bearing-requirements and a note that output_bytes for NFR items is the k6/Lighthouse/axe-core result artifact — which makes resolving the byte-source question (dedup evidence-hash-byte-source) mandatory for non-test artifacts.
- _adds EARS/use-case wiring_

**253. store_to_postgres fallback must be flagged as an N-25 gate-consistency obligation (never fail-open)**  ·  _[evidence_collector.py]_
- **anchor:** Task 30.1, tasks.md:477 ('fall back to feature_list.json update if DB unavailable') and task 30.2, tasks.md:482
- **change:** store_to_postgres's feature_list.json fallback (tasks.md:477,482) is specified only mechanically. Per the N-25 reconciliation obligation (tasks.md:730), when db_connection.health_check() fails the file-backed fallback MUST reach the SAME allow/block decision as Postgres and NEVER fail-open, taking the more conservative (blocking) result on divergence. Add this gate-consistency constraint to task 30.1/30.2 and cite REQ-STORE-001/003 (requirements.md:296,298 — Postgres single source of truth, reconstructable independently of any model session). A fail-open or divergent collector fallback would silently weaken the OPA zero-evidence gate.
- _adds EARS/use-case wiring_

**254. No ingestion path turns the WIRING candidate into a feature_list.json coverage item (8.1 second clause)**  ·  _[wiring_checker.py]_
- **anchor:** Task 19.1 body, tasks.md:329 "For each dead-code finding, emit a WIRING coverage item candidate (JSON to stdout)" — the candidate stops at stdout
- **change:** Add a subtask (e.g. 19.3) OR extend post_tool_use_hook.py task 18.1 specifying the ingestion path that satisfies criterion 8.1's second clause ('SHALL represent each wiring obligation as a WIRING coverage item in feature_list.json'). State the actor and the write path: the implementer/initializer, on receiving the wiring_checker.py candidate as PostToolUse next-turn feedback, MUST add a type:'WIRING' CoverageItem (status default 'unproven', in_scope true, ≥1 acceptance criterion per Property 1) to feature_list.json via the PreToolUse status-guard-permitted creation path (design.md:215). Cite REQ-EXEC-010. Today the data path stops at stdout/next-turn text and no task creates the item.
- _adds EARS/use-case wiring_

**255. Wire orphan_detector.py as a blocking traceability gate to satisfy REQ-6.3 'block the run'**  ·  _[orphan_detector.py]_
- **anchor:** Task 20.1 'Implement tools/orphan_detector.py' (tasks.md:339-344), _Requirements: 6.3; and task 14.2 required-checks list (tasks.md:270)
- **change:** Add a blocking caller for orphan_detector. Create a new task (e.g. 20.x / 40.x) 'traceability-gate CI workflow': add `.github/workflows/traceability-gate.yml` that runs `python3 tools/orphan_detector.py` and fails the merge (non-zero exit) when any orphan is found; register `traceability-gate` as a REQUIRED status check. Add `traceability-gate` to the required-checks list at tasks.md:270 (`docs/github-ruleset.md`) alongside formal-verification/coverage-gate/secrets-scan/audit-chain-verify. State the exit-code-to-block contract (CI required check fails merge; equivalently a PreToolUse exit-2 gate). This is the production mechanism the REQ-6.3 'block the run' obligation currently lacks — orphan_detector's non-zero exit is consumed by nothing but a test (task 26.1).
- _adds EARS/use-case wiring_

**256. Wire a blocking enforcer for REQ-6.2 commit-trailer obligation (shares orphan_detector's unwired-gate defect)**  ·  _[orphan_detector.py]_
- **anchor:** Task 29.1 (tasks.md:466) 'assert_commit_has_req_id(commit_sha): raise TraceabilityError if no requirement ID found in trailer'; REQ-6.2 (requirements.md:142)
- **change:** REQ-6.2 ('WHEN a commit is created, require >=1 referenced requirement ID in the commit trailer', requirements.md:142) has no blocking enforcer wired: assert_commit_has_req_id (tasks.md:466) only raises TraceabilityError with no stated caller/hook/CI gate. Both 6.2 and 6.3 share the same unwired-enforcement defect. Add a bullet naming the caller for assert_commit_has_req_id — fold it into the same traceability-gate CI check (or a commit-msg/PreToolUse hook) so a single shared commit-trailer gate satisfies both REQ-6.2 and REQ-6.3's trailer obligation.
- _adds EARS/use-case wiring_

**257. Add a Phase-1 subtask to re-confirm coverage-gate.yml invokes the swapped policy and stays a required check**  ·  _[coverage_query.rego]_
- **anchor:** Task 21 — '`coverage_query.rego` — Full OPA Zero-Evidence Merge Policy' (tasks.md:352-358)
- **change:** Add a subtask under task 21 to (a) confirm `.github/workflows/coverage-gate.yml` (created in task 14.1) invokes the Phase-1 full policy after the stub→full swap, and (b) confirm `coverage-gate` remains in the GitHub repository ruleset's required checks (cross-ref task 14.2 at tasks.md:270, whose required-checks list `formal-verification`, `coverage-gate`, `secrets-scan`, `audit-chain-verify` is the only place coverage-gate is named as required). Task 21 currently replaces the .rego content with no task verifying the swapped policy is still the gating one. Also note in task 14.1/21 that the Phase-0 stub (status-only, passes on zero unproven) is intentionally weaker than the Phase-1 full policy (adds evidence presence), so Phase-0 green does NOT imply Phase-1 green — the required-status-check registration must survive the swap (re-register, do not assume continuity).
- _adds EARS/use-case wiring_

**258. Wire schema validation into pre_tool_use_hook status guard**  ·  _[feature_list.schema.json (schema/)]_
- **anchor:** tasks.md:131-136 (task 6.1, pre_tool_use_hook check functions) and design.md:138 ('PreToolUse validates against this')
- **change:** Add a sub-bullet to task 6.1 (tasks.md:131-136): 'load schema/feature_list.schema.json and call jsonschema.validate on the proposed feature_list.json write; block (exit 2) on ValidationError'. design.md:138 claims PreToolUse validates writes against the schema, but the only task that loads the schema is feature_list_init.py (tasks.md:240); no hook task wires jsonschema.validate into the PreToolUse path, so the design.md:138 claim has no implementing code path.
- _adds EARS/use-case wiring_

**259. Add a task implementing the design.md:623 schema negative-validation acceptance**  ·  _[feature_list.schema.json (schema/)]_
- **anchor:** tasks.md:24-41 (task 2 / 2.1 / 2.2 / 2.3) and design.md:623 (Phase-0 spine row 1 acceptance: 'Schema validates a hand-written valid example; rejects an example missing output_hash')
- **change:** Add a non-optional subtask under task 2 (e.g., 2.4) that implements the design.md:623 acceptance directly: jsonschema.validate a hand-written valid feature_list.json passes, and an item whose evidence is missing output_hash is rejected, against schema/feature_list.schema.json. The smoke/integration tests (task 16.1 tasks.md:281-288, task 17.1) only exercise the four hooks and target subagent_stop_hook's output_hash check — not jsonschema.validate against the schema file — and tasks 2.2/2.3 are optional ([ ]*) hypothesis property tests, so the component's own stated pass condition has no implementing task.
- _adds EARS/use-case wiring_

**260. actor_agent column missing from task 28.4 evidence_records description**  ·  _[db/migrations/ (all 8 tables 001-008)]_
- **anchor:** 28.4 Write migration `db/migrations/004_evidence_records.sql` — bullet: "`evidence_records` table with FK to `requirements`, `evidence_complete` CHECK constraint enforcing no empty fields"
- **change:** Add `actor_agent TEXT NOT NULL` to the 28.4 column list, annotated 'MUST be the Verifier, never the Implementer — Property 24 (design.md:473, 1066-1070)'. Update the _Requirements_ line to also cite 9.2. Without this a builder following the task omits actor_agent and silently breaks Property 24 and the Property 19 round-trip.
- _adds EARS/use-case wiring_

**261. No runtime writer wired for the amendment flow (insert version row + reset coverage status)**  ·  _[db/migrations/007_requirement_versions.sql]_
- **anchor:** - [ ] 48. Requirement-amendment versioning (**REQ-COV-007**) — `requirement_versions` table (migration `db/migrations/007_requirement_versions.sql`, task 28.7); on amendment re-enter `unproven`; block COMPLETE while any amended item un-reproven. Only `*→proven` requires a complete Evidence_Record. Property 25 (test in task 39.15). _Z3 CHECK-10a/10b._
- **change:** Add a subtask 48.1 naming the amendment producer/caller, mirroring the audit_log producer pattern (design.md:1173-1174): author `tools/amendment_handler.py` exposing `record_amendment(requirement_id, new_text, author, rationale)` that, in a single transaction, (1) inserts the next `requirement_versions` row and (2) resets the linked `coverage_items.status` to 'unproven'; and name the invoking actor (human operator action; not an autonomous agent). Without this, the table can be created but nothing populates it or triggers the status reset.
- _adds EARS/use-case wiring_

**262. Wire state_integrity.py into the SessionStart hook implementation task**  ·  _[state_integrity.py]_
- **anchor:** - [ ] 7.1 Implement `.claude/hooks/session_start_hook.py` (tasks.md:163-169), whose body lists only read git status / progress file / feature_list.json / inject summary, _Requirements: 11.3_; contrast task 49 (tasks.md:963) which never edits 7.1, and task 52.3 (tasks.md:970) which explicitly wires audit_log.append into three hooks
- **change:** No task makes session_start_hook.py compute the hash or write resume_integrity_ok. Add a subtask under task 7 (or an explicit edit step inside task 49) that modifies .claude/hooks/session_start_hook.py to import state_integrity, recompute the resumed-state hash, compare against run_state.state_hash, and write run_state.resume_integrity_ok — mirroring task 52.3's explicit audit_log.append wiring into the three hooks. Reference Requirement 11.3 / REQ-STATE-005 and keep it non-blocking (exit 0 always).
- _adds EARS/use-case wiring_

**263. Give resume_integrity_ok and the baseline hash a file-backed home for Postgres-down fallback**  ·  _[state_integrity.py]_
- **anchor:** tasks.md:730 Postgres-unavailable fallback note ('PreToolUse status/integrity guards ... MUST fall back to the file-backed state (feature_list.json + claude-progress.txt) ... never fail-open') and task 31.1 save_run_state (tasks.md:486-488) which writes claude-progress.txt as a duplicate but does NOT enumerate resume_integrity_ok or state_hash
- **change:** tasks.md:730 requires the PreToolUse integrity guard to reach the SAME allow/block decision on DB-down using file-backed state, but resume_integrity_ok lives only in run_state (Postgres) and task 31.1's save_run_state does not write it (nor the baseline state_hash) to claude-progress.txt. Amend task 31.1 so save_run_state persists resume_integrity_ok and state_hash into the claude-progress.txt file-backed duplicate, giving the integrity guard a file-backed source on DB-down (otherwise the fallback decision is undefined and risks fail-open).
- _adds EARS/use-case wiring_

**264. Add REQ-AUDIT-003 to task 52.2 (audit_verify) requirements line**  ·  _[audit_verify.py]_
- **anchor:**   - [ ] 52.2 Audit-chain verifier `tools/audit_verify.py` — recompute the chain and fail on any broken link.
- **change:** Change task 52.2's '_Requirements: REQ-AUDIT-002._' to '_Requirements: REQ-AUDIT-002, REQ-AUDIT-003._' to match design.md:145 which attributes BOTH REQ-AUDIT-002 and REQ-AUDIT-003 to audit_verify.py. REQ-AUDIT-003 (retain log as execution-proof complement to SLSA) is currently attached only to the producer-wiring subtask 52.3, leaving the verifier's retention/proof obligation unmapped on the verifier task itself. Add a note that REQ-AUDIT-003 is satisfied jointly by migration 008 (retention) + audit_verify (proof) + the SLSA release workflow (task 32.1).
- _adds EARS/use-case wiring_

**265. Add merged-chain (DB + file-backed log) verification spec for audit_verify.py**  ·  _[audit_verify.py]_
- **anchor:**   - [ ] 52.2 Audit-chain verifier `tools/audit_verify.py` — recompute the chain and fail on any broken link.
- **change:** Add a bullet to task 52.2 specifying that when Postgres is unavailable the verifier reads BOTH the `gate_audit_log` table AND any file-backed log segments written by the producer (per Notes line 730), orders the merged entries by `seq`, and verifies the single combined chain; define the seam rule (the file segment's first `prev_hash` MUST chain to the DB segment's last `entry_hash`). Currently tasks.md:730 specifies only the PRODUCER's DB-down file fallback and reconcile-on-reconnect; nothing tells the VERIFIER how to reconstruct a chain split across Postgres + a file-backed log, so a split chain could falsely fail at the seam or be silently skipped.
- _adds EARS/use-case wiring_

**266. Author the host file verifier.md so perf_a11y_verifier has a concrete home**  ·  _[perf_a11y_verifier (capability in verifier.md)]_
- **anchor:** - [ ] 10.3 Write `.claude/agents/verifier.md` — Independent Evaluator
- **change:** verifier.md does not exist on disk (.claude/agents/ contains only .gitkeep), yet design.md:146 cites perf_a11y_verifier as a 'capability within .claude/agents/verifier.md'. Task 10.3 authors verifier.md but its bullet body stops at 'structural/semantic/behavioral/security'. Add a forward-reference bullet to task 10.3: 'Fifth (performance/accessibility + UI-screen-render) verification layer is added to THIS file in task 51 (perf_a11y_verifier capability).' and reword task 51 from 'implement a perf_a11y_verifier component in the Verifier' to 'amend .claude/agents/verifier.md (authored in task 10.3) to add the fifth perf/a11y/UI-render layer'. Until verifier.md is authored with this layer, the design.md:146 citation is dangling.
- _adds EARS/use-case wiring_

**267. Pin k6/Lighthouse/axe-core tool dependencies and add a CI step that runs perf/a11y**  ·  _[perf_a11y_verifier (capability in verifier.md)]_
- **anchor:** Create `pyproject.toml` (or `requirements.txt`) pinning `z3-solver`, `hypothesis`, `jsonschema`, `psycopg2-binary`, `semgrep`, `playwright`
- **change:** Scaffold task 1 (tasks.md:15) pins z3-solver/hypothesis/jsonschema/psycopg2-binary/semgrep/playwright but NOT k6, Lighthouse, or axe-core; and no workflow runs perf/a11y as a status check, so the capability has no execution path in CI. Add k6 / Lighthouse / @axe-core/playwright (or playwright-axe) to the task-1 dependency pins, and add a CI step (extend coverage-gate.yml or add a perf-a11y job) that runs the perf/a11y/render checks for NFR-subtype items — mirroring the Playwright step in task 15.1.
- _adds EARS/use-case wiring_

**268. Add a task to create .github/workflows/deepeval-gate.yml and register deepeval-gate as a REQUIRED status check**  ·  _[deepeval_gate.py]_
- **anchor:** - [ ] 55. Eval-gating in CI (**REQ-EVAL-001**) — implement `tools/deepeval_gate.py` with DeepEval `assert_test()` ... register `deepeval-gate` as a REQUIRED CI status check at merge.
- **change:** Add a subtask 55.1 (mirroring task 40.3 / task 56) 'Create .github/workflows/deepeval-gate.yml running `pytest tools/deepeval_gate.py` (or `deepeval test run`), and register `deepeval-gate` as a REQUIRED status check in the GitHub repository ruleset; document in docs/github-ruleset.md.' Today task 55 only implements the Python module — unlike zap-baseline (task 56) which creates its `.github/workflows/zap-baseline.yml`, and secrets-scan/audit-chain-verify (tasks 40.2/40.3) which create their workflows. The design.md:149 row points at `tools/deepeval_gate.py` (a module), so the 'REQUIRED CI status check at merge' claim has no workflow file or ruleset-registration task behind it.
- _adds EARS/use-case wiring_

**269. Pin deepeval in the dependency manifest**  ·  _[deepeval_gate.py]_
- **anchor:** Create `pyproject.toml` (or `requirements.txt`) pinning `z3-solver`, `hypothesis`, `jsonschema`, `psycopg2-binary`, `semgrep`, `playwright`
- **change:** Add `deepeval` (version-pinned) to the dependency-pin list in task 1 (line 15), alongside the existing pins. tools/deepeval_gate.py (task 55) imports DeepEval but the package is never declared, so a Phase-3 build would have an undeclared/unpinned dependency.
- _adds EARS/use-case wiring_

**270. Add zap-baseline (and deepeval-gate) to the canonical required-status-checks enumeration**  ·  _[zap-baseline.yml]_
- **anchor:** Task 14.2, line 270: 'Document (in `docs/github-ruleset.md`) the required status checks: `formal-verification`, `coverage-gate`, `secrets-scan` (task 45), `audit-chain-verify` (task 52), plus human PR reviewer requirement'
- **change:** Append `zap-baseline` (task 56) and `deepeval-gate` (task 55) to the required-status-checks enumeration at line 270 so the canonical merge-gate list matches the REQUIRED-check obligations of REQ-SEC-008.2 (`zap-baseline`) and REQ-EVAL-001.3 (`deepeval-gate`). Resulting list: `formal-verification`, `coverage-gate`, `secrets-scan` (task 45), `audit-chain-verify` (task 52), `zap-baseline` (task 56), `deepeval-gate` (task 55), plus human PR reviewer requirement.
- _adds EARS/use-case wiring_

**271. Add task 40.4/40.5 subtasks that create-and-register zap-baseline (and deepeval-gate) as REQUIRED CI checks**  ·  _[zap-baseline.yml]_
- **anchor:** Task 40 'Integrate `formal_verification_merged.py` as required CI check (Phase 4 hardening)' (line 689); existing subtasks 40.1 (line 690), 40.2 secrets-scan (line 695), 40.3 audit-chain-verify (line 700); dependency-graph wave 33 `"tasks": ["40.1", "40.2", "40.3"]` (line 875)
- **change:** Add a subtask 40.4 'Create `.github/workflows/zap-baseline.yml` and register `zap-baseline` as a REQUIRED status check' mirroring 40.3: run `zaproxy/action-baseline@v0.12.0` with `fail_action: true` against the defined scan target; add the workflow to the GitHub repository ruleset as a required check; document in `docs/github-ruleset.md`; _Requirements: REQ-SEC-008 (31.2)_. Add a parallel 40.5 for `deepeval-gate` (REQ-EVAL-001.3, task 55). Add `40.4` (and `40.5`) to dependency-graph wave 33 at line 875 alongside 40.1/40.2/40.3. Rationale: required-status-check registration is a Phase-4 task-40 activity; the Phase-2 build tasks 55/56 cannot themselves register a Phase-4 ruleset gate, leaving REQ-SEC-008.2 and REQ-EVAL-001.3 unscheduled in every phase. Cross-link task 56 (and 55) to the new 40.x subtask.
- _adds EARS/use-case wiring_

**272. Add 3.1, 5.4, and 29.1 omission emission to task 10.1 requirement map and behaviors**  ·  _[initializer.md]_
- **anchor:**   - [ ] 10.1 Write `.claude/agents/initializer.md` — Spec Compiler + Coverage Model Builder ... _Requirements: 1.1, 1.2, 1.3, 1.4, 2.1, 2.2, 2.3, 4.1, 4.2, 4.3, 4.4, 4.5, 5.1, 18.2_
- **change:** Extend task 10.1's requirement map from '1.1, 1.2, 1.3, 1.4, 2.1, 2.2, 2.3, 4.1, 4.2, 4.3, 4.4, 4.5, 5.1, 18.2' to additionally include 3.1 (initializer triggers Research_Sub_Agent on a new/undefined product class — currently only research task 10.4 maps 3.1), 5.4 (initializer must emit WIRING and NFR items as first-class coverage items, per Property 1 'Validates 5.1, 5.4'), and 29.1/REQ-SPEC-018 (initializer output must carry a non-null omission_declaration). Add three corresponding bullets to the key-behaviors list: 'trigger the Research subagent when the detected product class has no approved checklist'; 'emit WIRING and NFR coverage items as first-class entries, not comments'; 'emit a non-null omission_declaration enumerating, by EARS scenario category {Primary/Alternate/Exception/Recovery/Non-Functional/Edge-Case}, every scenario class not covered, each with a [Gap] marker (REQ-SPEC-018).'
- _adds EARS/use-case wiring_

**273. Wire task 10.1's loop/timeout defaults to the task-44 config module**  ·  _[initializer.md]_
- **anchor:**     - Include HANDOFF conditions: no-progress (count doesn't decrease) → immediate HANDOFF; pass cap reached → HANDOFF
- **change:** Add a dependency note in task 10.1 that the initializer's bounded spec-completion loop (DEFAULT 7 passes, SPEC_COMPLETION_HARD_CAP) and the Z3 validator timeout (DEFAULT 60s) are READ from the centralized execution-bounds config module built in task 44 (tasks.md:956), which explicitly lists 'loop 7, timeout 60s' among its operator-overridable DEFAULTs — the initializer consumes but does not own these thresholds. The current task 10.1 references the 7-pass cap with no link to its owning config module.
- _adds EARS/use-case wiring_

**274. Kill-switch (REQ-CTRL-001) instrumentability not declared for implementer.md as an agent entry point**  ·  _[implementer.md]_
- **anchor:** - [ ] 57. Agent kill-switch (**REQ-CTRL-001**) — deploy self-hosted flagd service
- **change:** Task 57 says 'wire OpenFeature SDK into agent entry points' and REQ-CTRL-001.2 requires the affected agent capability be disabled within ≤30s without process restart, but neither implementer.md's definition (design.md:240-251) nor task 10.2 states the implementer must be a flag-gated, kill-switchable capability. Add a bullet to task 57 (and a note in the design implementer.md section) naming implementer (and the other three agents) as kill-switchable agent entry points whose capability is checked against the flagd flag before a slice starts.
- _adds EARS/use-case wiring_

**275. Add check_checklist_approval implementation subtask under task 50**  ·  _[research.md]_
- **anchor:** - [ ] 50. Checklist-approval gate + research epistemic integrity (**REQ-SPEC-016/017**) (tasks.md:965)
- **change:** Add a subtask under task 50 (mirroring the existing task 49.1 integrity-guard subtask): '50.1 Add check_checklist_approval(event) to .claude/hooks/pre_tool_use_hook.py — block any Initializer/discovery Write that references a domain-baseline checklist whose `approved_at` is NULL in `domain_baseline_checklists`; chain it with the existing plan/scope/artifact/status/integrity PreToolUse checks. _Requirements: 24.1 (REQ-SPEC-016)._' Also update task 6.1 (tasks.md:130-137): its title and body name exactly four checks (check_plan_approval, check_scope_sequencing, check_artifact_guard, check_status_transition) — note that the checklist-approval check is added later by task 50.1 (and the integrity-guard by 49.1), so the PreToolUse hook is not 'complete' at task 6. The design.md:213 hook-wiring row for the checklist-approval guard currently has NO implementing task.
- _adds EARS/use-case wiring_

**276. Specify research.md frontmatter: web-search tool grant and model, reconciled with permission scope**  ·  _[research.md]_
- **anchor:** - [ ] 10.4 Write `.claude/agents/research.md` — Domain-Baseline Checklist Sourcer (tasks.md:218-221)
- **change:** Task 10.4 and the design.md:269-280 definition specify no agent frontmatter (name, description, tools, model). The agent MUST 'query web sources' (design.md:275) yet is granted only 'read/write to baselines/; no access to ... CI' (design.md:280) with no web/network tool — a contradiction. Add to task 10.4: specify frontmatter `tools:` granting a web-search/fetch capability (e.g. WebSearch / firecrawl) sufficient for competitive-analysis + standards + OSS-reference queries, and a `model:`. Update the Permissions line in design.md:280 to grant the web/network tool alongside `baselines/` read-write, resolving the scope-vs-behavior contradiction.
- _adds EARS/use-case wiring_

**277. Add a phase-ordering constraint so research.md's epistemic gates exist before research is invoked**  ·  _[research.md]_
- **anchor:** Task Dependency Graph wave 9: tasks ['9.2','10.1','10.2','10.3','10.4','11.1'] (tasks.md:779); tasks 50/54 in waves 41/45 (tasks.md:907,923)
- **change:** research.md (task 10.4) is in the Phase-0 parallel wave 9 with NO dependency edge to its epistemic-integrity gate (task 50, REQ-SPEC-016/017) or its omission-declaration gate (task 54, REQ-SPEC-018) or the checklist-approval test (39.17/39.19), all of which are Phase 3+ (waves 41/45). The agent is buildable-and-mergeable ~3 phases before its gates exist, so during Phase 0-2 research.md could emit unlabeled/unfact-checked DRAFT claims and an un-gated DRAFT checklist could be used for discovery. Either (a) add a Notes constraint that research must not be INVOKED on an un-gated product class until tasks 50/54 land, or (b) add a dependency edge / note that task 10.4's research-invocation path is gated by 50/54. Document the intentional Phase-0-build / Phase-3-gate deferral explicitly in the task 10.4 body.
- _adds EARS/use-case wiring_

### ⚪ UNDERSPECIFIED (31)

**278. Add a CI git-blob-SHA check enforcing checklist_ref.sha fidelity**  ·  _[feature_list.json]_
- **anchor:** Property 18 Checklist Version Link Fidelity (design.md:1018-1022); task 39.9 (tasks.md:626-630)
- **change:** Property 18 asserts checklist_ref.sha matches the actual git blob SHA, but no task computes the git blob SHA in CI — task 39.9 only writes the property test. Add a CI/task step (extend task 39.9 or the coverage-gate workflow) that runs `git hash-object` on checklist_ref.path and fails if it does not equal feature_list.json's recorded checklist_ref.sha, wiring Property 18 to an actual deterministic check.
- _adds EARS/use-case wiring_

**279. Add subtasks for emit_handoff_summary, write_run_state, and the slice-history counters**  ·  _[stop_hook.py]_
- **anchor:** task 5 / 5.1 (tasks.md:95-102) — enumerates only load_feature_list/load_run_state/check_no_progress/evaluate_stop/with_reentrancy_guard
- **change:** evaluate_stop references emit_handoff_summary, write_run_state, count_items_proven_since, and count_commits_since (design.md:700-713,663-672) and the budget branch (design.md:704) but none of these helpers is tasked anywhere; task 5.1 stops at evaluate_stop/with_reentrancy_guard. The HANDOFF-summary content contract (remaining unproven items, last N slice outputs, reason — design.md:828) also has no subtask. Add subtasks under task 5: (5.1b) emit_handoff_summary implementing the design.md:828 content contract; (5.1c) write_run_state/save_run_state persistence; (5.1d) count_items_proven_since / count_commits_since slice-history counters used by check_no_progress.

**280. Capture the N-25 Postgres-unavailable fallback gate-consistency constraints in the Stop-hook load_run_state spec**  ·  _[stop_hook.py]_
- **anchor:** task 5.1 line 97 `Implement load_feature_list() and load_run_state() (reading from feature_list.json and claude-progress.txt / in-memory fallback before Postgres)`
- **change:** Note N-25 (tasks.md:730) is a load-bearing obligation on the Stop coverage gate: when db_connection.health_check() fails, the gate MUST fall back to file-backed state (feature_list.json + claude-progress.txt) and reach the SAME allow/block decision it would on Postgres — 'never fail-open', and 'on divergence the gate takes the more conservative (blocking) result.' task 5.1's load_run_state bullet captures only the read order, dropping the three correctness constraints (file-fallback equivalence, never-fail-open, conservative-on-divergence). Add these three constraints to the load_run_state bullet and cross-reference Note N-25.

**281. Define the full ordered 6+-guard chain in task 6.1 and update 'Chain all four checks'**  ·  _[pre_tool_use_hook.py]_
- **anchor:** Task 6.1 'Chain all four checks; return first blocking decision' (tasks.md:136); task 49.1 'chain it BEFORE the existing plan/scope/artifact/status checks' (tasks.md:964)
- **change:** Replace 'Chain all four checks' with an explicit ordered chain that includes every guard the design assigns to this hook: integrity-guard (task 49.1) → plan → scope → artifact → status → checklist-approval (+ secret-block once added). Task 49.1 says the integrity guard chains BEFORE plan/scope/artifact/status, but task 6.1 still defines a four-check chain with no slot for the integrity or checklist guards, so the actual ordering across the 6 guards is unspecified. Pin the order here so the build is deterministic.

**282. Define the scope-gate's exact command/argument detection patterns**  ·  _[pre_tool_use_hook.py]_
- **anchor:** Task 6.2 `check_scope_sequencing` (tasks.md:133); design.md:211 scope-gate row; CHECK-6a (formal_verification_merged.py:288)
- **change:** Specify the exact Bash command/argument patterns the scope gate inspects (e.g. `git worktree add`, the worktree_manager.py create entry points, and any slice-assign command surface) and the tool surfaces it covers, in task 6.2 and the design.md:211 row. Add a property test for bypass forms (worktree created via a wrapper script or non-Bash tool) and note that CHECK-6a proves only the abstract scopeGateBlocks boolean, not the concrete command detection. As written, 'Bash worktree-create or slice-assign call' has no precise detection definition, so the gate can be bypassed.

**283. Make OTel hook.decision span honest for non-gating hooks (SessionStart/PreCompact)**  ·  _[session_start_hook.py]_
- **anchor:** Task 36.1 'Extend all six hooks ... to emit OTel gate-decision events' — bullet setting attributes hook.event/tool.name/decision(allow/block)/reason/requirement.id on hook.decision span (tasks.md:539-542)
- **change:** Task 36.1 sets a 'decision' (allow/block) attribute uniformly on a 'hook.decision' span for ALL six hooks, but SessionStart and PreCompact never make an allow/block gate decision (always exit 0), so decision=allow/block is degenerate; and requirement.id is ambiguous for SessionStart (tagged with BOTH REQ-STATE-003 and REQ-STATE-005 per design.md:219). Specify the attribute set per hook class: for non-gating hooks (SessionStart, PreCompact) emit an informational span (distinct name e.g. 'hook.session_start' or decision='n/a') and either omit requirement.id or list both ids explicitly, rather than a gate-decision allow/block attribute — so the trace schema reflects which hooks actually gate. (REQ-OBS-003 / criterion 12.3 covers only true gate decisions.)

**284. Specify audit_log.append is called inside the decision path before exit (ordering)**  ·  _[subagent_stop_hook.py]_
- **anchor:** Task 52.3 'Wire audit-log producer into the three hooks — modify stop_hook.py, pre_tool_use_hook.py, and subagent_stop_hook.py to call tools/audit_log.append(...) on every allow/block decision' (tasks.md:970); and the Postgres-fallback note (tasks.md:730)
- **change:** Specify in task 52.3 (and the new design subsection) that for subagent_stop_hook.py the audit_log.append call is invoked INSIDE the decision path BEFORE the exit, wrapped so an append failure degrades to the file-backed log (per the tasks.md:730 fallback) and NEVER suppresses or skips the gate decision/exit. Add a unit test: 'audit_log.append raises (e.g. Postgres down before file-fallback engages) -> the allow/block decision is still recorded to the file-backed log AND the hook exits with the correct code'. Without this, a fail-closed unhandled-exception exit 2 (task 8.1 / tasks.md:182) could fire before any audit entry is written, breaking REQ-AUDIT-001's 'on every allow/block decision' obligation (requirements.md:460).

**285. Drop misleading `git add -f` on non-gitignored checkpoint files (or add the intended .gitignore entry)**  ·  _[pre_compact_hook.py]_
- **anchor:** tasks.md:394 `Commit both files via `git add -f ... && git commit -m "chore: PreCompact checkpoint [skip ci]"` if anything has changed`
- **change:** The `-f` (force-add) implies claude-progress.txt / the snapshot are gitignored, but the actual .gitignore (lines 1-47) ignores only __pycache__, venvs, dist/build, test/coverage caches, .idea//.vscode/, OS files, .playwright-mcp/, and .env* — there is NO claude-progress entry. Resolve the intent mismatch: either (a) drop the `-f` so it reads `git add claude-progress.txt feature_list.json` (the files are tracked normally), or (b) if checkpoint files are meant to be ignored, add them to .gitignore and make task 1 (tasks.md:17 'Create ... .gitignore entries') actually create that entry — which justifies the `-f`. Recommend (a) given SessionStart must read feature_list.json back.

**286. Decide empty/zero-byte artifact validity in collect()**  ·  _[evidence_collector.py]_
- **anchor:** Task 3.3, tasks.md:57 — 'collect() with empty bytes still produces valid sha256 hash'
- **change:** tasks.md:57 asserts empty bytes 'still produces valid sha256 hash' (sha256('') = e3b0c442...), but the spec never decides whether a zero-byte artifact is a VALID proof — such a hash is well-formed and passes all four-field and schema checks, so the gate would silently accept an empty/short-circuited test as proof. Decide and specify one of: (a) collect() raises on empty output_bytes; or (b) the Verifier must assert the artifact is non-trivial before calling collect(). Then make tasks.md:57's test assert the chosen behavior (reject-empty, or document why empty is acceptable) rather than blessing the meaningless hash. Add a matching EARS criterion or note under Requirement 5.3 / 9.3 if option (a) changes the collector contract.

**287. Add evidence-quality caveat: no Z3 check exercises collect()'s real sha256/datetime/format behavior**  ·  _[evidence_collector.py]_
- **anchor:** Task 3.3, tasks.md:56-61 (unit tests for evidence_collector.py) and the CHECK-7 family in verification/formal_verification_merged.py:301-338
- **change:** CHECK-7a/7b/7c (formal_verification_merged.py:311-338) operate purely on abstract booleans (`evidenceComplete == And(evidenceHasHash, evidenceHasTestFile, ...)`, line 302) — they model the four-field GATE LOGIC, not the collector's hashing/timestamp behavior. NO Z3 check exercises collect()'s actual sha256 computation, datetime output, or the `^sha256:[a-f0-9]{64}$` format; those are only unit-tested (task 3.3, tasks.md:57-59). Add an explicit caveat to task 3.3 (or the testing-strategy note) stating that the sha256/timestamp/format correctness is unit-test-verified only and is NOT in the Z3 coverage, so the Z3 'evidence schema enforced' claim is not mistaken for runtime hash-format coverage.

**288. Changed-files input contract between post_tool_use_hook.py and wiring_checker.py is unspecified (incl. MultiEdit)**  ·  _[wiring_checker.py]_
- **anchor:** Task 18.1 tasks.md:310 'Triggered on Write, Edit, MultiEdit completion' / tasks.md:313 'Run python3 tools/wiring_checker.py on changed files'; and task 19.1 tasks.md:326 'Accept a list of changed files as CLI args'
- **change:** Specify the handoff contract on both sides: in task 18.1 state how the PostToolUse hook derives the changed-file list from the hook payload (e.g. tool_input.file_path for Write/Edit; the set of edited paths for MultiEdit, since one MultiEdit call can mutate multiple files) and passes them as argv; in task 19.1 state the exact argv format (one file path per positional arg). The matcher is Write|Edit|MultiEdit (design.md:178,216) so the MultiEdit multi-file case must be handled. Today neither side pins the format.

**289. Define 'verification artifact' concretely for the backward-orphan check**  ·  _[orphan_detector.py]_
- **anchor:** Task 20.1 bullet (tasks.md:342) and Property 11 (design.md:962-966)
- **change:** Define what counts as a 'verification artifact' so the backward-orphan pass/fail boundary is implementable. The Postgres schema already provides the exact candidate definition (design.md:457-458 fixes traceability_links.link_type to the closed set including 'test'/'evidence'; design.md:465-477 defines evidence_records) but it is never bound to task 20's rule. Add to task 20.1 AND Property 11: a backward orphan = a requirement ID with NO traceability_links row of link_type IN ('test','evidence') AND/OR no evidence_records row. State this is the canonical definition the check keys on.

**290. Pin orphan_detector run scope, file glob, language scope, and excludes**  ·  _[orphan_detector.py]_
- **anchor:** Task 20.1 bullet (tasks.md:340) 'Scan all source files...'
- **change:** Resolve scan-scope ambiguity. tasks.md:340 says 'all source files' (full-repo, CI-suited) while the sibling wiring_checker/PostToolUse pipeline operates on changed files (tasks.md:313,326). Add bullets to task 20.1: (1) state full-repo scope (recommended, since it is wired as a CI blocking gate per the traceability-gate edit); (2) define the file glob and exclude list (e.g. exclude tests/, generated dirs, __init__.py, migrations); (3) state the language scope (Python source). This determines where it can be wired and bounds false positives.

**291. Add an orphan-exemption / allowlist mechanism to bound forward-orphan false positives**  ·  _[orphan_detector.py]_
- **anchor:** Task 20.1 bullet (tasks.md:342) 'source files/functions with no requirement ref (forward orphans)' and Property 11 (design.md:962-966)
- **change:** Add an exemption mechanism so the forward-orphan rule does not fail-noisy on legitimately un-annotated units (helpers, __init__.py, config, generated code). Add to task 20.1 and Property 11: an explicit ignore mechanism — a `# orphan-exempt: <reason>` inline marker AND/OR a config file of excluded paths — and define granularity (file-level vs function-level). Without this the check is unimplementable as a non-noisy blocking gate.

**292. Disambiguate the requirement-ID regex for multi-segment IDs in the orphan scan**  ·  _[orphan_detector.py]_
- **anchor:** Task 20.1 bullet (tasks.md:340) regex `[A-Z]+-[A-Z]+-[0-9]{3}`; CoverageItem.id pattern `^[A-Z]+-[A-Z]+-[0-9]{3}$` (design.md:328)
- **change:** Clarify the ID regex's match scope. The detector scans for unanchored `[A-Z]+-[A-Z]+-[0-9]{3}` (tasks.md:340) and ties it to CoverageItem.id `^[A-Z]+-[A-Z]+-[0-9]{3}$` (design.md:328), but the actual feature_list.json IDs are multi-segment (REQ-TRACE-001, REQ-VERIFY-007, REQ-SPEC-018; requirements.md:137, design.md:146,148) and the EARS dotted short-IDs (e.g. 6.3) are a different namespace the same regex cannot match. Add a bullet stating the regex matches the canonical 3-segment CoverageItem.id form (unanchored within comments/trailers), explicitly NOT the dotted EARS criterion numbers, and confirm it matches all REQ-<DOMAIN>-NNN IDs present in feature_list.json.

**293. Add enum-sync consistency test (JSON schema enums vs Postgres CHECK / spec_validator)**  ·  _[feature_list.schema.json (schema/)]_
- **anchor:** tasks.md:24-29 (task 2) and design.md:333,347 (JSON type/ears_pattern enums) vs design.md:434,436 (requirements table: type CHECK present, ears_pattern TEXT NOT NULL with NO CHECK)
- **change:** Add a consistency test task asserting the JSON schema's `type` enum {functional,NFR,WIRING} equals the coverage/requirements Postgres `type` CHECK (design.md:434) and the spec_validator pattern set, and that the schema's `ears_pattern` enum (5 base patterns, design.md:347) is kept in sync with spec_validator (design.md:1188) — note that requirements.ears_pattern at design.md:436 is `TEXT NOT NULL` with NO CHECK, so that enum can silently drift. There is currently no task ensuring these three enum sources stay aligned.
- _adds EARS/use-case wiring_

**294. No migration runner, schema_migrations bookkeeping, or apply-on-deploy step specified**  ·  _[db/migrations/ (all 8 tables 001-008)]_
- **anchor:** Task 28 header 'Postgres schema migrations' (tasks.md:423); only application path is the integration tests (28.9, 33.1) on a test Neon branch
- **change:** Add a sub-task under 28 (and a matching design subsection) naming the migration runner and a `schema_migrations` tracking table: pick one of golang-migrate / dbmate / sqitch / alembic (decide a default), define the apply order = file-name prefix 001..008, and add a CI/deploy step that applies pending migrations to the target (non-test) Neon branch on merge/deploy — distinct from the test-branch application in 28.9. Currently no runner, no bookkeeping table, and no real-DB apply step exist anywhere.
- _adds EARS/use-case wiring_

**295. No idempotency / re-run / rollback contract for the migrations**  ·  _[db/migrations/ (all 8 tables 001-008)]_
- **anchor:** Task 28 'Postgres schema migrations' (tasks.md:423-456); migration 008 gate_audit_log (28.8) is append-only
- **change:** State the idempotency contract for the migration set: either use one-time tracked apply via the schema_migrations table OR `CREATE TABLE IF NOT EXISTS`; declare whether DOWN/rollback files exist (and if not, say migrations are forward-only); explicitly document `gate_audit_log` (008) as create-once so a re-run does not error or duplicate the append-only log. No idempotency/rollback text exists today.

**296. evidence_records has no uniqueness/index to dedup or support requirement_id+commit_sha lookup**  ·  _[db/migrations/ (all 8 tables 001-008)]_
- **anchor:** 28.4 Write migration `db/migrations/004_evidence_records.sql`; Property 19 retrieval by requirement_id+commit_sha (design.md:1028) and Property 22 COUNT (design.md:1052)
- **change:** Add to migration 004 a `UNIQUE (requirement_id, commit_sha, test_name)` constraint (or at least an index on `(requirement_id, commit_sha)`) so duplicate evidence rows for the same requirement/commit/test are rejected and Property 19 lookup is indexed. Without it, duplicate inserts are silently allowed and can inflate the Property 22 OPA COUNT.

**297. Phase-2 integration tests assert only generic existence for requirement_versions, not its constraints**  ·  _[db/migrations/007_requirement_versions.sql]_
- **anchor:**     - Against a test Neon branch: run all 8 migrations (001–008); assert all EIGHT tables exist (`requirements`, `coverage_items`, `traceability_links`, `evidence_records`, `run_state`, `domain_baseline_checklists`, `requirement_versions`, `gate_audit_log`) with correct columns and constraints *(Reconciliation 2026-06-15: was "all 6 migrations")*
- **change:** Strengthen task 33.1 (tasks.md:501) and task 28.9 (tasks.md:459) to assert requirement_versions-specific constraint behavior, matching the granularity already applied to evidence_records/coverage_items: assert the `UNIQUE (requirement_id, version)` constraint rejects a duplicate (requirement_id, version) insert; assert `created_at` is populated by DEFAULT now() when omitted; assert the `requirements(id)` FK rejects a row with a dangling requirement_id. Currently both tasks cover this table only by generic 'correct columns and constraints'.

**298. Specify how file-backed (DB-down) audit entries reconcile their seq on reconnect**  ·  _[db/migrations/008_gate_audit_log.sql]_
- **anchor:** tasks.md:730 `... The \`gate_audit_log\` producer (task 52.1) appends to the file-backed log when the DB is unavailable and reconciles on reconnect.`
- **change:** Under DB-down, the producer appends to the file-backed log and reconciles on reconnect, but seq (BIGSERIAL) does not exist in the file backend and seq is part of the hashed canonical form — so DB-assigned seq on reconnect differs from any file placeholder and re-hashing cannot reproduce the file-time entry_hash; reconciliation is undefined. In tasks.md:730 and Req 27 (requirements.md:461), specify either (a) a client-generated monotonic seq (UUIDv7 / hybrid logical clock) that survives both backends and is what gets hashed, or (b) that file-era entry_hashes are explicitly recomputed and re-chained against DB-assigned seq on reconnect (and that audit_verify treats the reconnect boundary accordingly).
- _adds EARS/use-case wiring_

**299. Specify chain-linearization / concurrency control for audit_log.append**  ·  _[audit_log.py]_
- **anchor:** Line 968 (task 52.1): 'Audit-log PRODUCER tools/audit_log.py — expose append(...)' — and the gate_audit_log DDL at design.md:1156-1167
- **change:** The hash chain is strictly sequential (entry_hash = sha256(canonical_row || prev_hash) and INCLUDES the BIGSERIAL seq, per design.md:1166/1172), so two concurrent append() calls from different hook processes that read the same prev_hash but receive distinct seq values fork the chain at identical prev_hash — exactly the prev_hash != recomputed(prior) condition audit_verify treats as tampering (Property 28), a guaranteed false positive that bricks the merge gate. Add to task 52.1: append() MUST serialize prev_hash-read + insert under a lock (e.g. pg_advisory_xact_lock keyed on the log, or SELECT … FOR UPDATE on a chain-head sentinel) within one transaction so the chain linearizes. Mirror this as an Error-Handling note.

**300. Add audit_log.py pseudocode and a dedicated write-side unit-test subtask**  ·  _[audit_log.py]_
- **anchor:** Line 968 (task 52.1): 'Audit-log PRODUCER tools/audit_log.py' — has no unit-test subtask; contrast task 39.18 which tests audit_verify.py, not the producer's hashing
- **change:** audit_log.py (the security-critical write side) has no pseudocode block (unlike evaluate_stop in design.md:685-737 and check_plan_approval in design.md:777-799) and no unit-test subtask of its own — only the indirect Property 28 PBT (task 39.18) which exercises audit_verify.py. Add a unit-test subtask under task 52.1 (mirroring the Phase-0 'Test to pass' pattern at design.md:621-637) asserting: (a) genesis prev_hash == sha256(""); (b) entry_hash determinism for a fixed input tuple; (c) a round-trip append→audit_verify passes on an intact chain. Optionally add an audit_log.append pseudocode block to design.md (read-prev-hash → canonicalize JSON → sha256 → insert).

**301. State audit-completeness scope start point (pre-52.3 decisions unlogged) for REQ-AUDIT-001**  ·  _[audit_log.py]_
- **anchor:** Line 970 (task 52.3): 'Wire audit-log producer into the three hooks'
- **change:** The producer is wired into its caller hooks only at task 52.3, so during Phase 0/1 (and most of 2/3) the Stop/PreToolUse/SubagentStop hooks make gate decisions WITHOUT producing audit entries — the 'append every gate decision' invariant (REQ-AUDIT-001, requirements.md:457) does not hold until 52.3 lands. Either (a) add an explicit note at task 52.3 that REQ-AUDIT-001 completeness begins at 52.3 and pre-52.3 decisions are out of audit scope, or (b) move the append wiring earlier (it only needs the file-backed fallback, which has no Phase-2 DB dependency) so the chain is complete from Phase 0.

**302. Specify structured failure report for audit_verify.py**  ·  _[audit_verify.py]_
- **anchor:**   - [ ] 52.2 Audit-chain verifier `tools/audit_verify.py` — recompute the chain and fail on any broken link.
- **change:** Add to task 52.2 that on a broken link audit_verify.py emits a machine-readable report to stdout (JSON: `first_broken_seq`, `expected_hash`, `stored_prev_hash`, `computed_prev_hash`) in addition to the non-zero exit, so the audit-chain-verify CI check (task 40.3) can annotate the specific failing entry. Currently every spec mention defines only an opaque non-zero exit, identifying neither which `seq` broke nor the expected-vs-actual hashes.

**303. Add positive intact-chain producer-contract reproduction to Property 28 test scope**  ·  _[audit_verify.py]_
- **anchor:**   - [ ]* 39.18 Write `tests/property/test_audit_log.py` — **Property 28: Audit-Log Tamper Detection (REQ-AUDIT-002)**
- **change:** Broaden task 39.18's scope (currently '_Requirements: REQ-AUDIT-002_', tamper detection only) to also assert a POSITIVE intact-chain reproduction of the producer contract the verifier must reproduce: genesis `prev_hash = sha256("")`, the exact canonical field-set and ordering `(seq, event_name, tool_name, decision, reason, requirement_id, actor_agent, created_at)`, and that `seq`+`created_at` are INCLUDED in `entry_hash` (REQ-AUDIT-001 / Req 27.5). Update the requirements line to '_Requirements: REQ-AUDIT-001, REQ-AUDIT-002_'. Today the only Property-28 test maps to REQ-AUDIT-002 alone, so the verifier's reproduction of the REQ-AUDIT-001/27.5 producer contract is untested.

**304. Define an acceptance/fixture test for secrets-scan.yml ('done' is currently undefined)**  ·  _[secrets-scan.yml]_
- **anchor:** Task 45 at line 957 and task 40.2 at lines 695-698 (neither defines any fixture/acceptance test); contrast Phase-0 spine 'Test to pass' column at design.md:621-636
- **change:** No acceptance/integration test is specified for this component anywhere (the Phase-0 spine table has a 'Test to pass' column but no secrets-scan row since it is Phase 1, and tasks 40.2/45 define no fixture). Add an explicit pass/fail test to task 45 (or task 40.2), e.g. 'a fixture PR diff containing a fake AWS key -> workflow fails (merge blocked); a clean diff -> workflow passes', placed in the existing integration suite (e.g. tests/integration/test_github_actions.py). This both gives the component a 'done' definition and supplies the fixture referenced by the new correctness property/no-Z3 note.

**305. Add explicit on: trigger block to secrets-scan task (sole sibling workflow lacking one)**  ·  _[secrets-scan.yml]_
- **anchor:** Task 40.2 at line 696 ('Run `gitleaks` (or `trufflehog`) on the PR diff ...') — specifies only 'on the PR diff', no on: block
- **change:** Every comparable required-check workflow in tasks.md states its trigger explicitly (codeql.yml 'Trigger on push and pull_request' tasks.md:379; coverage-gate 'Trigger on pull_request' tasks.md:263; formal-verification 'Trigger on push and pull_request' tasks.md:251; property-tests 'Trigger on every pull_request' tasks.md:707); secrets-scan.yml is the sole exception. Add an explicit trigger line to task 40.2, e.g. 'Trigger on pull_request (scan the PR diff)', matching the sibling-workflow convention.

**306. Specify the zap-baseline workflow `on:` trigger (on every PR)**  ·  _[zap-baseline.yml]_
- **anchor:** Task 56, line 976: '56. DAST baseline security scan (**REQ-SEC-008**) — add `.github/workflows/zap-baseline.yml` using `zaproxy/action-baseline@v0.12.0` with `fail_action: true`; register `zap-baseline` as a REQUIRED CI status check; baseline passive scan only ...'
- **change:** Add the workflow trigger to task 56 (and the design.md:150 inventory row): `on: pull_request` (optionally with a target-branch filter for the protected branch), so REQ-SEC-008.1's 'on every PR' prose has a concrete mechanism. Mirror how task 41.1 (property-tests, line 707) states 'Trigger on every `pull_request`'. No canonical file currently defines any `on:` block, branch filter, or event scope for zap-baseline.

**307. flagd provider choice 'file-based or gRPC' left open; ≤30s guarantee unbacked**  ·  _[flagd]_
- **anchor:** - [ ] 57. Agent kill-switch (**REQ-CTRL-001**) — deploy self-hosted flagd service (`flagd/` directory, Apache 2.0, CNCF Incubating); wire OpenFeature SDK into agent entry points; verify near-real-time flag propagation (≤ 30 s) without process restart.
- **change:** Amend task 57 to pick a concrete provider rather than leaving 'file-based or gRPC' open: specify either gRPC streaming (near-instant propagation) OR file-based polling with an explicit poll interval ≤30 s, and tie the ≤30 s verification step to the chosen provider's mechanism. Also add the scaffold deliverables: create the `flagd/` directory with flag-definitions file (flagd.json), provider config, and a self-hosted deployment manifest, plus the kill-switch flag schema enumerating flag name(s) and gated capabilities.

**308. Decompose task 10.1 into testable subtasks with acceptance tests**  ·  _[initializer.md]_
- **anchor:**   - [ ] 10.1 Write `.claude/agents/initializer.md` — Spec Compiler + Coverage Model Builder
- **change:** The initializer carries the heaviest requirement load of any subagent yet task 10.1 is a single subtask whose only acceptance test (design.md:628) is 'Hand-run: produces valid feature_list.json from sample intent'. Decompose into testable steps each with an acceptance check: (a) spec-compile to EARS + atomic IDs; (b) validator-loop with 7-pass cap and strict-decrease no-progress → HANDOFF; (c) discovery + UNMAPPED flagging; (d) provenance marking (stated/inferred) with per-inferred-item human confirmation gating before items enter feature_list.json (Req 2.3 — currently only bulk plan-mode review); (e) WIRING/NFR first-class emission (5.4); (f) feature_list.json seed+populate; (g) plan-mode handoff without writing plan-approved.json; (h) omission_declaration emission (29.1). Add a behavioral fixture beyond the single hand-run.


## schema/feature_list.schema.json — 2 edits

### 🟡 MISSING-WIRING (1)

**309. Schema file does not exist — materialize it and check off task 2.1**  ·  _[feature_list.schema.json (schema/)]_
- **anchor:** design.md:290-413 (the inline FeatureList JSON Schema block) and tasks.md:25 ([ ] 2.1 Implement schema/feature_list.schema.json)
- **change:** Create the file schema/feature_list.schema.json by materializing the design.md:290-413 draft-07 schema verbatim (incorporating the fixes in the sibling edits below), and flip task 2.1 in tasks.md:25 from [ ] to [x]. Every Phase-0 gate (PreToolUse status guard, feature_list_init validation at tasks.md:240, design.md:623 acceptance smoke) depends on this concrete artifact existing; today there is no schema/ directory and git ls-files returns nothing.
- _adds EARS/use-case wiring_

### ⚪ UNDERSPECIFIED (1)

**310. Add plan_approved.schema.json, validate before indexing, and test the corrupt-file path**  ·  _[plan-approved.json]_
- **anchor:** Inventory row `schema/` (design.md:138); check_plan_approval bare index `approval["feature_list_sha"]` (design.md:792); tasks 6.1 (tasks.md:132) and smoke tests (tasks.md:285, 295-296)
- **change:** Author a sibling `schema/plan_approved.schema.json` (required: approved_by, approved_at, feature_list_sha, spec_version; notes optional) and have check_plan_approval validate the parsed approval object against it BEFORE indexing `approval["feature_list_sha"]`, so a present-but-malformed file (missing key / non-JSON) fails closed via an explicit schema check rather than relying on the bare KeyError + try/except. Add a smoke/integration test for the present-but-corrupt case (currently only the ABSENT case is tested at tasks.md:285 and 295) asserting exit 2 when plan-approved.json exists but is malformed or missing feature_list_sha.


## verification/formal_verification_merged.py — 8 edits

### 🟠 INCONSISTENCY (1)

**311. Make CHECK-9a citation tag cite the canonical UNMAPPED criterion**  ·  _[initializer.md]_
- **anchor:** ], "unsat", "CHECK-9a: Advancement while UNMAPPED items exist is UNSAT [REQ-SPEC-011]")
- **change:** CHECK-9a's description tag reads '[REQ-SPEC-011]', but the prose criterion for UNMAPPED-blocks-advancement is Requirement 2.2, mapped to the REQ-SPEC-010..012 range (requirements.md:78,384). REQ-SPEC-011 is within range but is not the canonical single criterion ID. Change the tag to '[REQ-SPEC-012 / Requirement 2.2]' (or '[Requirement 2.2 / REQ-SPEC-010..012]') to remove the dangling-citation ambiguity. Minor.

### 🟡 MISSING-WIRING (3)

**312. Zero Z3 coverage for the PostToolUse never-blocks invariant**  ·  _[post_tool_use_hook.py]_
- **anchor:** New CHECK in the 'New checks' section after CHECK-13b (formal_verification_merged.py:500), parallel to the HANDOFF exit-0 checks CHECK-5b/5c/8c (lines 266-275, 364-370)
- **change:** Add a CHECK pinning the PostToolUse 'cannot block' invariant, parallel to CHECK-5b/5c/8c which pin the Stop-hook HANDOFF exit-0 invariant. Confirmed gap: grep of formal_verification_merged.py for post_tool_use/postHook/lint/sast/wiring returns NO MATCH; the invariant 'PostToolUse MUST exit 1 / never exit 2 / cannot undo' (requirements.md:251, REQ-STEER-002) lives only in prose plus the generic Property 21. Add e.g. a Bool `postHookBlocks` with axiom `Not(postHookBlocks)` (PostToolUse is non-blocking by contract) and a check that `postHookBlocks` (a PostToolUse path that exits 2) is UNSAT — formally pinning the prevention(PreToolUse)/feedback(PostToolUse) split on both sides. Update the self-counting group comment/total accordingly (the harness self-counts checks_run, so no hardcoded figure needs changing, but add the new CHECK label to the printed group description).
- _adds EARS/use-case wiring_

**313. Model the feature_list_sha-mismatch block path in Z3 and scope Invariant 3 wording**  ·  _[plan-approved.json]_
- **anchor:** CHECK-4f at lines 250-253 (`codeWrite, Not(planApproved)` → unsat); Bool decls at lines 68-69
- **change:** Add a Boolean `shaMatches = Bool("shaMatches")` and a core axiom that an implementation write requires BOTH presence and a matching SHA, e.g. `Implies(codeWrite, And(planApproved, shaMatches))`. Add CHECK-4g asserting `codeWrite ∧ planApproved ∧ ¬shaMatches` is UNSAT (the SHA-mismatch block path). Then narrow Critical Invariant 3 (requirements.md:369) and the CHECK-4f description so the existing CHECK-4f claim is scoped to the PRESENCE branch only, and the new CHECK-4g covers the SHA bind — so the docs no longer imply the whole gate (including the cryptographic SHA core) is proven by CHECK-4f alone.
- _adds EARS/use-case wiring_

**314. No machine-checked invariant / Correctness Property pins the kill-switch**  ·  _[flagd]_
- **anchor:** Harness self-count: 'Groups: core-14 (CHECK-1..5c) + Kiro-12 (CHECK-6a..9c) + new-8 (CHECK-10a..13b)' (verification/formal_verification_merged.py:520); highest CHECK is CHECK-13b — grep confirms zero flagd/OpenFeature/CTRL-001/Requirement-32 references
- **change:** Decide and document the verification posture for REQ-CTRL-001. Either (a) add a new CHECK (e.g. CHECK-14a/14b) and a numbered Correctness Property pinning the kill-switch invariant — e.g. 'a set kill-switch flag implies the gated capability is disabled' and 'flagd-unreachable implies disabled (fail-closed)' — updating the harness self-count and the design.md Correctness Properties list (Property 31); OR (b) explicitly state in design.md/requirements.md that REQ-CTRL-001 is runtime/CI-verified only (like Property 28 audit-chain and Property 29 research-authority, which carry 'no Z3 check' notes) with the ≤30 s propagation verified by task 57's integration test rather than Z3. Pick one and record it so the kill-switch is not silently unverified.
- _adds EARS/use-case wiring_

### ⚪ UNDERSPECIFIED (4)

**315. Model the three-state status-transition graph in the Z3 harness or soften the machine-checked claim**  ·  _[feature_list.json]_
- **anchor:** Section 1 coverage/completion Booleans (lines 73-92); harness has no {unproven,proven,failed} enum or edge graph
- **change:** The harness models coverage with standalone Booleans (unproven/passed/evidence*) and never encodes the {unproven,proven,failed} three-state machine or the permitted-edge set (unproven→proven, unproven→failed, failed→unproven) with all other edges UNSAT, so Req 5.8/Property 3's 'machine-checked status graph' is overstated. Add a CHECK group (e.g. CHECK-14a..n) using an Int/enum status variable that asserts each permitted edge is SAT and every non-permitted edge (e.g. proven→failed, proven→unproven without amendment, failed→proven without evidence) is UNSAT, and reference it from requirements.md:123/129. Until added, soften 'machine-checked' wording for the status graph in requirements.md and design.md.
- _adds EARS/use-case wiring_

**316. Add a standalone Z3 check for the budget HANDOFF trigger (blocking-UNSAT and routing-SAT)**  ·  _[stop_hook.py]_
- **anchor:** CHECK-5b (lines 266-269) and CHECK-5c (lines 272-275) — the cap/no-progress HANDOFF-exit-0 UNSAT checks; CHECK-3b (line 207-212) the cap routing-SAT check
- **change:** budgetExceeded appears only inside the core_axioms disjunction (line 152) and as a negated literal in CHECK-1/3b/8b/10b; it has NEITHER a standalone blocking-UNSAT check (mirroring CHECK-5b/5c: `core_axioms + [budgetExceeded, hookExitsBlocking]` expected UNSAT) NOR a routing-SAT check (mirroring CHECK-3b: `core_axioms + [budgetExceeded, handoff, Not(complete), Not(hookExitsBlocking)]` expected SAT). It is the only one of the three HANDOFF triggers with no dedicated check. Add CHECK-5d (budget blocking-UNSAT) and optionally CHECK-3c (budget routing-SAT). Update the harness self-count banner (currently 34: 14 core) and the requirements.md:9 / design.md:1191 '34/34' figure accordingly.

**317. Add a Z3 CHECK family modeling the status-transition edge set including the failed state**  ·  _[pre_tool_use_hook.py]_
- **anchor:** New 8 checks section (CHECK-10a..13b, lines 409-500); model variables section (lines 121-131)
- **change:** Add a CHECK family (e.g. CHECK-14a/b/c) introducing booleans for the status-transition guard: declare edge enums or booleans {edgeUnprovenProven, edgeUnprovenFailed, edgeFailedUnproven, edgeOther} plus evidenceComplete, and assert: (a) any transition into proven without evidenceComplete is UNSAT; (b) edgeOther (any transition not in the permitted set) is UNSAT; (c) edgeUnprovenFailed and edgeFailedUnproven WITHOUT evidence are SAT (the widened edges are reachable). The harness currently has no boolean for the `failed` status or the transition edge set, so the most-recently-widened guard logic (req 5.8 / Property 3) is unverified by the formal model. Update the header self-count comment accordingly.

**318. Add Z3 evidence-at-SubagentStop-acceptance assertion (CHECK-7d)**  ·  _[subagent_stop_hook.py]_
- **anchor:** evidence_schema_axioms block `Implies(passed, evidenceComplete)` (line 308) and CHECK-7a/7c (lines 312-338); model the SubagentStop omission gate at lines 481-500 (subagentAccepted13)
- **change:** Add a CHECK-7d-style assertion that machine-checks the four-field evidence schema at the SubagentStop ACCEPTANCE boundary, symmetric with the omission gate (CHECK-13a). Introduce a `subagentAcceptedEvidence` (or reuse a SubagentStop-acceptance) bool and axiom `Implies(Not(evidenceComplete), Not(subagentAcceptedEvidence))`, then assert `(Not(evidenceComplete) AND subagentAcceptedEvidence)` is UNSAT. Currently the schema is only tied to the `passed` transition variable via `Implies(passed, evidenceComplete)`, never to the SubagentStop accept decision — so this hook's Phase-0 raison d'etre is not formally checked at its own gate. Update the harness self-count/group comments and the requirements.md/design.md '34 assertions' figures accordingly.
