# Supporting Pages — Kiro-style spec (domain: PAGE)

Authoring complete. Structured output returned via StructuredOutput tool.
Domain covers: /how-it-works, /proof (incl. interactive sample audit-trace),
/manifesto (essay structure), /writing (optional), /docs (stub).
Grounded in: based-on-the-attached-eventual-cat.md (brief),
PRD_MERGED_canonical_spec_to_evidence.md, requirements.md, Agentic-Driven SDLC.md.

Key claims-integrity constraint encoded: do NOT publish a "N/N formally verified"
requirement count; the harness self-counts 32/32 machine-checked Z3 assertions
(legacy "21/21" deprecated). Every public claim must be corpus-traceable.
