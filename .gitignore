
!.env.template
# Claude Code / Playwright MCP — regenerable browser-session artifacts, never source
# Distribution / packaging
# IDE
# Local Plane self-host (secrets + local infra)
# OS
# Python
# Secrets — only the template is tracked
# Testing
# Type checking
# Virtual environments
*$py.class
*.cover
*.egg
*.egg-info/
*.py[cod]
*.pyc
*.so
*.swo
*.swp
*~
.DS_Store
.coverage
.coverage.*
.env
.env.*
.idea/
.mypy_cache/
.playwright-mcp/
.pyright/
.pytest_cache/
.venv/
.vscode/
ENV/
Thumbs.db
__pycache__/
build/
coverage.xml
dist/
htmlcov/
plane-integration/.agent_queue.jsonl
plane-integration/.provision_state.json
plane-integration/.webhook_deliveries.json
plane-selfhost/credentials.env
plane-selfhost/plane.env
venv/
plane-integration/.reorg_state.json
# Local MCP wiring may carry a machine-specific creds path; commit .mcp.json.example instead.
.mcp.json
# Spine loop-driver durable state + canary fire-sink — regenerated per run, never source.
run_state.json
.spine_hook_fires.jsonl
